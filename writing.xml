<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Yee Seng Chan — Writing</title>
<link>https://yeesengchan.com/writing.html</link>
<atom:link href="https://yeesengchan.com/writing.xml" rel="self" type="application/rss+xml"/>
<description>Essays on how modern language models work, and on building and evaluating the systems around them.</description>
<generator>quarto-1.10.18</generator>
<lastBuildDate>Sun, 02 Aug 2026 00:00:00 GMT</lastBuildDate>
<item>
  <title>AnswerTrail: building a deep-research agent over a bounded corpus</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/answertrail-agent/</link>
  <description><![CDATA[ 





<p>AnswerTrail answers questions about one AI/LLM YouTube channel by reading that channel’s video transcripts, and it checks every citation in its answer against those transcripts before you ever see the answer. It runs live on AWS.</p>
<p>The agent itself is the small part. The loop that drives it is about a page of control flow, and its answers hold up because of a citation check and two guardrails. The work is everything around the model, and it takes two articles.</p>
<p>This one builds the system that produces the answers: the corpus it reads, the agent harness that searches and cites over that corpus, and the AWS stack that serves the whole thing. The <a href="../../posts/answertrail-evaluation/index.html">second article</a> is the evaluation: curating the gold data and running the experiments that measure how well the agent answers.</p>
<section id="what-answertrail-is" class="level2">
<h2 class="anchored" data-anchor-id="what-answertrail-is">What AnswerTrail is</h2>
<p>AnswerTrail is a deep-research agent over a <strong>bounded corpus</strong>, a fixed body of text the agent is allowed to read and nothing else. The corpus here is the video transcripts of a single AI/LLM YouTube channel. Given a question, the agent runs a ReAct loop written by hand: it searches the transcripts, reads what comes back, decides whether it has enough, and writes an answer whose citations are validated against the corpus. Retrieval is dense-only over Postgres, the model is stateless, and the agent drives its own exploration until it decides it is ready to answer.</p>
<p>Bounding the corpus to one channel is the point, not a limitation. The model has no parametric knowledge of what these particular videos say, so every answer has to come from what the agent retrieves rather than from the model’s own memory. And because the agent may only read those transcripts, “the corpus cannot answer this” is a real outcome the system can reach and report, rather than a prompt it papers over. The live corpus is 475 videos from the channel’s most recent 16 months, cut into 25,401 transcript chunks, about 53 per video. It is live at <code>answertrail.yeesengchan.com</code>, though the site may be taken down at times to avoid running costs.</p>
</section>
<section id="the-corpus-is-built-once-offline" class="level2">
<h2 class="anchored" data-anchor-id="the-corpus-is-built-once-offline">The corpus is built once, offline</h2>
<p>Postgres holds the corpus, and the embeddings and the search index are built from it. An offline pipeline builds the corpus once per channel, in five stages:</p>
<ul>
<li><strong>List the videos.</strong> Enumerate the channel’s uploads through the YouTube Data API, drop anything 180 seconds or shorter so Shorts are excluded, and write a manifest of the survivors with their titles, descriptions, and publish dates.</li>
<li><strong>Fetch the transcripts.</strong> Pull each video’s caption track, retrying transient network drops with backoff so one dropped connection does not kill a long batch. The language, and whether the captions are auto-generated or human-written, is recorded per video.</li>
<li><strong>Clean the ASR.</strong> Auto-generated captions carry speech-recognition errors, so a <code>gpt-5.4-mini</code> pass repairs them in small batches, each batch given its neighbors as read-only context. A length guard keeps the raw text whenever the rewrite drifts too far from the original, and videos with human captions skip this stage.</li>
<li><strong>Chunk into 30-second windows.</strong> Group the caption lines into fixed 30-second windows, the atomic unit the agent later searches and cites. Each chunk’s id is <code>video_id:start_second</code>.</li>
<li><strong>Embed.</strong> Embed every chunk with OpenAI’s <code>text-embedding-3-large</code> and store the vector back in Postgres.</li>
</ul>
<p>The last three stages each read from Postgres the rows not yet at their stage, so the pipeline is resumable: a crash resumes where it stopped rather than restarting. A final stage builds a local OpenSearch index, used only by the offline evaluation for one retrieval comparison; production never touches it.</p>
<p>The embeddings are stored at 1536 dimensions rather than the model’s native 3072, because pgvector’s HNSW index caps vectors at 2000. That truncation is cheap because <code>text-embedding-3-large</code> produces <strong>Matryoshka embeddings</strong>: a leading prefix of the full vector is itself a complete embedding, with the earliest dimensions carrying the most information. Corpus and query are embedded the same way, so they share one vector space.</p>
</section>
<section id="the-agent-is-a-small-loop" class="level2">
<h2 class="anchored" data-anchor-id="the-agent-is-a-small-loop">The agent is a small loop</h2>
<p>With the corpus in place, the agent turns a question into an answer over it.</p>
<!-- ================= FIGURE 1 ================= -->
<div id="fig-react-loop" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A question enters a stateless agent loop. The loop either calls search_transcripts, which runs a dense pgvector search and returns the top-10 chunks to read, or calls mark_ready. A guardrail check, explored enough, sends the run back to keep searching until it has explored enough, then a final submit_answer turn validates every citation against the corpus before emitting the answer; a rejected citation returns to be fixed, and an accepted answer with citations is the loop's stop signal.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-react-loop-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/answertrail-agent/react-loop.png" class="img-fluid figure-img" alt="A question enters a stateless agent loop. The loop either calls search_transcripts, which runs a dense pgvector search and returns the top-10 chunks to read, or calls mark_ready. A guardrail check, explored enough, sends the run back to keep searching until it has explored enough, then a final submit_answer turn validates every citation against the corpus before emitting the answer; a rejected citation returns to be fixed, and an accepted answer with citations is the loop's stop signal.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-react-loop-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: The agent is a stateless ReAct loop with two moves each turn: search the transcripts, or mark itself ready. It keeps looping until it has run enough searches or reaches a 15-step budget, and only then does a final submit_answer turn run. That turn is a tool call like any other, so the loop validates every citation the answer makes against the corpus before accepting it, and a fabricated citation comes back on the normal tool channel to be corrected. An accepted answer with valid citations is what stops the loop.
</figcaption>
</figure>
</div>
<!-- ============================================ -->
<p>The model is stateless, so the loop holds the whole conversation and hands it back every turn, and the agent acts only through tools, which is what lets the loop stop it and force it to finish.</p>
<p>It has three tools:</p>
<ul>
<li><strong><code>search_transcripts</code></strong> embeds the query and runs a dense nearest-neighbour search over the pre-embedded chunks using the Postgres HNSW index, returning the top 10 by cosine similarity with no re-ranking, each with its full transcript text.</li>
<li><strong><code>mark_ready</code></strong> takes no arguments; the model calls it to end the search phase once it has gathered enough.</li>
<li><strong><code>submit_answer</code></strong> delivers the final answer, whose citations are ids only (<code>video_id</code>, <code>start_ts</code>, <code>end_ts</code>). The loop checks every citation against the index before accepting the answer, so a fabricated one is rejected and sent back to be corrected.</li>
</ul>
<p>Making the answer a tool is the design choice that carries the reliability, because producing the answer then runs through the same loop as searching. A rejected citation comes back on the normal tool-result channel to be fixed, an accepted answer is the loop’s stop signal, and <code>tool_choice</code> can force the <code>submit_answer</code> call when the step budget runs out.</p>
<p>Two guardrails keep the loop honest. The model must run at least 2 distinct searches before it may answer, so it cannot answer without exploring. And a run that keeps searching but finds nothing still stops, reporting that the corpus cannot answer rather than looping forever.</p>
<p>Every run is recorded as a <code>RunEvidenceState</code>: its searches and queries, the chunks retrieved and cited, the token usage and cost, and the outcome, stamped with the recipe version and the git SHA. That provenance is what lets the <a href="../../posts/answertrail-evaluation/index.html">second article</a> freeze one agent and score every variant against it.</p>
</section>
<section id="deployment-on-aws" class="level2">
<h2 class="anchored" data-anchor-id="deployment-on-aws">Deployment on AWS</h2>
<p>The system runs on AWS, defined entirely in Terraform and designed to be torn down and rebuilt on demand. No secret is kept in code or git.</p>
<!-- ================= FIGURE 2 ================= -->
<div id="fig-aws" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A browser fetches static web pages from CloudFront backed by S3, and makes API calls to a load balancer inside the VPC. The internet gateway is an attachment on the VPC boundary rather than a hop. Public subnets hold the load balancer and the Fargate container; private subnets hold the Postgres database. The container reaches the OpenAI API outbound through its own public IP, so no NAT gateway is needed, and receives secrets from Secrets Manager.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-aws-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/answertrail-agent/aws-architecture.png" class="img-fluid figure-img" alt="A browser fetches static web pages from CloudFront backed by S3, and makes API calls to a load balancer inside the VPC. The internet gateway is an attachment on the VPC boundary rather than a hop. Public subnets hold the load balancer and the Fargate container; private subnets hold the Postgres database. The container reaches the OpenAI API outbound through its own public IP, so no NAT gateway is needed, and receives secrets from Secrets Manager.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-aws-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: AnswerTrail on AWS. The browser loads a static site from S3 through CloudFront, and makes API calls to a load balancer inside one VPC. The load balancer and the Fargate container sit in public subnets across two availability zones; the Postgres database sits in private subnets. The container is given its own public IP so it can call the OpenAI API outbound through the internet gateway, which is what lets the project skip a paid NAT gateway, and it reads its secrets from Secrets Manager. Traffic only ever flows inward one tier at a time.
</figcaption>
</figure>
</div>
<!-- ============================================ -->
<p>The frontend is a static site on S3, served through CloudFront. The API is a single small Fargate container behind a load balancer that runs the agent and streams progress back over Server-Sent Events. Both sit in one VPC across two availability zones.</p>
<p>A few choices are worth calling out. The app container sits in a public subnet with its own public IP, so it can reach the OpenAI API through the internet gateway; the alternative, a private subnet, would need a NAT gateway at about $32 per month for the same outbound calls. Security is a chain of one-way tiers: the load balancer is the only thing open to the internet, the container accepts traffic only from the load balancer, and the database accepts traffic only from the container, each rule written against the tier’s security group rather than any IP address. The database is disposable, holding nothing that cannot be reloaded from a dump kept in S3, so the whole stack can be destroyed when idle and rebuilt later with a single <code>terraform apply</code>.</p>
<p>Deploys are automatic: every push to <code>main</code> builds the container image, ships it to ECS, then rebuilds the static frontend and clears the CloudFront cache, authenticating to AWS through OpenID Connect rather than stored keys.</p>
</section>
<section id="the-loop-is-small-on-purpose" class="level2">
<h2 class="anchored" data-anchor-id="the-loop-is-small-on-purpose">The loop is small on purpose</h2>
<p>The agent is a page of control flow, and its reliability comes from the citation check and two guardrails (it must search before it can answer, and it stops rather than invent one when the corpus has no answer). The work is everything around it: the ingestion pipeline that turns a channel into 25,401 searchable chunks, the agent harness that searches and cites over them, and the AWS stack that serves the whole thing and rebuilds from a single command.</p>
<p>Building that system is only half the effort. The other half is measuring it. The <a href="../../posts/answertrail-evaluation/index.html">second article</a> curates the evaluation data and runs the experiments that score the agent.</p>
</section>
<section id="code-companion" class="level2">
<h2 class="anchored" data-anchor-id="code-companion">Code companion</h2>
<p>The <a href="https://github.com/chanys/bounded-deep-research">bounded-deep-research repo</a> has the ingestion pipeline, the agent, and the full Terraform stack, and the system is live at <a href="https://answertrail.yeesengchan.com">answertrail.yeesengchan.com</a> (occasionally taken down to save on running costs). A short demonstration video is planned: ▸ EDIT ME.</p>


</section>

 ]]></description>
  <category>agents</category>
  <category>RAG</category>
  <category>retrieval</category>
  <category>AWS</category>
  <guid>https://yeesengchan.com/posts/answertrail-agent/</guid>
  <pubDate>Sun, 02 Aug 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>AnswerTrail: evaluating a deep-research agent over a bounded corpus</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/answertrail-evaluation/</link>
  <description><![CDATA[ 





<p>The <a href="../../posts/answertrail-agent/index.html">previous article</a> built AnswerTrail: a research agent that answers questions over one AI/LLM YouTube channel’s video transcripts, with every citation checked against the corpus. This article measures how well it answers.</p>
<p>AnswerTrail is scored on two kinds of question.</p>
<ul>
<li>A <strong>factual question</strong> asks for one specific fact, which usually sits in one place in the corpus.</li>
<li>A <strong>longitudinal question</strong> asks how the creator’s view on a topic changed over time, which means gathering and connecting evidence spread across many videos and many months.</li>
</ul>
<p>Plenty of write-ups show how to build a retrieval agent. Far fewer report what one actually scores, and fewer still say how much to trust the score. Most of this article is about how that measurement was built and checked:</p>
<ul>
<li><strong>Data curation.</strong> 26,240 claims are extracted from the transcripts and composed into questions with their answers. The pipeline is semi-automatic: an LLM proposes at every stage, and a human adjudicates what survives. The result is 25 factual questions and 35 longitudinal questions, each carrying the facts its answer must state.</li>
<li><strong>LLM judge calibration.</strong> Scoring uses one LLM judge, drawn from a different model family than the agent it grades. It agrees with a human on 66 blind-labeled pairs at Cohen’s kappa 0.879.</li>
</ul>
<section id="how-do-you-score-a-paragraph" class="level2">
<h2 class="anchored" data-anchor-id="how-do-you-score-a-paragraph">How do you score a paragraph?</h2>
<p>An answer from the agent is free text, often several paragraphs long, and there is no obvious way to put a number on prose. Marking the whole answer right or wrong throws away almost everything it says. Comparing it word by word against a reference answer scores the phrasing rather than the content.</p>
<p>A workable alternative is to decide in advance which individual facts a correct answer has to state, then check the answer for each one. Those facts are <strong>nuggets</strong>, the atomic units recall is scored against. A factual nugget is a single fact; a longitudinal nugget is a stance plus a time window.</p>
<p>Scoring this way comes from the AutoNuggetizer framework of <span class="citation" data-cites="pradeep_etal_2025">(Pradeep et al. 2025)</span>, which the TREC 2024 RAG track used to evaluate long-form answers: list the nuggets a good answer should contain, then check the answer against each one. In AnswerTrail the nuggets are generated by an LLM and then adjudicated by a human.</p>
<p>Nuggets are not written from scratch. They are extracted from claims, and claims are extracted from chunks:</p>
<ul>
<li>A <strong>chunk</strong> is a fixed 30-second transcript window, the atomic unit the agent searches and cites. Its id is <code>video_id:start_second</code>.</li>
<li>A <strong>claim</strong> is one atomic, faithful assertion a video makes, extracted from the transcript and tied to the chunk or chunks that state it. Its id is <code>video_id#cNNN</code>, and the extractor also attaches short topic and entity tags.</li>
<li>A <strong>nugget</strong> is one atomic fact a correct answer must state.</li>
</ul>
</section>
<section id="from-chunks-to-claims" class="level2">
<h2 class="anchored" data-anchor-id="from-chunks-to-claims">From chunks to claims</h2>
<p>Claims do double duty in the gold set. Every question is written from one or more claims, and every nugget is decomposed from a claim. For each video, the pipeline loads the video’s chunks in start-time order, renders them as a numbered list, and calls Sonnet 5 with thinking off and a claim-extraction prompt (snippet below). A single call produces, for each claim, its text, the chunk indices that support it, and its topic and entity tags.</p>
<pre><code>Extract EVERY concrete claim, finding, number, result, comparison, or stated opinion...
For each claim, list in evidence_indices the indices of the chunk(s) that support it.
Never cite a chunk that does not state the claim.</code></pre>
<p>A real extracted claim, with the tags produced in the same call:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode json code-with-copy"><code class="sourceCode json"><span id="cb2-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">{</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"claim_id"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"-HjPWrKavyA#c016"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb2-2">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"text"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span>     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Claude 4.5 scored 77% on general finance in this study."</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb2-3">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"chunk_ids"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"-HjPWrKavyA:00390"</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">,</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"-HjPWrKavyA:00420"</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb2-4">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"topics"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span>   <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"FIRE benchmark"</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">,</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"model comparison"</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb2-5">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"entities"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Claude 4.5"</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb2-6">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"confidence"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"high"</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">}</span></span></code></pre></div></div>
<p>Over the corpus this yields 26,240 claims, a median of 54 per video.</p>
</section>
<section id="factual-gold-25-questions-53-nuggets" class="level2">
<h2 class="anchored" data-anchor-id="factual-gold-25-questions-53-nuggets">Factual gold: 25 questions, 53 nuggets</h2>
<p>The factual tier asks for a single specific fact, so its gold is a small set of atomic facts per question, produced in four steps.</p>
<ol type="1">
<li><strong>Sampling videos over time.</strong> Order the 475 videos by date, split them into 5 equal bins of about 95, and draw 10 from each with a fixed seed. The 50 source videos then span the whole 16-month range instead of clustering on the channel’s busiest months.</li>
<li><strong>Composing questions from videos.</strong> The claims for all 475 videos already exist, so this step works from the sampled video’s claims. A cheap thinking-off Sonnet 5 call picks the 2 most question-worthy high-confidence ones, keeping one as primary and one as fallback. An adaptive-thinking call then writes a natural question from the primary claim.</li>
<li><strong>Human adjudication of the generated questions.</strong> A human vets each question against its claim and chunks. Questions get cut for containing their own answer, or for reading like something no real user would type: <em>“In a paper’s worked example on agent matchmaking with a pool of 100 agents… which agent was identified as the best fit for subtask one, and what was its similarity score?”</em></li>
<li><strong>Extracting nuggets from claims.</strong> Every question that survived adjudication carries the source claim it was written from. A Sonnet 5 call decomposes that claim into the atomic facts an answer must state, omitting anything the question already gives away.</li>
</ol>
<p>Adjudicating the 50 generated questions and their nuggets removed half of the questions, leaving 25 questions with 53 nuggets between them. One of them, with its source claim and the nuggets extracted from it:</p>
<pre><code>question : Which universities published ReasonFlux, and when did the paper come out?
claim    : The creator states that Princeton University and Peking University already
           implemented this exact approach, publishing it on February 10th, 2025,
           calling it ReasonFlux.
nuggets  : fc-0001_n1  "ReasonFlux was published by Princeton University."
           fc-0001_n2  "ReasonFlux was published by Peking University."
           fc-0001_n3  "ReasonFlux was published on February 10th, 2025."</code></pre>
</section>
<section id="longitudinal-gold-35-questions-102-nuggets" class="level2">
<h2 class="anchored" data-anchor-id="longitudinal-gold-35-questions-102-nuggets">Longitudinal gold: 35 questions, 102 nuggets</h2>
<p>The longitudinal tier draws on the same claims, but a change of view never shows up in a single claim. So the gold is built from a topic’s claims tracked over time, again in four steps.</p>
<ol type="1">
<li><p><strong>Building threads.</strong> A <strong>thread</strong> groups high-confidence claims that share a topic tag. A thread is kept only if it spans at least 3 videos and 90 days, which drops one-off topics and windows too short for a view to change. A stop-list removes generic tags like <code>benchmarks</code> and <code>methodology</code> first. An example thread, on the topic of reinforcement learning, is shown below.</p>
<pre><code>### topic:reinforcement learning  [videos=134, span=461d, claims=301]  2025-01-10 -&gt; 2026-04-16
  2025-01-10 [FR8oE8chp7c#c019] new data or RL approaches do not override the root-cause reasoning engine's outdated knowledge
  2025-02-02 [bjktcqGxxac#c039] RL-tuned models like o1 and R1 are optimized for final-answer correctness, not reasoning
  ... (+299 more claims)</code></pre></li>
<li><p><strong>Composing a question per thread.</strong> A thread holds many dated claims on one topic, and one adaptive-thinking Sonnet 5 call reads all of them. The call first rejects the thread if its claims never reflect a change of view over time, which removes about half of the 1,105 threads and leaves 505. For each thread that passes, it returns a question, 2 to 4 statements describing how the view changed, and 3 to 6 of the thread’s claims as dated evidence.</p></li>
<li><p><strong>Human review of the generated questions.</strong> That automated check still lets through threads whose claims merely share a topic word, like several unrelated facts about RAG. A human reads all 505 and keeps the 35 whose claims show a real change of view over time.</p></li>
<li><p><strong>Extracting nuggets from a thread’s claims.</strong> Not every fact in a thread becomes a nugget. The gold keeps only the facts that show how the view moved, the <strong>stance nuggets</strong>: where it started, each point where it turned, and where it ended.</p></li>
</ol>
<p>The nuggets for one longitudinal question:</p>
<pre><code>n1  He was optimistic about agentic AI's real-world capability and near-term
    impact [early 2025]
n2  He tempered that view: on realistic evaluation, agentic performance was
    weak [mid 2025]
n3  He concluded the anticipated agentic-AI revolution had stalled [early 2026]</code></pre>
</section>
<section id="one-judge-three-metrics" class="level2">
<h2 class="anchored" data-anchor-id="one-judge-three-metrics">One judge, three metrics</h2>
<p>With the gold built, the question is how the frozen agent scores against it. Scoring uses a single LLM judge that runs on a different model family, Claude Sonnet 5, from the OpenAI <code>gpt-5.4</code> agent it grades, so the system never grades its own output. The judge always does the same thing: given a CLAIM and a TEXT, it decides HIT or MISS on whether the text supports the claim. A snippet of the judge prompt:</p>
<pre><code>You decide whether a single CLAIM is supported by a given TEXT. Output a binary
verdict - HIT or MISS - and a one-sentence reason. There is no middle category...
Be strict about substance and lenient about wording. Do not reward a text that is
merely on the same topic; require that it actually supports the specific claim.</code></pre>
<p>Three metrics come out of that one judge, differing only in what is passed as the CLAIM and the TEXT:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 25%">
<col style="width: 25%">
<col style="width: 25%">
<col style="width: 25%">
</colgroup>
<thead>
<tr class="header">
<th>Metric</th>
<th>CLAIM</th>
<th>TEXT</th>
<th>What a HIT means</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Recall</td>
<td>a gold nugget</td>
<td>the agent’s answer</td>
<td>the answer states something it should</td>
</tr>
<tr class="even">
<td>Retrieval ceiling</td>
<td>a gold nugget</td>
<td>the chunks the agent retrieved</td>
<td>the evidence for that nugget was retrieved</td>
</tr>
<tr class="odd">
<td>Groundedness</td>
<td>an assertion extracted from the agent’s answer</td>
<td>the chunks the agent retrieved</td>
<td>that assertion is backed by the agent’s own evidence</td>
</tr>
</tbody>
</table>
<p>Recall and the retrieval ceiling both pass a gold nugget straight to the judge. Groundedness needs one step first, because the agent’s answer is prose rather than a list of assertions. A separate LLM call extracts the atomic assertions the answer makes, and each one becomes a CLAIM.</p>
<p>The <strong>retrieval ceiling</strong> is named for what it bounds: it is the highest recall the agent could reach from what its searches surfaced.</p>
</section>
<section id="can-we-trust-the-judge" class="level2">
<h2 class="anchored" data-anchor-id="can-we-trust-the-judge">Can we trust the judge?</h2>
<p>Every number rests on the judge’s verdicts, and an LLM judge is itself a fallible model, so before relying on it we calibrate it against a human. We draw 66 claim-text pairs and hand each to both the judge and a human, who each say HIT or MISS on whether the claim is supported by the text.</p>
<p>The pairs cover all three metrics: recall, retrieval, and groundedness. Each metric needs both clear hits and clear misses. Groundedness is almost always a hit, so we add a few constructed misses: one of the agent’s own claims paired with the chunks retrieved for a different, unrelated question, which cannot support it. The human labels all 66 blind, seeing only each claim and its text, with the judge’s verdict and reasoning hidden. Across the 66 pairs the judge called 36 hits and 30 misses, while the human labeled 32 hits and 34 misses.</p>
<p>The calibration score is Cohen’s kappa: how often the two agree, corrected for the agreement expected by chance, where <img src="https://latex.codecogs.com/png.latex?p_o"> is the observed agreement and <img src="https://latex.codecogs.com/png.latex?p_e"> the agreement expected if each labeled at its own hit and miss rate.</p>
<p><img src="https://latex.codecogs.com/png.latex?%5Ckappa%20=%20%5Cfrac%7Bp_o%20-%20p_e%7D%7B1%20-%20p_e%7D%0A%20%20%20%20%20%20%20=%20%5Cfrac%7B0.9394%20-%200.4986%7D%7B1%20-%200.4986%7D%20=%200.879"></p>
<p>The two agreed on 62 of the 66 pairs, so <img src="https://latex.codecogs.com/png.latex?p_o%20=%200.9394">, and the chance agreement implied by the two label distributions is <img src="https://latex.codecogs.com/png.latex?p_e%20=%20%5Cfrac%7B36%7D%7B66%7D%5Ccdot%5Cfrac%7B32%7D%7B66%7D%20+%20%5Cfrac%7B30%7D%7B66%7D%5Ccdot%5Cfrac%7B34%7D%7B66%7D%20=%200.4986">. A kappa of 0.879 is high agreement, so the scores below reflect the agent’s behavior rather than the judge’s quirks.</p>
</section>
<section id="the-results" class="level2">
<h2 class="anchored" data-anchor-id="the-results">The results</h2>
<p>The main study is simple to state: run the frozen agent over every gold question and score each answer. The agent (<code>gpt-5.4</code>, reasoning effort low, dense pgvector retrieval, top-10 per search, up to 15 steps) answered each of the 25 factual and 35 longitudinal questions three times.</p>
<!-- ================= FIGURE 1 ================= -->
<div id="fig-recall-decomposition" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Two horizontal stacked bars. Factual questions, 53 nuggets over 3 runs = 159: 135 expressed in the answer, 4 retrieved but not used, 20 never retrieved. Longitudinal questions, 102 nuggets over 3 runs = 306: 97 expressed, 48 retrieved but not used, 161 never retrieved. The longitudinal bar is dominated by the never-retrieved segment, the factual bar by the expressed segment.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-recall-decomposition-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/answertrail-evaluation/recall-decomposition.png" class="img-fluid figure-img" alt="Two horizontal stacked bars. Factual questions, 53 nuggets over 3 runs = 159: 135 expressed in the answer, 4 retrieved but not used, 20 never retrieved. Longitudinal questions, 102 nuggets over 3 runs = 306: 97 expressed, 48 retrieved but not used, 161 never retrieved. The longitudinal bar is dominated by the never-retrieved segment, the factual bar by the expressed segment.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-recall-decomposition-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Every gold nugget, by what happened to it. The leftmost segment is recall itself; the other two are the ways a nugget is missed. Almost every factual nugget is expressed in the answer, while more than half the longitudinal nuggets were never retrieved at all. That is the shape of a retrieval problem rather than a reasoning one: the agent cannot say what its searches never surfaced.
</figcaption>
</figure>
</div>
<!-- ============================================ -->
<table class="caption-top table">
<thead>
<tr class="header">
<th>Metric</th>
<th>Factual</th>
<th>Longitudinal</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Recall</td>
<td>84.9%</td>
<td>31.7%</td>
</tr>
<tr class="even">
<td>Retrieval ceiling</td>
<td>86.8%</td>
<td>43.5%</td>
</tr>
<tr class="odd">
<td>Groundedness</td>
<td>96.4%</td>
<td>98.0%</td>
</tr>
</tbody>
</table>
<p>Groundedness stays high on both tiers, so the low longitudinal score is not a case of the agent inventing content, and almost everything it asserts is backed by the chunks it retrieved.</p>
<p>Every figure here is pooled: a ratio of totals across all runs, rather than an average of per-question scores. The first two segments of a bar in the figure do not add up to the retrieval ceiling. For the longitudinal questions, 97 + 48 = 145 of 306 would be 47.4%, while the ceiling is 133 of 306, or 43.5%. The difference is 12 nuggets whose evidence was never retrieved but which the answer stated anyway, so they sit in the expressed segment while counting as not retrieved. The factual side has one such case, which is why 135 + 4 = 139 becomes a ceiling of 138 of 159.</p>
<p>The two tiers even work differently. A factual run took about 5 reasoning loops and about 4 searches, and surfaced about 19 chunks for synthesis. A longitudinal run worked harder, about 7 loops and about 9 searches, often firing several searches in one loop, and surfaced about 40 chunks.</p>
<p>Longitudinal questions are inherently harder to answer, so the lower score there is expected. A factual answer needs one fact that usually sits in one place, while a longitudinal answer has to find several stances spread across a year and then connect them.</p>
</section>
<section id="what-would-move-it" class="level2">
<h2 class="anchored" data-anchor-id="what-would-move-it">What would move it</h2>
<p>With a judge we can trust, we can perform two pre-registered ablations.</p>
<ul>
<li><strong>Hybrid retrieval.</strong> Swap dense retrieval for dense plus BM25, fused with reciprocal rank fusion, on the longitudinal questions. The single hybrid run landed about 3 points below the dense baseline on both recall and the retrieval ceiling.</li>
<li><strong>No agent loop.</strong> Replace the ReAct loop with one dense retrieval on the raw question and one synthesis call. Recall falls from 84.9% to 79.2% on factual questions and from 31.7% to 23.5% on longitudinal ones, so looping and issuing several different queries is worth 5.7 and 8.2 points.</li>
</ul>
</section>
<section id="in-short" class="level2">
<h2 class="anchored" data-anchor-id="in-short">In short</h2>
<p>The factual-versus-longitudinal gap, 85% against 32%, is dominantly a retrieval problem: evidence for barely 44% of what a trajectory answer must say is ever retrieved. A better ranker does not get past it. The agent’s reasoning is not the bottleneck. The next thing worth building is retrieval that can surface evidence spread thinly across time, which is a different problem from making the agent smarter.</p>
</section>
<section id="code-companion" class="level2">
<h2 class="anchored" data-anchor-id="code-companion">Code companion</h2>
<p>The <a href="https://github.com/chanys/bounded-deep-research">bounded-deep-research repo</a> has the evaluation code under <code>eval/</code>: the claim and nugget extraction, the judge, the calibration, and both ablations.</p>



</section>

<div id="quarto-appendix" class="default"><section class="quarto-appendix-contents" id="quarto-bibliography"><h2 class="anchored quarto-appendix-heading">References</h2><div id="refs" class="references csl-bib-body hanging-indent">
<div id="ref-pradeep_etal_2025" class="csl-entry">
Pradeep, Ronak, Nandan Thakur, Shivani Upadhyay, Daniel Campos, Nick Craswell, and Jimmy Lin. 2025. <span>“The Great Nugget Recall: Automating Fact Extraction and RAG Evaluation with Large Language Models.”</span> <em>Proceedings of the 48th International ACM SIGIR Conference on Research and Development in Information Retrieval (SIGIR 2025)</em>. <a href="https://doi.org/10.1145/3726302.3730090">https://doi.org/10.1145/3726302.3730090</a>.
</div>
</div></section></div> ]]></description>
  <category>evaluation</category>
  <category>RAG</category>
  <category>retrieval</category>
  <category>LLM judge</category>
  <guid>https://yeesengchan.com/posts/answertrail-evaluation/</guid>
  <pubDate>Sun, 02 Aug 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>What MCP Actually Removes From a Host</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/what-an-agent-actually-is/03-what-mcp-removes/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>What an agent actually is</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-what-is-an-agent/">What the heck is an AI agent?</a></li>
<li><a href="../02-agent-frameworks/">Behind the scenes of AI agent frameworks</a></li>
<li><a href="../03-what-mcp-removes/">What MCP Actually Removes From a Host</a></li>
</ol>
</div>
<p>The <a href="../../../../posts/series/what-an-agent-actually-is/02-agent-frameworks/index.html">previous article</a> showed the host building its tool list and dispatch map by hand, and this one is about what MCP does to those two structures.</p>
<p>The Model Context Protocol is usually introduced with a diagram. On the left, (M) AI applications. On the right, (N) tools. Between them, a dense mesh of M×N one-off integrations. Then a second diagram: MCP in the middle, M+N connections, order restored.</p>
<p>The claim sounds reasonable, but a diagram is not something you can check. I wanted to know what it meant in actual host code, so I built the smallest thing that would tell me.</p>
<section id="the-three-roles" class="level2">
<h2 class="anchored" data-anchor-id="the-three-roles">The three roles</h2>
<p>MCP is a JSON-RPC protocol connecting an AI application to external tools.</p>
<ul>
<li>The <strong>host</strong> is the application you write. It owns the model calls, the permissions, and the loop that ties them together.</li>
<li>The <strong>client</strong> is a component inside the host managing one connection to one server. The SDK provides it as <code>ClientSession</code>, so you never write protocol code.</li>
<li>The <strong>server</strong> is a program exposing capabilities, written by whoever owns the tool.</li>
</ul>
<p>Servers commonly expose tools, and also resources (read-only data) and prompts (user-selected templates). This experiment only uses tools.</p>
</section>
<section id="the-experiment" class="level2">
<h2 class="anchored" data-anchor-id="the-experiment">The experiment</h2>
<p>I wrote a small MCP server that reads a CSV of Massachusetts town data and exposes four tools: median home price, school rating, crime rate, and distance to Boston.</p>
<p>Then I wrote two hosts against it. The first one takes the tool name from the command line. There is no LLM involved at all, so I could watch the protocol messages without model behavior getting in the way. The second one adds an LLM: the model proposes which tool to call, and the host checks that call before running it.</p>
<p>What follows compares that second host against the version I would have to write without MCP. That version is not in the repo, it is the thing this article is about.</p>
<p>One note on wording: I use “model” and “LLM” interchangeably throughout.</p>
</section>
<section id="without-mcp-two-structures-that-grow" class="level2">
<h2 class="anchored" data-anchor-id="without-mcp-two-structures-that-grow">Without MCP: two structures that grow</h2>
<p>Suppose there is no MCP. You have two tool sources: a Python package for the town data, and a weather service with an HTTP API and no package at all. To let a model use either one, you have to write two things yourself, and both live in your host.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb1-1"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> requests</span>
<span id="cb1-2"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> town_explorer</span>
<span id="cb1-3"></span>
<span id="cb1-4">TOOLS <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> [</span>
<span id="cb1-5">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"name"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"get_housing"</span>,</span>
<span id="cb1-6">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Median home price for one town, in USD."</span>,</span>
<span id="cb1-7">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"parameters"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"object"</span>,</span>
<span id="cb1-8">                    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"properties"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"town"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"string"</span>}},</span>
<span id="cb1-9">                    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"required"</span>: [<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"town"</span>]}},</span>
<span id="cb1-10">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"name"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"get_schools"</span>,</span>
<span id="cb1-11">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"School district rating for one town, 1 to 10."</span>,</span>
<span id="cb1-12">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"parameters"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"object"</span>,</span>
<span id="cb1-13">                    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"properties"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"town"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"string"</span>}},</span>
<span id="cb1-14">                    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"required"</span>: [<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"town"</span>]}},</span>
<span id="cb1-15">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"name"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"get_forecast"</span>,</span>
<span id="cb1-16">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Weather forecast for a coordinate."</span>,</span>
<span id="cb1-17">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"parameters"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"object"</span>,</span>
<span id="cb1-18">                    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"properties"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"lat"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"number"</span>},</span>
<span id="cb1-19">                                   <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"lon"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"number"</span>}},</span>
<span id="cb1-20">                    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"required"</span>: [<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"lat"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"lon"</span>]}},</span>
<span id="cb1-21">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># one block per tool</span></span>
<span id="cb1-22">]</span>
<span id="cb1-23"></span>
<span id="cb1-24"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># The weather service has no Python package, so the call is mine to write.</span></span>
<span id="cb1-25"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> get_forecast(lat, lon):</span>
<span id="cb1-26">    r <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> requests.get(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"https://api.example.com/forecast"</span>,</span>
<span id="cb1-27">                     params<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>{<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"lat"</span>: lat, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"lon"</span>: lon})</span>
<span id="cb1-28">    r.raise_for_status()</span>
<span id="cb1-29">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> r.json()[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"properties"</span>][<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"forecast"</span>]</span>
<span id="cb1-30"></span>
<span id="cb1-31">DISPATCH <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb1-32">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"get_housing"</span>:  town_explorer.get_housing,</span>
<span id="cb1-33">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"get_schools"</span>:  town_explorer.get_schools,</span>
<span id="cb1-34">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"get_forecast"</span>: get_forecast,</span>
<span id="cb1-35">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># one entry per tool</span></span>
<span id="cb1-36">}</span></code></pre></div></div>
<p><code>TOOLS</code> is what the model sees: each tool’s name, what it does, and what arguments it takes, written in the format the model provider (OpenAI, Anthropic) expects. <code>DISPATCH</code> is how the host gets from the name the model picked back to the function that runs. Both grow by one entry every time you add a tool.</p>
<p>Notice that the two sources are not even the same kind of work. The town data arrived as a Python package I can import. The weather service did not, so the HTTP call, the error handling, and the digging into the response body are all mine. Every new source is its own small integration project, and the shape of it depends on what the owner happened to ship.</p>
<p>Notice what the <code>TOOLS</code> entry actually is. Someone else wrote <code>get_housing</code>, and they decided it takes one argument called <code>town</code>. My <code>TOOLS</code> entry is my handwritten copy of that decision. Nothing checks that the copy is still right. If they release a new version where <code>get_housing</code> also requires a <code>year</code>, my copy still tells the model there is one argument, and the model keeps calling it the old way.</p>
</section>
<section id="with-mcp-the-catalog-arrives-at-runtime" class="level2">
<h2 class="anchored" data-anchor-id="with-mcp-the-catalog-arrives-at-runtime">With MCP: the catalog arrives at runtime</h2>
<section id="the-server-declares-its-tools-once" class="level3">
<h3 class="anchored" data-anchor-id="the-server-declares-its-tools-once">The server declares its tools once</h3>
<p>Now the same tools, but the owner has wrapped them in an MCP server instead of shipping a package. On their side, each tool is an ordinary function with a decorator on it. <code>FastMCP</code> is the class that turns a Python module into an MCP server, and <code>@mcp.tool()</code> marks a function as something the server should offer:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb2-1"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">from</span> mcp.server.fastmcp <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> FastMCP</span>
<span id="cb2-2"></span>
<span id="cb2-3">mcp <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> FastMCP(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"towns"</span>)</span>
<span id="cb2-4"></span>
<span id="cb2-5"><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">@mcp.tool</span>()</span>
<span id="cb2-6"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> get_housing(town: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span>) <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span> <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">dict</span>:</span>
<span id="cb2-7">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"""Median home price for one town, in USD."""</span></span>
<span id="cb2-8">    ...</span></code></pre></div></div>
<p>That decorator is doing the work that <code>TOOLS</code> used to do. FastMCP reads the function’s signature and builds the argument schema from it: the parameter name <code>town</code> becomes a property, the <code>str</code> annotation becomes <code>"type": "string"</code>, and because the parameter has no default it goes into the required list. The docstring becomes the tool’s description, which is the sentence the model reads when deciding whether to call it. Asking the server what it offers returns exactly that:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode json code-with-copy"><code class="sourceCode json"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb3-2">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"name"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"get_housing"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb3-3">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"description"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Median home price for one town, in USD."</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb3-4">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"inputSchema"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb3-5">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"type"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"object"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb3-6">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"properties"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">{</span><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"town"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">{</span><span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"title"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Town"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span> <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"type"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"string"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">}},</span></span>
<span id="cb3-7">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"required"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"town"</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb3-8">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"title"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"get_housingArguments"</span></span>
<span id="cb3-9">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">}</span></span>
<span id="cb3-10"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">}</span></span></code></pre></div></div>
<p>The tool owner wrote a function and a docstring. Everything else was generated.</p>
<p>The weather service gets the same treatment. Its server is a thin wrapper around the same HTTP call I wrote by hand earlier:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb4-1">mcp <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> FastMCP(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"weather"</span>)</span>
<span id="cb4-2"></span>
<span id="cb4-3"><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">@mcp.tool</span>()</span>
<span id="cb4-4"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> get_forecast(lat: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">float</span>, lon: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">float</span>) <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span> <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">dict</span>:</span>
<span id="cb4-5">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"""Weather forecast for a coordinate."""</span></span>
<span id="cb4-6">    r <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> requests.get(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"https://api.example.com/forecast"</span>,</span>
<span id="cb4-7">                     params<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>{<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"lat"</span>: lat, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"lon"</span>: lon})</span>
<span id="cb4-8">    r.raise_for_status()</span>
<span id="cb4-9">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> r.json()[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"properties"</span>]</span></code></pre></div></div>
<p>The GET request did not disappear. It moved into the server, where it belongs to whoever owns the service rather than to me.</p>
</section>
<section id="the-host-asks-what-is-available" class="level3">
<h3 class="anchored" data-anchor-id="the-host-asks-what-is-available">The host asks what is available</h3>
<p>Three names from the MCP Python SDK show up on this side, so briefly:</p>
<ul>
<li><code>StdioServerParameters</code> says how to start a server: a command and its arguments, the same thing you would type in a shell.</li>
<li><code>stdio_client</code> runs that command as a child process and hands back two pipes, one to read from and one to write to. It moves bytes and knows nothing about MCP.</li>
<li><code>ClientSession</code> wraps those pipes and speaks JSON-RPC, so <code>initialize</code>, <code>list_tools</code>, and <code>call_tool</code> are method calls rather than JSON messages you assemble yourself. This is the MCP client I mentioned earlier, the piece inside the host that manages one connection to one server.</li>
</ul>
<p>Here is a complete host program that connects to two servers, one for town data and one for weather, and builds the same two structures I would otherwise have written by hand:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb5-1"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> asyncio, sys</span>
<span id="cb5-2"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">from</span> contextlib <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> AsyncExitStack</span>
<span id="cb5-3"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">from</span> mcp <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> ClientSession, StdioServerParameters</span>
<span id="cb5-4"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">from</span> mcp.client.stdio <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> stdio_client</span>
<span id="cb5-5"></span>
<span id="cb5-6"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Each server is just a command to run. Both of mine are Python,</span></span>
<span id="cb5-7"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># but a server can be anything: an npm package, a Go binary, a remote URL.</span></span>
<span id="cb5-8">SERVERS <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb5-9">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"towns"</span>:   [sys.executable, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"towns_server.py"</span>],</span>
<span id="cb5-10">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"weather"</span>: [sys.executable, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"weather_server.py"</span>],</span>
<span id="cb5-11">}</span>
<span id="cb5-12"></span>
<span id="cb5-13"><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">async</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> main():</span>
<span id="cb5-14">    tools <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> []      <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># what the model sees</span></span>
<span id="cb5-15">    dispatch <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {}   <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># how the host runs a call</span></span>
<span id="cb5-16"></span>
<span id="cb5-17">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">async</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">with</span> AsyncExitStack() <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">as</span> stack:   <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># bookkeeping: closes every connection on the way out</span></span>
<span id="cb5-18">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> server_name, cmd <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> SERVERS.items():</span>
<span id="cb5-19">            <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Start the server as a child process and open a session on it.</span></span>
<span id="cb5-20">            params <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> StdioServerParameters(command<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>cmd[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>], args<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>cmd[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>:])</span>
<span id="cb5-21">            read, write <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">await</span> stack.enter_async_context(stdio_client(params))</span>
<span id="cb5-22">            session <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">await</span> stack.enter_async_context(ClientSession(read, write))</span>
<span id="cb5-23">            <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">await</span> session.initialize()          <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># agree on protocol version</span></span>
<span id="cb5-24"></span>
<span id="cb5-25">            <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Ask this server what it offers, and record each tool twice:</span></span>
<span id="cb5-26">            <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># once for the model to read, once so we know how to run it</span></span>
<span id="cb5-27">            <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> t <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> (<span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">await</span> session.list_tools()).tools:</span>
<span id="cb5-28">                <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># prefix the name: two servers can both offer "search"</span></span>
<span id="cb5-29">                qualified <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">f"</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span>server_name<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span><span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">__</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span>t<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">.</span>name<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span><span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span></span>
<span id="cb5-30">                tools.append({<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"name"</span>: qualified,</span>
<span id="cb5-31">                              <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: t.description,</span>
<span id="cb5-32">                              <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"parameters"</span>: t.inputSchema})</span>
<span id="cb5-33">                dispatch[qualified] <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> (session, t.name)</span>
<span id="cb5-34"></span>
<span id="cb5-35">        <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Later, when the model picks a tool by its qualified name:</span></span>
<span id="cb5-36">        session, real_name <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> dispatch[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"towns__get_housing"</span>]</span>
<span id="cb5-37">        result <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">await</span> session.call_tool(real_name, {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"town"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Winchester"</span>})</span>
<span id="cb5-38">        <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">print</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">""</span>.join(b.text <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> b <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> result.content))</span>
<span id="cb5-39"></span>
<span id="cb5-40">asyncio.run(main())</span></code></pre></div></div>
<p>Running it prints the three tools the two servers between them offer, and then the result of the call:</p>
<pre class="text"><code>towns__get_housing    | Median home price for one town, in USD.       | ['town']
towns__get_schools    | School district rating for one town, 1 to 10. | ['town']
weather__get_forecast | Weather forecast for a coordinate.            | ['lat', 'lon']

{"town": "Winchester", "median_home_price": 1250000}</code></pre>
<p><code>tools</code> and <code>dispatch</code> are the same two structures as before, doing the same two jobs. The difference is that nothing in the host program describes a tool. The loop asks each server and writes down the answer, and the host program would be identical if the servers offered thirty tools instead of three.</p>
<p>Without MCP, the two structures grow linearly: every tool added means one more hand-written entry in each. With MCP they do not grow at all.</p>
<p>That is a specific claim, and I want to keep it specific. Plenty of other things still grow as you add tools: the context the schemas take up, the validation you write, the permissions you decide, the tests. Adding tools still costs something. The bookkeeping is what went away.</p>
<div id="fig-before-after" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Two panels. Without MCP, the host holds a TOOLS list and a DISPATCH map with one hand-written entry per tool source, wired separately to each source. With MCP, the host still has TOOLS and DISPATCH, but builds both at startup from what each server reports over tools/list, and each server owns its own tool descriptions.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-before-after-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/what-an-agent-actually-is/03-what-mcp-removes/before-after-mcp.png" class="img-fluid figure-img" alt="Two panels. Without MCP, the host holds a TOOLS list and a DISPATCH map with one hand-written entry per tool source, wired separately to each source. With MCP, the host still has TOOLS and DISPATCH, but builds both at startup from what each server reports over tools/list, and each server owns its own tool descriptions.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-before-after-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: The same host, written twice. On the left, every tool source adds one entry to TOOLS, describing the tool for the model, and one entry to DISPATCH, pointing the tool’s name at the function that runs it. Both are written by hand, both are copies of decisions made in somebody else’s code, and both grow with each source added. On the right the host still has TOOLS and DISPATCH, but it no longer writes them: it asks each server what it offers at startup and fills both structures from the answer. The descriptions did not disappear, they moved to the party that owns the function.
</figcaption>
</figure>
</div>
</section>
</section>
<section id="discovery-is-the-part-doing-the-work" class="level2">
<h2 class="anchored" data-anchor-id="discovery-is-the-part-doing-the-work">Discovery is the part doing the work</h2>
<p>This is the drift problem from earlier. Without tool discovery via <code>session.list_tools()</code>, the host is holding its own description of somebody else’s function. It is right on the day you write it, and after that nobody is checking. The model can only work from what that description says, so when it goes stale the model is the one calling the tool wrongly.</p>
<div id="fig-schema-drift" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Two columns. On the left, the tool owner's function. On the right, the host's hand-written copy of its description. At first both say get_housing takes one argument, town, and they match. Later the owner adds a second required argument, year, but the host copy still says one argument, so the model calls the tool the old way.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-schema-drift-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/what-an-agent-actually-is/03-what-mcp-removes/schema-drift.png" class="img-fluid figure-img" alt="Two columns. On the left, the tool owner's function. On the right, the host's hand-written copy of its description. At first both say get_housing takes one argument, town, and they match. Later the owner adds a second required argument, year, but the host copy still says one argument, so the model calls the tool the old way.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-schema-drift-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: Why the hand-written copy is a liability. At first the two agree, because the copy was written by reading the function. Later the tool owner adds a second required argument, and nothing connects that change to the host’s copy, so the copy keeps describing the old version. The result is either a TypeError when the arguments reach a function that no longer accepts them, or a wrong answer that nobody notices. Neither side did anything wrong, which is what makes it easy to miss.
</figcaption>
</figure>
</div>
<p><code>tools/list</code> fixes this by asking the server every time the host starts. The server owns its tools, so the server’s description is the correct one, and that is the description the model sees.</p>
<p>MCP does more than discovery. It also standardizes how a session starts, how tools get invoked, how results and errors come back, and how the messages travel.</p>
</section>
<section id="what-mcp-does-not-solve" class="level2">
<h2 class="anchored" data-anchor-id="what-mcp-does-not-solve">What MCP does not solve</h2>
<p><strong>Provider adapters remain.</strong> MCP hands the host <code>tool.name</code>, <code>tool.description</code>, and <code>tool.inputSchema</code>, and stops there. The host still has to wrap those three fields in whatever shape the model provider expects, and every provider expects a different one:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb7-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> to_openai_tools(mcp_tools):</span>
<span id="cb7-2">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> [{<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"name"</span>: t.name,</span>
<span id="cb7-3">             <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: t.description,</span>
<span id="cb7-4">             <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"parameters"</span>: normalize_for_openai(t.inputSchema)}</span>
<span id="cb7-5">            <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> t <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> mcp_tools]</span>
<span id="cb7-6"></span>
<span id="cb7-7"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> to_claude_tools(mcp_tools):</span>
<span id="cb7-8">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> [{<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"name"</span>: t.name, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: t.description,</span>
<span id="cb7-9">             <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"input_schema"</span>: normalize_for_claude(t.inputSchema)}</span>
<span id="cb7-10">            <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> t <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> mcp_tools]</span></code></pre></div></div>
<p>These are short because OpenAI, Anthropic, and MCP all describe tool arguments using JSON Schema, so most of the time only the wrapper around the schema changes. It does not always copy cleanly, though: FastMCP emits fields like <code>title</code> that providers do not need, and providers support different subsets of JSON Schema, so a real adapter normalizes rather than passes through.</p>
<p>The useful thing about this cost is that it does not grow. You write one adapter per provider, and it stays the same whether you connect four tools or forty.</p>
<p><strong>Tool schemas cost you context.</strong> If your host sends the whole tool list with every request, which is the obvious way to write it, the model re-reads all those schemas every turn. The fix is host-side: keep the full definitions in your own memory, show the model names and descriptions, and load a schema only when it picks that tool. The protocol does not help here, and the <a href="https://modelcontextprotocol.io/docs/develop/clients/client-best-practices">MCP client best practices</a> are explicit that this is the host’s problem.</p>
</section>
<section id="when-mcp-is-worth-it" class="level2">
<h2 class="anchored" data-anchor-id="when-mcp-is-worth-it">When MCP is worth it</h2>
<p>If the tool is a Python function I wrote myself, importing it is simpler. Calling it is a dictionary lookup. Going through MCP means starting a subprocess, opening a session, and making the whole host async to get to the same function. For one host and a handful of stable local tools, MCP can easily cost more than it saves.</p>
<p>The reasons to use it anyway are not about writing less code.</p>
<p>It becomes worth it when at least one of these holds:</p>
<ul>
<li>The tool is owned by someone else, so their schema changes propagate without my involvement.</li>
<li>The tool is written in another language.</li>
<li>The tool runs in another process or remotely.</li>
<li>Dependency or crash isolation matters. An ordinary import runs inside the host interpreter and creates no process boundary.</li>
<li>The application is a general-purpose host whose users connect it to servers I have never seen.</li>
</ul>
</section>
<section id="code-companion" class="level2">
<h2 class="anchored" data-anchor-id="code-companion">Code companion</h2>
<p>The <a href="https://github.com/chanys/mcp-town-explorer">mcp-town-explorer repo</a> has the server, the command-line host, and the LLM host. The command-line host takes a <code>--verbose</code> flag that prints every protocol message to stderr, which is the fastest way to see what initialization, discovery, and invocation actually look like on the wire.</p>
</section>
<section id="closing" class="level2">
<h2 class="anchored" data-anchor-id="closing">Closing</h2>
<p>The host no longer writes a schema entry and a dispatch entry for every tool. The server publishes them, the host asks at startup, and the copy that used to go stale no longer exists.</p>
<p>What MCP moved is the tool description, back to the party that owns the tool, with one standard way for the host to discover and invoke it.</p>


</section>

 ]]></description>
  <category>What an agent actually is</category>
  <guid>https://yeesengchan.com/posts/series/what-an-agent-actually-is/03-what-mcp-removes/</guid>
  <pubDate>Thu, 23 Jul 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Your eval system also drifts</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/llm-evaluation-honestly/07-eval-drift/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>LLM evaluation, honestly</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-stop-vibe-checking/">Stop vibe-checking your agent</a></li>
<li><a href="../02-eval-dataset/">Read traces before you write the labeling guide</a></li>
<li><a href="../03-judge-vs-code/">Don’t ask an LLM judge what code can check</a></li>
<li><a href="../04-judge-is-classifier/">Your LLM judge is a classifier</a></li>
<li><a href="../05-scores-are-samples/">Eval scores are samples, not truth</a></li>
<li><a href="../06-rag-evaluation/">Your RAG score hides the diagnosis</a></li>
<li><a href="../07-eval-drift/">Your eval system also drifts</a></li>
</ol>
</div>
<p>An eval system has to be maintained like the product it measures.</p>
<p>The running example is the docs Q&amp;A agent from <a href="../../../../posts/series/llm-evaluation-honestly/04-judge-is-classifier/index.html">the judge calibration</a> and <a href="../../../../posts/series/llm-evaluation-honestly/06-rag-evaluation/index.html">RAG evaluation</a> articles. It answers retention-policy questions. At launch, the eval stack looks solid: retrieval checks, faithfulness judges, answer relevance judges, and answerability tests.</p>
<p>Six months later, the dashboard is still green. That only means the agent still passes the old eval.</p>
<p>Suppose the retention policy changes. Trial accounts used to retain data for 90 days. Now they retain data for 120 days. The docs are updated, and the agent correctly answers “120 days.” The eval set still expects “90 days,” so the dashboard reports a regression.</p>
<p>The agent did not regress; the eval system became stale.</p>
<p>The fix is eval maintenance:</p>
<ul>
<li><strong>Update the active case:</strong> the current expected answer is now 120 days.</li>
<li><strong>Version the old case:</strong> the 90-day case applies only to the previous policy.</li>
<li><strong>Update judge examples:</strong> examples that assume 90 days should not teach the judge the old rule.</li>
<li><strong>Add a new regression case:</strong> the 120-day case should gate future releases.</li>
</ul>
<p>The old case should not disappear. It may still matter for archived records or historical behavior. But it should not decide whether the current product is correct.</p>
<section id="eval-cases-need-versioning" class="level2">
<h2 class="anchored" data-anchor-id="eval-cases-need-versioning">Eval cases need versioning</h2>
<p>A regression case carries assumptions about the product. Those assumptions need to be visible.</p>
<p>Each case should record:</p>
<ul>
<li><strong>Policy version:</strong> which rule the case expects.</li>
<li><strong>Document version:</strong> which source document supports the expected answer.</li>
<li><strong>Added date:</strong> when the case entered the eval set.</li>
<li><strong>Reason added:</strong> production incident, known failure mode, launch coverage, or regression.</li>
<li><strong>Release-gating status:</strong> whether the case currently blocks a release.</li>
</ul>
<p>Without these fields, stale examples look authoritative.</p>
</section>
<section id="production-traces-keep-the-eval-current" class="level2">
<h2 class="anchored" data-anchor-id="production-traces-keep-the-eval-current">Production traces keep the eval current</h2>
<p>A locked eval set protects known failures but does not discover new ones.</p>
<p>Production traces show what users are asking now, what the agent is retrieving now, and which answers are failing now. Sample them regularly and feed the important cases back into the eval set.</p>
<p>Use a small sampling plan:</p>
<ul>
<li><strong>Random samples:</strong> catch normal failures that have no obvious signal.</li>
<li><strong>Important slices:</strong> preserve coverage of high-risk areas like retention, billing, security, or permissions.</li>
<li><strong>Failure signals:</strong> review thumbs down, repeated queries, low retrieval scores, or missing citations.</li>
<li><strong>Incidents:</strong> turn serious production failures into regression cases.</li>
</ul>
<p>User complaints are useful, but they are incomplete. Users often rephrase, fix the answer manually, ask someone else, or abandon the feature. Random and slice-based sampling catch quiet failures that complaints miss.</p>
</section>
<section id="judges-need-rechecking" class="level2">
<h2 class="anchored" data-anchor-id="judges-need-rechecking">Judges need rechecking</h2>
<p>A judge is valid only under the conditions where it was tested. Recheck judges when the conditions change:</p>
<ul>
<li><strong>Judge model changes:</strong> compare the new judge against human labels.</li>
<li><strong>Knowledge base changes:</strong> re-run judge validation on current documents.</li>
<li><strong>Correctness standard changes:</strong> update the labeling guide, judge prompt, and examples.</li>
<li><strong>Traffic changes:</strong> test the judge on recent production traces.</li>
</ul>
<p>The recheck does not need to be huge. Sample recent traces, label them with the current standard, and compare the judge’s verdicts against the human labels. If agreement drops, update the judge before trusting its score.</p>
</section>
<section id="the-eval-system-is-part-of-the-product" class="level2">
<h2 class="anchored" data-anchor-id="the-eval-system-is-part-of-the-product">The eval system is part of the product</h2>
<p>A dashboard is useful only if the eval still matches the product.</p>
<p>The eval system has to track current policies, current documents, current traffic, judge validity, and the team’s standard for correctness. Production traces become labels. Labels become failure modes. Failure modes become checks, judges, and regression cases. Incidents update the suite.</p>
<p>Teams that treat eval as first-class code version, review, monitor, and maintain it; teams that do not eventually read numbers they can no longer trust.</p>
<p>A reading like “the eval system says we’re at 87%” is only worth trusting if you know when the eval system was last checked to confirm it still tells the truth.</p>


</section>

 ]]></description>
  <category>LLM evaluation, honestly</category>
  <guid>https://yeesengchan.com/posts/series/llm-evaluation-honestly/07-eval-drift/</guid>
  <pubDate>Fri, 05 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Your RAG score hides the diagnosis</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/llm-evaluation-honestly/06-rag-evaluation/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>LLM evaluation, honestly</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-stop-vibe-checking/">Stop vibe-checking your agent</a></li>
<li><a href="../02-eval-dataset/">Read traces before you write the labeling guide</a></li>
<li><a href="../03-judge-vs-code/">Don’t ask an LLM judge what code can check</a></li>
<li><a href="../04-judge-is-classifier/">Your LLM judge is a classifier</a></li>
<li><a href="../05-scores-are-samples/">Eval scores are samples, not truth</a></li>
<li><a href="../06-rag-evaluation/">Your RAG score hides the diagnosis</a></li>
<li><a href="../07-eval-drift/">Your eval system also drifts</a></li>
</ol>
</div>
<p>Most RAG dashboards score one thing: whether the answer was good. The same bad answer can come from retrieval (missed the right passage, returned too much noise) or from the generator (ignored the context, answered the wrong question, answered when it should have declined). A single quality score hides which.</p>
<p>The running example is a documentation Q&amp;A agent for a data-retention policy: 90 days for trial accounts, 30 days for standard, escalation for enterprise accounts on legal hold.</p>
<section id="capture-the-full-rag-trace" class="level2">
<h2 class="anchored" data-anchor-id="capture-the-full-rag-trace">Capture the full RAG trace</h2>
<p>A RAG eval needs the question, the retrieved context, and the final answer. Without the retrieved context, you can’t tell whether retrieval failed, the generator failed, or the system should have refused.</p>
<p>The trace also needs enough metadata to reproduce the result against the same knowledge base: document and chunk IDs, retrieval scores, top-k settings, embedding model, reranker version, metadata filters, and the exact text passed to the generator. For agentic RAG, capture tool selection too; if the agent picked the wrong source, retrieval metrics inside the right index will look fine while the system still fails.</p>
</section>
<section id="five-questions-locate-the-failure" class="level2">
<h2 class="anchored" data-anchor-id="five-questions-locate-the-failure">Five questions locate the failure</h2>
<p>Comparing the question, context, and answer gives five diagnostic questions, each pointing to a different failure mode and a different fix.</p>
<div id="fig-rag-diagnostic" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A diagnostic map. A pipeline strip: Question, then retrieval arrow, then Retrieved context, then generation arrow, then a red box quote 30 days unquote marked wrong. Below, two grouped columns. RETRIEVAL: Retrieval recall, needed passage missing, fix chunking, embeddings, reranker; Retrieval precision, right evidence buried in noise, fix ranking, top-k, filters. GENERATION: Grounding, ignored or overreached the context, fix generation prompt and grounding; Answer relevance, answered an adjacent question, fix conditioning and response style. A full-width amber card, Abstention, answered when it should have escalated, fix escalation and refusal rules. Footer: same surface failure, different root causes, different fixes.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-rag-diagnostic-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/llm-evaluation-honestly/06-rag-evaluation/rag_diagnostic_map.png" class="img-fluid figure-img" alt="A diagnostic map. A pipeline strip: Question, then retrieval arrow, then Retrieved context, then generation arrow, then a red box quote 30 days unquote marked wrong. Below, two grouped columns. RETRIEVAL: Retrieval recall, needed passage missing, fix chunking, embeddings, reranker; Retrieval precision, right evidence buried in noise, fix ranking, top-k, filters. GENERATION: Grounding, ignored or overreached the context, fix generation prompt and grounding; Answer relevance, answered an adjacent question, fix conditioning and response style. A full-width amber card, Abstention, answered when it should have escalated, fix escalation and refusal rules. Footer: same surface failure, different root causes, different fixes.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-rag-diagnostic-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: One bad answer, five different bugs. The same wrong answer (“30 days” to a trial-account question) can come from five places along the RAG pipeline, each with a different fix. Retrieval side: recall (needed passage missing, fix chunking, embeddings, reranker) and precision (right evidence buried in noise, fix ranking, top-k, filters). Generation side: grounding (ignored or overreached the context, fix the generation prompt) and answer relevance (answered an adjacent question, fix conditioning). Abstention, the most-skipped check, spans the whole pipeline: the system answered when it should have escalated.
</figcaption>
</figure>
</div>
<table class="caption-top table">
<colgroup>
<col style="width: 25%">
<col style="width: 25%">
<col style="width: 25%">
<col style="width: 25%">
</colgroup>
<thead>
<tr class="header">
<th>Area</th>
<th>Question</th>
<th>Diagnoses</th>
<th>Fix targets</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Retrieval recall</td>
<td>Did we retrieve the needed evidence?</td>
<td>Missing evidence</td>
<td>Chunking, embeddings, query rewriting, reranker, metadata filters</td>
</tr>
<tr class="even">
<td>Retrieval precision</td>
<td>Did we retrieve mostly relevant context?</td>
<td>Noisy context</td>
<td>Ranking, top-k, filtering, duplicate or noisy chunks</td>
</tr>
<tr class="odd">
<td>Grounding</td>
<td>Is the answer supported by the retrieved context?</td>
<td>Hallucination, overreach, or ignored evidence</td>
<td>Generation prompt, grounding behavior, judge calibration</td>
</tr>
<tr class="even">
<td>Answer relevance</td>
<td>Did the answer address the user’s question?</td>
<td>Generic or adjacent answer</td>
<td>Prompting, question conditioning, response style</td>
</tr>
<tr class="odd">
<td>Abstention</td>
<td>Should the system have declined or escalated?</td>
<td>Confident answer to an unanswerable question</td>
<td>Out-of-scope examples, escalation rules, refusal behavior</td>
</tr>
</tbody>
</table>
<p>Retrieval comes first because the generator can’t use evidence it never sees. For the retention-policy agent, <em>“How long do trial-account records stay available?”</em> should retrieve the trial-account passage. If it doesn’t appear in the top-k, that’s a recall failure; the fix is <a href="../../../../posts/series/research-foundations-of-modern-llms/03-retrieval/index.html">better chunking, embeddings, query rewriting, or reranking</a>, not a better generation prompt. If the passage is present but a standard-account passage dominates and the answer says “30 days,” the failure is partly precision and partly grounding.</p>
<p>For most documentation Q&amp;A systems, start with retrieval recall, grounding, answer relevance, and abstention. Add retrieval precision when noisy context becomes a visible problem. Keep the five separate; the metric that drops is the one that points to the next debugging step.</p>
</section>
<section id="dont-skip-abstention" class="level2">
<h2 class="anchored" data-anchor-id="dont-skip-abstention">Don’t skip abstention</h2>
<p>Abstention is the most commonly missing piece of a RAG eval, and the most damaging to skip. Most eval sets contain only questions the system should answer, which quietly rewards over-answering: the system is never tested on cases where the right move is to decline, ask for clarification, or escalate.</p>
<p>For the retention-policy agent: <em>“How long are records retained for enterprise account ABC-2026-447 while it is on legal hold?”</em> Account-specific legal-hold questions require escalation, so the correct response is to escalate, not to answer.</p>
<p>Include negative cases in the eval set deliberately: ambiguous questions, out-of-scope questions, escalation-required questions, and questions where the retrieved context is insufficient. Without them, the eval set teaches the system that confident answers are always rewarded.</p>
</section>
<section id="the-retrieval-environment-drifts-too" class="level2">
<h2 class="anchored" data-anchor-id="the-retrieval-environment-drifts-too">The retrieval environment drifts too</h2>
<p>RAG evaluation has one extra source of drift: the knowledge base changes. Documents get added, edited, deprecated. Chunking changes. Embedding models change. Rerankers change. The same question can retrieve different chunks next month even with no agent change.</p>
<p>Every RAG result should be tied to the retrieval environment that produced it: the metadata captured in the trace. Without that record, a regression is hard to interpret: did the agent get worse, or did the index change underneath it?</p>
</section>
<section id="what-comes-next" class="level2">
<h2 class="anchored" data-anchor-id="what-comes-next">What comes next</h2>
<p>A composite RAG score is fine as a summary, but it can’t drive debugging. A report of <em>RAG quality is 84%</em> only becomes actionable once you know which part is 84% and what you would fix if it dropped.</p>
<p><a href="../../../../posts/series/llm-evaluation-honestly/07-eval-drift/index.html">The next article</a> closes the series on the eval system itself: judges go stale, traffic shifts, documents change, and old eval cases stop representing the current product. The agent isn’t the only thing that drifts.</p>


</section>

 ]]></description>
  <category>LLM evaluation, honestly</category>
  <guid>https://yeesengchan.com/posts/series/llm-evaluation-honestly/06-rag-evaluation/</guid>
  <pubDate>Mon, 01 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Eval scores are samples, not truth</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/llm-evaluation-honestly/05-scores-are-samples/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>LLM evaluation, honestly</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-stop-vibe-checking/">Stop vibe-checking your agent</a></li>
<li><a href="../02-eval-dataset/">Read traces before you write the labeling guide</a></li>
<li><a href="../03-judge-vs-code/">Don’t ask an LLM judge what code can check</a></li>
<li><a href="../04-judge-is-classifier/">Your LLM judge is a classifier</a></li>
<li><a href="../05-scores-are-samples/">Eval scores are samples, not truth</a></li>
<li><a href="../06-rag-evaluation/">Your RAG score hides the diagnosis</a></li>
<li><a href="../07-eval-drift/">Your eval system also drifts</a></li>
</ol>
</div>
<p>An eval score is a measurement from a sample, and samples are noisy. The dashboard reported 73% on Monday. By Friday it reported 75%. The team called it progress. But the eval set had 120 examples. The difference may just be noise.</p>
<p>Three habits cover most of the eval-noise problem:</p>
<ol type="1">
<li>Compare versions on the same examples.</li>
<li>Report uncertainty on the difference, not just the score.</li>
<li>Check whether the result is large enough to act on.</li>
</ol>
<section id="compare-versions-on-the-same-examples" class="level2">
<h2 class="anchored" data-anchor-id="compare-versions-on-the-same-examples">1. Compare versions on the same examples</h2>
<p>When comparing two versions of a system, run them on the same eval set. If the samples differ, the result mixes two things: whether v8 is better, and whether v8 happened to get easier examples.</p>
<p>Suppose both versions are run on 300 traces:</p>
<ul>
<li>v7 passes 213 traces: 71%</li>
<li>v8 passes 228 traces: 76%</li>
</ul>
<p>The headline improvement is 5 percentage points. Before treating that as real progress, we need to ask how much uncertainty surrounds that estimate.</p>
</section>
<section id="report-the-ci-of-the-difference" class="level2">
<h2 class="anchored" data-anchor-id="report-the-ci-of-the-difference">2. Report the CI of the difference</h2>
<p>Because both versions ran on the same 300 traces, each trace falls into one of four outcomes:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Outcome</th>
<th style="text-align: right;">Count</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Both pass</td>
<td style="text-align: right;">198</td>
</tr>
<tr class="even">
<td>Both fail</td>
<td style="text-align: right;">57</td>
</tr>
<tr class="odd">
<td>v8 fixes a v7 failure</td>
<td style="text-align: right;">30</td>
</tr>
<tr class="even">
<td>v8 breaks a v7 pass</td>
<td style="text-align: right;">15</td>
</tr>
</tbody>
</table>
<p>The signal is in the disagreement rows. v8 fixes 30 cases and breaks 15, so the net gain is:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cfrac%7B30%20-%2015%7D%7B300%7D%20=%200.05%20=%205%5Ctext%7B%20percentage%20points%7D%0A"></p>
<div id="fig-paired-outcome" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A split-flow. A box of 300 traces on the same eval set branches two ways. The upper, muted branch goes to Agreements, 255, made of 198 both pass and 57 both fail, which flows to a dashed box labelled cancel out, no signal, d equals 0. The lower, highlighted branch goes to Disagreements, 45, made of 30 fixed at plus 1 in green and 15 broke at minus 1 in red, which flows to a box reading net plus 15 traces equals plus 5 points. Caption: pairing on the same examples is why 255 traces drop out; the whole 5-point gain lives in 45.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-paired-outcome-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/llm-evaluation-honestly/05-scores-are-samples/paired_outcome_split.png" class="img-fluid figure-img" alt="A split-flow. A box of 300 traces on the same eval set branches two ways. The upper, muted branch goes to Agreements, 255, made of 198 both pass and 57 both fail, which flows to a dashed box labelled cancel out, no signal, d equals 0. The lower, highlighted branch goes to Disagreements, 45, made of 30 fixed at plus 1 in green and 15 broke at minus 1 in red, which flows to a box reading net plus 15 traces equals plus 5 points. Caption: pairing on the same examples is why 255 traces drop out; the whole 5-point gain lives in 45.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-paired-outcome-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: The signal is in the disagreements. Running v7 and v8 on the same 300 traces, 255 are agreements (198 both pass, 57 both fail); these cancel and carry no signal (d = 0). Only the 45 disagreements move the number: 30 cases v8 fixed (d = +1) and 15 it broke (d = −1), a net of +15 traces, which is the observed +5 points.
</figcaption>
</figure>
</div>
<p>But this 5-point gain is still an estimate from one eval sample. If we drew another 300 traces, the measured improvement might be 3.8 points, 6.1 points, or 4.4 points. The confidence interval tells us how noisy the estimate is.</p>
<section id="step-1-define-the-trace-level-change" class="level3">
<h3 class="anchored" data-anchor-id="step-1-define-the-trace-level-change">Step 1: Define the trace-level change</h3>
<p>For each trace, define a before-and-after value <img src="https://latex.codecogs.com/png.latex?d">:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0Ad%20=%0A%5Cbegin%7Bcases%7D%0A+1%20&amp;%20%5Ctext%7Bif%20v8%20fixes%20a%20v7%20failure%7D%20%5C%5C%0A-1%20&amp;%20%5Ctext%7Bif%20v8%20breaks%20a%20v7%20pass%7D%20%5C%5C%0A0%20&amp;%20%5Ctext%7Bif%20both%20versions%20agree%7D%0A%5Cend%7Bcases%7D%0A"></p>
<p>In this example:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0AP(d=+1)=%5Cfrac%7B30%7D%7B300%7D=0.10,%5Cquad%0AP(d=-1)=%5Cfrac%7B15%7D%7B300%7D=0.05,%5Cquad%0AP(d=0)=%5Cfrac%7B255%7D%7B300%7D=0.85%0A"></p>
<p>The expected value of <img src="https://latex.codecogs.com/png.latex?d">, meaning the average trace-level change, is:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cbegin%7Baligned%7D%0AE%5Bd%5D%0A&amp;=%20P(d=1)(1)%20+%20P(d=-1)(-1)%20+%20P(d=0)(0)%20%5C%5C%0A&amp;=%20(0.10)(1)%20+%20(0.05)(-1)%20+%20(0.85)(0)%20%5C%5C%0A&amp;=%200.05%0A%5Cend%7Baligned%7D%0A"></p>
<p>That matches the observed 5-point improvement.</p>
</section>
<section id="step-2-compute-the-standard-deviation" class="level3">
<h3 class="anchored" data-anchor-id="step-2-compute-the-standard-deviation">Step 2: Compute the standard deviation</h3>
<p>Standard deviation measures how spread out the trace-level changes are around their average.</p>
<p>To get it, first compute the variance:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cmathrm%7BVar%7D(d)%20=%20E%5Bd%5E2%5D%20-%20E%5Bd%5D%5E2%0A"></p>
<p>Compute <img src="https://latex.codecogs.com/png.latex?E%5Bd%5E2%5D">:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cbegin%7Baligned%7D%0AE%5Bd%5E2%5D%0A&amp;=%20%5Csum_x%20P(d=x)%20%5Ccdot%20x%5E2%20%5C%5C%0A&amp;=%20P(d=+1)(+1)%5E2%20+%20P(d=-1)(-1)%5E2%20+%20P(d=0)(0)%5E2%20%5C%5C%0A&amp;=%20(0.10)(1)%5E2%20+%20(0.05)(-1)%5E2%20+%20(0.85)(0)%5E2%20%5C%5C%0A&amp;=%200.15%0A%5Cend%7Baligned%7D%0A"></p>
<p>Then:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cbegin%7Baligned%7D%0A%5Cmathrm%7BVar%7D(d)%0A&amp;=%20E%5Bd%5E2%5D%20-%20E%5Bd%5D%5E2%20%5C%5C%0A&amp;=%200.15%20-%20(0.05)%5E2%20%5C%5C%0A&amp;=%200.1475%0A%5Cend%7Baligned%7D%0A"></p>
<p>Standard deviation is the square root of variance:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cmathrm%7BSD%7D%20=%20%5Csqrt%7B0.1475%7D%20%5Capprox%200.384%0A"></p>
<p>So the individual trace-level changes have standard deviation about <img src="https://latex.codecogs.com/png.latex?0.384">.</p>
</section>
<section id="step-3-convert-standard-deviation-into-standard-error" class="level3">
<h3 class="anchored" data-anchor-id="step-3-convert-standard-deviation-into-standard-error">Step 3: Convert standard deviation into standard error</h3>
<p>The standard deviation tells us how much individual trace outcomes vary; the standard error tells us how much the average improvement would vary across repeated eval samples.</p>
<p>The standard error is:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cmathrm%7BSE%7D%20=%20%5Cfrac%7B%5Cmathrm%7BSD%7D%7D%7B%5Csqrt%7BN%7D%7D%0A"></p>
<p>Here:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cbegin%7Baligned%7D%0A%5Cmathrm%7BSE%7D%0A&amp;=%20%5Cfrac%7B0.384%7D%7B%5Csqrt%7B300%7D%7D%20%5C%5C%0A&amp;%5Capprox%200.022%0A%5Cend%7Baligned%7D%0A"></p>
<p>So the standard error is 2.2 percentage points.</p>
</section>
<section id="step-4-build-the-95-confidence-interval" class="level3">
<h3 class="anchored" data-anchor-id="step-4-build-the-95-confidence-interval">Step 4: Build the 95% confidence interval</h3>
<p>For a rough 95% confidence interval, use the estimate <img src="https://latex.codecogs.com/png.latex?%5Cpm%201.96"> standard errors:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cbegin%7Baligned%7D%0A%5Cmathrm%7BCI%7D%0A&amp;=%20%5Ctext%7Bimprovement%7D%20%5Cpm%201.96%20%5Ctimes%20%5Cmathrm%7BSE%7D%20%5C%5C%0A&amp;=%205.0%5C%25%20%5Cpm%201.96%20%5Ctimes%202.2%5C%25%20%5C%5C%0A&amp;=%205.0%5C%25%20%5Cpm%204.3%5C%25%20%5C%5C%0A&amp;%5Capprox%20%5B0.7%5C%25,%209.3%5C%25%5D%0A%5Cend%7Baligned%7D%0A"></p>
<p>The interval excludes zero, but only barely. v8 is probably better than v7, but the estimate is not precise. The best estimate is a 5-point gain; the plausible range is roughly a 1-point to 9-point gain.</p>
</section>
<section id="compare-the-difference-not-overlapping-cis" class="level3">
<h3 class="anchored" data-anchor-id="compare-the-difference-not-overlapping-cis">Compare the difference, not overlapping CIs</h3>
<p>Do not decide whether v8 is better by checking whether the two individual confidence intervals overlap. For example:</p>
<ul>
<li>v7 pass rate: 71%, 95% CI [65.9%, 76.1%]</li>
<li>v8 pass rate: 76%, 95% CI [71.2%, 80.8%]</li>
</ul>
<p>These intervals overlap, but that is not the right test.</p>
<p>The right test is the confidence interval of the paired difference:</p>
<ul>
<li>v8 - v7 = +5 percentage points</li>
<li>95% CI of the difference = [0.7%, 9.3%]</li>
</ul>
<p>This interval excludes zero, so the paired comparison says v8 is likely better.</p>
<div id="fig-overlap-vs-difference" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Two stacked panels. Top panel, red-bordered, the wrong test: a percentage axis with two interval bars, v7 at 71 percent spanning about 66 to 76, and v8 at 76 percent spanning about 71 to 81, with the overlapping region shaded and labelled overlap; verdict, reads as inconclusive. Bottom panel, green-bordered, the right test: an axis of difference in points with a dashed red line at zero, a single interval for v8 minus v7 centred at plus 5 points spanning roughly plus 0.6 to plus 9.4, entirely right of zero, labelled excludes 0; verdict, reads as v8 likely better. Caption: same comparison, two methods, trust the CI of the paired difference, not whether the individual CIs overlap.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-overlap-vs-difference-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/llm-evaluation-honestly/05-scores-are-samples/overlap_vs_difference_ci.png" class="img-fluid figure-img" alt="Two stacked panels. Top panel, red-bordered, the wrong test: a percentage axis with two interval bars, v7 at 71 percent spanning about 66 to 76, and v8 at 76 percent spanning about 71 to 81, with the overlapping region shaded and labelled overlap; verdict, reads as inconclusive. Bottom panel, green-bordered, the right test: an axis of difference in points with a dashed red line at zero, a single interval for v8 minus v7 centred at plus 5 points spanning roughly plus 0.6 to plus 9.4, entirely right of zero, labelled excludes 0; verdict, reads as v8 likely better. Caption: same comparison, two methods, trust the CI of the paired difference, not whether the individual CIs overlap.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-overlap-vs-difference-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: Overlap is the wrong test. Top (the wrong test): v7’s pass-rate CI [65.9%, 76.1%] and v8’s [71.2%, 80.8%] visibly overlap, which naively reads as inconclusive. Bottom (the right test): the paired-difference CI, v8 minus v7 = +5 points with 95% CI [0.7%, 9.3%], sits entirely to the right of zero, so the paired comparison says v8 is likely better. Same data, opposite conclusions depending on the method.
</figcaption>
</figure>
</div>
<p>The paired comparison uses the trace-by-trace difference. Both versions were tested on the same examples, so the comparison removes a lot of noise from easy versus hard traces.</p>
<p>So when comparing two versions, report the CI of the paired difference. Do not rely on whether the individual pass-rate CIs overlap.</p>
</section>
</section>
<section id="know-what-this-eval-set-can-reliably-detect" class="level2">
<h2 class="anchored" data-anchor-id="know-what-this-eval-set-can-reliably-detect">3. Know what this eval set can reliably detect</h2>
<p>The confidence interval <img src="https://latex.codecogs.com/png.latex?%5Cmathrm%7BCI%7D"> tells us how uncertain this observed 5-point improvement is. The same standard error <img src="https://latex.codecogs.com/png.latex?%5Cmathrm%7BSE%7D"> also gives a rough planning number: how large an improvement this eval set can reliably detect.</p>
<p>A common rule of thumb is:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Ctext%7Bdetectable%20improvement%7D%20%5Capprox%202.8%20%5Ctimes%20%5Cmathrm%7BSE%7D%0A"></p>
<p>The factor <img src="https://latex.codecogs.com/png.latex?2.8"> comes from two requirements:</p>
<ul>
<li>First, the observed improvement has to be large enough that the 95% confidence interval excludes zero. With a normal approximation, that means the improvement is about <img src="https://latex.codecogs.com/png.latex?1.96%20%5Ctimes%20%5Cmathrm%7BSE%7D"> away from zero.</li>
<li>Second, we do not want an eval set that detects the improvement only when we get lucky with the sample. We want enough <strong>power</strong>. Power means: if the improvement is real, how often would this eval setup detect it?</li>
</ul>
<p>An eval with <strong>80% power</strong> means that if the true improvement is exactly the size we care about, then repeated evals of the same size would detect it about 80% of the time.</p>
<p>To get 80% power, the true effect needs some extra margin beyond the 95% significance cutoff. Under the normal approximation, that extra margin is about <img src="https://latex.codecogs.com/png.latex?0.84%20%5Ctimes%20%5Cmathrm%7BSE%7D">.</p>
<p>So the rough detectable improvement is:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cbegin%7Baligned%7D%0A%5Ctext%7Bdetectable%20improvement%7D%0A&amp;%5Capprox%20(1.96%20+%200.84)%20%5Ctimes%20%5Cmathrm%7BSE%7D%20%5C%5C%0A&amp;%5Capprox%202.8%20%5Ctimes%20%5Cmathrm%7BSE%7D%0A%5Cend%7Baligned%7D%0A"></p>
<p>The improvement should be large enough to be detected reliably across repeated eval samples, not just to look significant in one lucky eval run.</p>
<p>In the example above:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cbegin%7Baligned%7D%0A%5Ctext%7Bdetectable%20improvement%7D%0A&amp;%5Capprox%202.8%20%5Ctimes%202.2%5C%25%20%5C%5C%0A&amp;%5Capprox%206.2%5C%25%0A%5Cend%7Baligned%7D%0A"></p>
<p>So this 300-trace eval can reliably detect improvements around 6 percentage points under this disagreement pattern. The observed improvement is 5 points. v8 is probably better, but the evidence is borderline.</p>
<p>For paired pass/fail evals with a disagreement pattern similar to this example, the rough detectable improvement is:</p>
<ul>
<li>100 traces: about 10 percentage points</li>
<li>300 traces: about 6 percentage points</li>
<li>1000 traces: about 3 percentage points</li>
</ul>
<div id="fig-detectable-vs-n" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A line chart. The x axis is eval-set size in traces from 0 to 1200; the y axis is detectable improvement in points from 0 to 16. A curve falls steeply then flattens, passing through marked points at 100 traces about 10 points, 300 traces about 6 points, and 1000 traces about 3 points. The region under the curve is shaded and labelled NOISE FLOOR, too small to reliably detect. A hollow marker at 300 traces and 5 points sits just below the curve, labelled observed +5 at 300, borderline. A box notes detectable is about 2.8 times SE, 1.96 significance plus 0.84 power. Caption: illustrative for this disagreement pattern, a +2-point gain on a small set is too small to trust.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-detectable-vs-n-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/llm-evaluation-honestly/05-scores-are-samples/detectable_vs_n.png" class="img-fluid figure-img" alt="A line chart. The x axis is eval-set size in traces from 0 to 1200; the y axis is detectable improvement in points from 0 to 16. A curve falls steeply then flattens, passing through marked points at 100 traces about 10 points, 300 traces about 6 points, and 1000 traces about 3 points. The region under the curve is shaded and labelled NOISE FLOOR, too small to reliably detect. A hollow marker at 300 traces and 5 points sits just below the curve, labelled observed +5 at 300, borderline. A box notes detectable is about 2.8 times SE, 1.96 significance plus 0.84 power. Caption: illustrative for this disagreement pattern, a +2-point gain on a small set is too small to trust.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-detectable-vs-n-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;3: Small eval sets can’t see small gains. The smallest improvement an eval set can reliably detect is roughly 2.8 × SE (1.96 for significance plus 0.84 for power). Since SE = SD/√N, the detectable threshold is proportional to 1/√N: about 10 points at 100 traces, 6 at 300, 3 at 1000, and you need 4× the traces to halve it. Anything below the curve is the noise floor; the observed +5-point gain at 300 traces sits just inside it, which is why the evidence is borderline.
</figcaption>
</figure>
</div>
<p>The exact numbers change with the disagreement rate, but the lesson is stable: small eval sets cannot reliably detect tiny improvements.</p>
<p>If v8 improves by 2 points on a small eval set, the honest conclusion is usually not <em>“v8 is better”</em> but <em>“directionally positive, but too small to trust on this eval set.”</em></p>
</section>
<section id="the-same-paired-logic-works-for-scored-ratings" class="level2">
<h2 class="anchored" data-anchor-id="the-same-paired-logic-works-for-scored-ratings">The same paired logic works for scored ratings</h2>
<p>Not every eval is pass/fail. Sometimes humans score each output on a small scale, such as 1, 2, or 3.</p>
<p>The same paired logic still applies. For each example, compute the score difference:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0Ad_i%20=%20%5Ctext%7Bscore%7D_%7Bv8,i%7D%20-%20%5Ctext%7Bscore%7D_%7Bv7,i%7D%0A"></p>
<p>Now the possible values are wider:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0Ad_i%20%5Cin%20%5C%7B-2,%20-1,%200,%20+1,%20+2%5C%7D%0A"></p>
<p>For example:</p>
<ul>
<li><img src="https://latex.codecogs.com/png.latex?1%20%5Crightarrow%203"> gives <img src="https://latex.codecogs.com/png.latex?d%20=%20+2"></li>
<li><img src="https://latex.codecogs.com/png.latex?2%20%5Crightarrow%203"> gives <img src="https://latex.codecogs.com/png.latex?d%20=%20+1"></li>
<li><img src="https://latex.codecogs.com/png.latex?2%20%5Crightarrow%202"> gives <img src="https://latex.codecogs.com/png.latex?d%20=%200"></li>
<li><img src="https://latex.codecogs.com/png.latex?3%20%5Crightarrow%202"> gives <img src="https://latex.codecogs.com/png.latex?d%20=%20-1"></li>
</ul>
<p>Then compute the average difference and its confidence interval the same way:</p>
<p><img src="https://latex.codecogs.com/png.latex?%0A%5Cmathrm%7BSE%7D%20=%20%5Cfrac%7B%5Cmathrm%7BSD%7D(d)%7D%7B%5Csqrt%7BN%7D%7D%0A"></p>
<p><img src="https://latex.codecogs.com/png.latex?%0A95%5C%25%5C,%5Cmathrm%7BCI%7D%20=%20%5Cbar%20d%20%5Cpm%201.96%20%5Ctimes%20%5Cmathrm%7BSE%7D%0A"></p>
<p>So if v7 averages 2.1 and v8 averages 2.6 on the same 300 examples, the mean paired improvement is <img src="https://latex.codecogs.com/png.latex?+0.5"> points on the 1–3 scale. The confidence interval depends on how much the per-example differences vary.</p>
</section>
<section id="what-comes-next" class="level2">
<h2 class="anchored" data-anchor-id="what-comes-next">What comes next</h2>
<p>Three habits cover most of the eval-noise problem. Compare versions on the same eval set. Report the CI of the paired difference. Know your eval set’s noise floor before debating small movements.</p>
<p>The next article <a href="../../../../posts/series/llm-evaluation-honestly/06-rag-evaluation/index.html">Your RAG score hides the diagnosis</a> takes this to RAG evaluation: did retrieval find the right passage, did generation stay faithful to it, did the answer address what the user actually asked, did the system recognize when to decline. Collapsing those into a single <em>“RAG quality”</em> score loses these distinctions.</p>
<p>The next time someone celebrates a 2-point improvement, first ask: <em>what is the CI, and how big is your eval set?</em></p>


</section>

 ]]></description>
  <category>LLM evaluation, honestly</category>
  <guid>https://yeesengchan.com/posts/series/llm-evaluation-honestly/05-scores-are-samples/</guid>
  <pubDate>Fri, 29 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Your LLM judge is a classifier</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/llm-evaluation-honestly/04-judge-is-classifier/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>LLM evaluation, honestly</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-stop-vibe-checking/">Stop vibe-checking your agent</a></li>
<li><a href="../02-eval-dataset/">Read traces before you write the labeling guide</a></li>
<li><a href="../03-judge-vs-code/">Don’t ask an LLM judge what code can check</a></li>
<li><a href="../04-judge-is-classifier/">Your LLM judge is a classifier</a></li>
<li><a href="../05-scores-are-samples/">Eval scores are samples, not truth</a></li>
<li><a href="../06-rag-evaluation/">Your RAG score hides the diagnosis</a></li>
<li><a href="../07-eval-drift/">Your eval system also drifts</a></li>
</ol>
</div>
<p>An LLM judge that predicts PASS or FAIL is a classifier. Like any classifier, it has to be tested against human labels before its scores mean anything.</p>
<p>This is the step teams often skip. They build a judge, spot-check a few outputs, decide the verdicts look reasonable, and start reporting a pass rate. The dashboard may say the judge passed 87% of traces, but that number is only useful if the judge itself is reliable.</p>
<p>This article is about how to validate an LLM judge.</p>
<p>The running example is a documentation Q&amp;A agent. The agent retrieves passages from internal docs and answers questions about a data-retention policy. For example, trial-account data is kept for 90 days, standard-account data for 30 days, and enterprise accounts under legal hold must be escalated.</p>
<p>The judge’s job is to check whether the answer is faithful to the retrieved source. In plain English: did the answer say what the document actually supports? Or did it use the wrong policy, drop an important caveat, or make a claim the document does not justify?</p>
<p>That is not something a simple regex can reliably check. It requires reading the question, the retrieved source, and the answer together. So it is a reasonable job for an LLM judge. The question is whether that judge is any good.</p>
<section id="validate-against-human-labels" class="level2">
<h2 class="anchored" data-anchor-id="validate-against-human-labels">Validate against human labels</h2>
<p>Validating a judge means comparing its verdicts against human labels on examples the judge has not seen.</p>
<p>The process is simple:</p>
<ol type="1">
<li>Start with traces that humans have labeled PASS or FAIL.</li>
<li>Keep a held-out set that is not used in the judge prompt.</li>
<li>Run the judge on that held-out set.</li>
<li>Compare the judge’s verdicts with the human labels.</li>
<li>Report the confusion matrix, precision, recall, and F1 for the class that matters.</li>
</ol>
<p>The held-out set is the key. If an example appears in the judge’s few-shot prompt or was used to develop the judge, it cannot also be used to validate the judge. The judge has already seen it.</p>
</section>
<section id="accuracy-hides-what-matters" class="level2">
<h2 class="anchored" data-anchor-id="accuracy-hides-what-matters">Accuracy hides what matters</h2>
<p>Suppose the faithfulness judge is tested on 100 human-labeled traces: 80 PASS and 20 FAIL.</p>
<p>It <strong>agrees with human labels</strong> on 83 of 100 traces, so the headline looks good: <strong>83% accuracy</strong>.</p>
<div id="fig-judge-confusion" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A two-by-two confusion matrix. Rows are what the judge says, pass or fail; columns are the human label, pass or fail. Judge pass and human pass is 77, correctly passed. Judge pass and human fail is 14, highlighted red, labelled missed failures. Judge fail and human pass is 3, false alarm. Judge fail and human fail is 6, caught 6 of 20. A metrics panel reads: 83 percent accuracy, looks fine; 30 percent recall on FAIL, misses 14 of 20; precision 67 percent, F1 41 percent on the FAIL class. Caption: a high headline accuracy can hide a judge that misses most of what it was built to catch.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-judge-confusion-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/llm-evaluation-honestly/04-judge-is-classifier/judge_confusion_matrix.png" class="img-fluid figure-img" alt="A two-by-two confusion matrix. Rows are what the judge says, pass or fail; columns are the human label, pass or fail. Judge pass and human pass is 77, correctly passed. Judge pass and human fail is 14, highlighted red, labelled missed failures. Judge fail and human pass is 3, false alarm. Judge fail and human fail is 6, caught 6 of 20. A metrics panel reads: 83 percent accuracy, looks fine; 30 percent recall on FAIL, misses 14 of 20; precision 67 percent, F1 41 percent on the FAIL class. Caption: a high headline accuracy can hide a judge that misses most of what it was built to catch.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-judge-confusion-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Accuracy hides what matters. On 100 human-labeled traces (80 pass, 20 fail), the faithfulness judge agrees with humans 83% of the time, which looks fine. The confusion matrix shows why it isn’t: the judge correctly passes 77 faithful answers and catches 6 failures, but waves through 14 of the 20 unfaithful ones. Recall on the FAIL class is only 30% (precision 67%, F1 41%).
</figcaption>
</figure>
</div>
<p>But the confusion matrix tells a different story:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th></th>
<th style="text-align: right;">Human PASS</th>
<th style="text-align: right;">Human FAIL</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Judge PASS</td>
<td style="text-align: right;">77</td>
<td style="text-align: right;">14</td>
</tr>
<tr class="even">
<td>Judge FAIL</td>
<td style="text-align: right;">3</td>
<td style="text-align: right;">6</td>
</tr>
</tbody>
</table>
<p>The judge correctly passes 77 of 80 faithful answers, but catches only 6 of 20 failures.</p>
<pre class="text"><code>Accuracy              = (77 + 6) / 100        = 83%
Precision (P) on FAIL = 6 / (6 + 3)           = 67%
Recall (R) on FAIL    = 6 / (6 + 14)          = 30%
F1 on FAIL            = (2 * P * R) / (P + R) = 41%</code></pre>
<p>So the judge is not good enough for faithfulness evaluation. It misses most of the unfaithful answers.</p>
</section>
<section id="hold-out-a-real-test-set" class="level2">
<h2 class="anchored" data-anchor-id="hold-out-a-real-test-set">Hold out a real test set</h2>
<p>Do not validate a judge on examples that appear in its prompt or were used to tune it. Split your labeled traces into: <strong>few-shot examples</strong> that go inside the judge prompt, <strong>dev set</strong> for iterating on the judge, and <strong>held-out test set</strong> for final validation only.</p>
<p>On the dev set, the most useful step is reading disagreements. For example:</p>
<ul>
<li><strong>User question:</strong> <em>“How long do trial-account records stay available?”</em></li>
<li><strong>Retrieved context:</strong> standard accounts keep records for 30 days; trial accounts keep them for 90 days.</li>
<li><strong>Agent answer:</strong> <em>“Records are retained for 30 days after closure.”</em></li>
<li><strong>Judge verdict:</strong> PASS (found a matching sentence in the context).</li>
<li><strong>Human label:</strong> FAIL (the answer used the wrong account type).</li>
</ul>
<p>That disagreement tells you what example to add: faithfulness requires support for the specific entity and condition in the user’s question, not just any matching sentence in the retrieved documents.</p>
<p>Once the judge performs well on dev, run it once on the held-out test set and report the result.</p>
</section>
<section id="when-calibration-is-poor" class="level2">
<h2 class="anchored" data-anchor-id="when-calibration-is-poor">When calibration is poor</h2>
<p>Reading the disagreements usually reveals one of a few patterns:</p>
<ul>
<li><strong>Missing failure subtype.</strong> The judge catches outright hallucinations but misses subtle contradictions, dropped caveats, or unsupported generalizations.</li>
<li><strong>Over-flagging.</strong> The judge calls everything FAIL when the agent paraphrases instead of quoting verbatim.</li>
<li><strong>Unclear judging standard.</strong> The judge says some answers are unfaithful that the humans accept as fine.</li>
<li><strong>Right for the wrong reason.</strong> The verdict is correct but the critique points at the wrong thing.</li>
<li><strong>Too-small validation set.</strong> Wide error bars on metrics; unreliable conclusions (<a href="../../../../posts/series/llm-evaluation-honestly/05-scores-are-samples/index.html">Eval scores are samples, not truth</a> covers when this matters).</li>
</ul>
<p>The fix progression is <strong>examples → criterion → model</strong>.</p>
<p>Examples almost always move first: if the judge is wrong on five cases of subtle contradiction, consider adding five contradiction <strong>examples</strong> to the few-shot block. If multiple disagreements trace to ambiguity in what counts as a failure rather than missing examples, the adjudicator sharpens the <strong>criterion</strong>. <strong>Model</strong> swaps come last. Most calibration problems live in the prompt, not the model, and switching models is expensive in cost, latency, and stability.</p>
</section>
<section id="revalidate-when-things-change" class="level2">
<h2 class="anchored" data-anchor-id="revalidate-when-things-change">Revalidate when things change</h2>
<p>The judge agrees with humans on the held-out set today. That doesn’t mean it will agree in six months. Foundation models update, the agent’s failure distribution shifts, and new failure types appear. Calibration is a snapshot that is accurate when measured and grows stale afterward.</p>
<p><a href="../../../../posts/series/llm-evaluation-honestly/07-eval-drift/index.html">Your eval system also drifts</a> covers what to do about it: re-validate against fresh labels on a schedule and monitor agreement rates over time. Large drift in those agreement rates is usually a signal that the failure distribution itself has changed.</p>
<p>Every deployed judge should carry a short record of what it was validated on: the domain, the trace types, the test-set size, the date, and what it’s allowed to be used for. Without that record, judges get re-used on data the original validation doesn’t cover, and the team trusts a number that no longer applies.</p>
</section>
<section id="the-judge-is-part-of-the-system" class="level2">
<h2 class="anchored" data-anchor-id="the-judge-is-part-of-the-system">The judge is part of the system</h2>
<p>An LLM judge is a model component that needs the same discipline as any other classifier: held-out labels, a confusion matrix, precision and recall on the class that matters, and periodic revalidation.</p>
<p>Once the judge is validated, there is still one more question: how much should we trust movement in the score? A judge may report that v2 improved by two points, but that movement may be real or may be noise. <a href="../../../../posts/series/llm-evaluation-honestly/05-scores-are-samples/index.html">The next article</a> covers that next layer: uncertainty, sample size, and when an eval result is large enough to act on.</p>


</section>

 ]]></description>
  <category>LLM evaluation, honestly</category>
  <guid>https://yeesengchan.com/posts/series/llm-evaluation-honestly/04-judge-is-classifier/</guid>
  <pubDate>Tue, 26 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Don’t ask an LLM judge what code can check</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/llm-evaluation-honestly/03-judge-vs-code/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>LLM evaluation, honestly</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-stop-vibe-checking/">Stop vibe-checking your agent</a></li>
<li><a href="../02-eval-dataset/">Read traces before you write the labeling guide</a></li>
<li><a href="../03-judge-vs-code/">Don’t ask an LLM judge what code can check</a></li>
<li><a href="../04-judge-is-classifier/">Your LLM judge is a classifier</a></li>
<li><a href="../05-scores-are-samples/">Eval scores are samples, not truth</a></li>
<li><a href="../06-rag-evaluation/">Your RAG score hides the diagnosis</a></li>
<li><a href="../07-eval-drift/">Your eval system also drifts</a></li>
</ol>
</div>
<p>Most bad LLM judges are doing a simpler evaluator’s job. They are asked whether JSON is valid. Whether a required field is present. Whether a tool was called. Whether a prohibited phrase appears in the final answer. Whether a handoff record contains an order ID.</p>
<p>That is checking, not judgment.</p>
<p>When a team asks an LLM judge to do what code could have checked, it gets the worst of both worlds: slower, noisier, more expensive, and harder to debug.</p>
<p>LLM judges are useful when the evaluator needs to understand meaning in context, and wasted when the failure has a structural shape. If the failure can be detected with code, use code.</p>
<section id="start-with-the-failure-mode" class="level2">
<h2 class="anchored" data-anchor-id="start-with-the-failure-mode">Start with the failure mode</h2>
<p>The wrong question is <em>“Should we use an LLM judge here?”</em> The right question is <em>“What exactly are we trying to detect?”</em></p>
<ul>
<li><strong>Code check</strong> when the failure has a structural shape: a missing required field, an invalid category, a prohibited phrase in the response.</li>
<li><strong>LLM judge</strong> when the failure requires reading language in context: tone mismatch, redundant clarifying questions, subtle policy violations.</li>
</ul>
</section>
<section id="use-code-for-structural-failures" class="level2">
<h2 class="anchored" data-anchor-id="use-code-for-structural-failures">Use code for structural failures</h2>
<p>A code-based evaluator is a function. Take a trace, return a score with a reason.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb1-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> check_handoff_schema(trace) <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span> Score:</span>
<span id="cb1-2">    required <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"case_id"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"user_email"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"issue_category"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"summary"</span>}</span>
<span id="cb1-3">    handoff <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> trace.outputs[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"handoff_record"</span>]</span>
<span id="cb1-4">    missing <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> required <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> handoff.keys()</span>
<span id="cb1-5">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> missing:</span>
<span id="cb1-6">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> Score(passed<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">False</span>, reason<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">f"Missing fields: </span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span>missing<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span><span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span>)</span>
<span id="cb1-7">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> Score(passed<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">True</span>, reason<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Handoff record well-formed"</span>)</span></code></pre></div></div>
<p>Five lines. Runs in milliseconds. Deterministic: same input, same output, every time. Debuggable: when it fails, the reason tells you exactly what to look at. Free: no API calls, no token costs, no rate limits. Runs on every trace, on every CI build, on every production trace if you want.</p>
<p>Code and judges aren’t always alternatives. Sometimes they’re layers. A regex check for refund-promise language catches explicit cases (<em>“Your refund has been approved”</em>); it misses subtler implications (<em>“We’ll make sure you get your money back”</em>). The fix isn’t to abandon the regex for a judge. Run the cheap regex on every trace; run a more expensive judge only when the trace involves a refund request and the regex didn’t fire. Run cheap checks broadly, and run judges where they add value.</p>
</section>
<section id="use-llm-judges-for-semantic-failures" class="level2">
<h2 class="anchored" data-anchor-id="use-llm-judges-for-semantic-failures">Use LLM judges for semantic failures</h2>
<p>Some failure modes don’t have a structural shape. For instance, the user is frustrated but the agent responds in an upbeat and causal manner.</p>
<p>A judge worth trusting is binary (pass or fail, not 1-5) and grounded in labeled examples from the dataset <a href="../../../../posts/series/llm-evaluation-honestly/02-eval-dataset/index.html">the previous article</a> produced. <a href="../../../../posts/series/llm-evaluation-honestly/04-judge-is-classifier/index.html">The next article</a> covers the prompt design and the calibration step: comparing the judge’s verdicts to human labels before treating its numbers as measurement. Until then, the discipline is simple: don’t deploy a judge whose agreement with humans you haven’t measured.</p>
</section>
<section id="keep-each-evaluator-narrow" class="level2">
<h2 class="anchored" data-anchor-id="keep-each-evaluator-narrow">Keep each evaluator narrow</h2>
<p>The mistake is building one big judge called <code>quality_judge_v3</code> and asking it to score everything: tone, helpfulness, schema validity, routing. That judge produces a number, not a diagnosis.</p>
<p>It’s the same reason you don’t write a single function called <code>is_correct()</code> in production code. Composite signals are harder to act on, harder to debug, and harder to improve.</p>
<p>Suppose the intake agent’s “overall quality” judge drops from 87% to 81% over a release. What changed? Tone got worse? Helpfulness got worse? Grounding got worse? Some combination? The composite verdict can’t tell you. You read failures by hand, you guess, and you propose a fix without knowing which dimension you’re targeting.</p>
<p>Specialized evaluators fix this. The tone judge tracks tone. The redundant-question judge tracks redundancy. When tone scores drop and redundancy scores hold steady, you know what changed. When you fix the tone issue, the tone judge tells you whether the fix worked.</p>
<div id="fig-composite-vs-narrow" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Two contrasting cards. Left card, red border: composite judge quality_judge_v3, showing one big number 87 percent dropping to 81 percent, minus 6 points over one release, then the question what changed in large italic red, with a note that you have to read failures by hand and guess. Right card, green border: narrow evaluators, listing five dimensions with their before-and-after scores. Tone drops 87 to 60, marked with a red down arrow and minus 27. Redundancy 89 to 88, Schema 96 to 96, Routing 86 to 87, Escalation 80 to 80, all held steady. Verdict in green: tone regressed, fix tone. Caption: composite scores tell you something moved, narrow evaluators tell you what.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-composite-vs-narrow-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/llm-evaluation-honestly/03-judge-vs-code/composite_vs_narrow.png" class="img-fluid figure-img" alt="Two contrasting cards. Left card, red border: composite judge quality_judge_v3, showing one big number 87 percent dropping to 81 percent, minus 6 points over one release, then the question what changed in large italic red, with a note that you have to read failures by hand and guess. Right card, green border: narrow evaluators, listing five dimensions with their before-and-after scores. Tone drops 87 to 60, marked with a red down arrow and minus 27. Redundancy 89 to 88, Schema 96 to 96, Routing 86 to 87, Escalation 80 to 80, all held steady. Verdict in green: tone regressed, fix tone. Caption: composite scores tell you something moved, narrow evaluators tell you what.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-composite-vs-narrow-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: One number can’t tell you what broke. The composite quality_judge_v3 score drops from 87% to 81% over a release, which leaves you asking “what changed?” and reading failures by hand to guess. The narrow stack of evaluators, run on the same release, shows Tone dropping from 87 to 60 while Redundancy, Schema, Routing, and Escalation all held steady. Same release, same data; the diagnosis only exists in the narrow view.
</figcaption>
</figure>
</div>
<p>The cost is more API calls per trace, one per judge instead of one total. In practice this is cheap, because most evaluation runs sample only a fraction of production traffic. The per-trace cost matters less than signal quality.</p>
<p>A reasonable evaluator stack for the intake agent might look like:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 33%">
<col style="width: 33%">
<col style="width: 33%">
</colgroup>
<thead>
<tr class="header">
<th>Evaluator</th>
<th>Failure mode</th>
<th>Mechanism</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>no_refund_promise</code></td>
<td>Forbidden refund-approval language</td>
<td>Code (regex)</td>
</tr>
<tr class="even">
<td><code>handoff_schema</code></td>
<td>Required handoff fields populated</td>
<td>Code (schema check)</td>
</tr>
<tr class="odd">
<td><code>order_id_confirmed</code></td>
<td>Order ID confirmed before handoff</td>
<td>Code (state predicate)</td>
</tr>
<tr class="even">
<td><code>escalation_honored</code></td>
<td>Escalation when user requests human</td>
<td>Code (state predicate)</td>
</tr>
<tr class="odd">
<td><code>tone_appropriate</code></td>
<td>Tone matches user’s emotional register</td>
<td>LLM judge</td>
</tr>
<tr class="even">
<td><code>no_redundant_question</code></td>
<td>Clarifying question wasn’t already answered</td>
<td>LLM judge</td>
</tr>
<tr class="odd">
<td><code>classification_correct</code></td>
<td>Issue routed to the right category</td>
<td>Reference-based check</td>
</tr>
</tbody>
</table>
<p>Seven evaluators, each answering one question. When the dashboard shows a regression, you can identify which question’s answer changed.</p>
<p>One related discipline: when a regression case enters the suite, tag it by the layer where the failure originated, not the layer where it became visible. A malformed handoff at turn 5 caused by a classifier misread at turn 3 should be tagged as a classifier failure.</p>
</section>
<section id="code-first-judges-when-you-need-them" class="level2">
<h2 class="anchored" data-anchor-id="code-first-judges-when-you-need-them">Code first, judges when you need them</h2>
<p><a href="../../../../posts/series/llm-evaluation-honestly/04-judge-is-classifier/index.html">Your LLM judge is a classifier</a> covers the next layer: making judges trustworthy when you do need them. A judge that produces verdicts in the right format doesn’t necessarily produce correct ones.</p>
<p>The next time someone proposes a judge, first ask: <em>could code answer this?</em></p>


</section>

 ]]></description>
  <category>LLM evaluation, honestly</category>
  <guid>https://yeesengchan.com/posts/series/llm-evaluation-honestly/03-judge-vs-code/</guid>
  <pubDate>Fri, 22 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Read traces before you write the labeling guide</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/llm-evaluation-honestly/02-eval-dataset/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>LLM evaluation, honestly</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-stop-vibe-checking/">Stop vibe-checking your agent</a></li>
<li><a href="../02-eval-dataset/">Read traces before you write the labeling guide</a></li>
<li><a href="../03-judge-vs-code/">Don’t ask an LLM judge what code can check</a></li>
<li><a href="../04-judge-is-classifier/">Your LLM judge is a classifier</a></li>
<li><a href="../05-scores-are-samples/">Eval scores are samples, not truth</a></li>
<li><a href="../06-rag-evaluation/">Your RAG score hides the diagnosis</a></li>
<li><a href="../07-eval-drift/">Your eval system also drifts</a></li>
</ol>
</div>
<p>The eval loop from <a href="../../../../posts/series/llm-evaluation-honestly/01-stop-vibe-checking/index.html">Stop vibe-checking your agent</a> is only as honest as the dataset underneath it. A dataset’s first job is to discover how the system fails.</p>
<p>The running example continues from <a href="../../../../posts/series/the-agent-harness/index.html">the harness series</a>: a scheduling assistant that books, reschedules, and cancels meetings. A user writes: <em>“Move my meeting with Alex to next Thursday afternoon if he has time.”</em> That one sentence assumes the assistant can guess which Alex, parse “next Thursday afternoon,” pick the right calendar, and not say “Done” before the write actually succeeds.</p>
<section id="start-with-traces-not-imagined-categories" class="level2">
<h2 class="anchored" data-anchor-id="start-with-traces-not-imagined-categories">Start with traces, not imagined categories</h2>
<p>A common mistake is to start with abstract qualities like accurate, helpful, concise, safe, professional. They describe how the team imagines quality before seeing how the system actually fails.</p>
<p>For a scheduling assistant, the real failures are more specific: choosing the wrong Alex, writing to the wrong calendar, confirming before the calendar API succeeds, losing information from earlier turns, or mishandling a forwarded email thread. These failures are concrete enough to test and fix, and they only emerge when humans read real traces.</p>
<p>So start with traces: label what happened, write short critiques, and let the failure modes emerge from the examples. When labeling reveals an obvious bug, fix it instead of building a judge to detect it.</p>
</section>
<section id="keep-labels-consistent" class="level2">
<h2 class="anchored" data-anchor-id="keep-labels-consistent">Keep labels consistent</h2>
<p>Reviewers will disagree on edge cases. Designate one <strong>adjudicator</strong> to own the labeling guide, make the call on contested cases, and update examples as the standard becomes clearer.</p>
<p>For domain-heavy products, the adjudicator should be whoever is closest to the product’s real standard of correctness, not whoever happens to be available. In a healthcare project I led, anchoring medical-billing-level labels to a domain expert’s judgment rather than engineers’ guesses gave the dataset real ground truth.</p>
<p>Keep the labels themselves simple: <strong>pass or fail</strong>. Likert scales feel more nuanced but are harder to act on and invite false precision. The nuance lives in the critique.</p>
</section>
<section id="design-for-coverage-and-difficulty" class="level2">
<h2 class="anchored" data-anchor-id="design-for-coverage-and-difficulty">Design for coverage and difficulty</h2>
<p>Once labels are consistent, the next question is whether the dataset is representative.</p>
<div id="fig-coverage-difficulty" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Two cards. Left, COVERAGE, what situations the set must contain: Main tasks, book reschedule cancel; Common ambiguity, which Alex vague time forwarded thread; High-risk failures, wrong calendar premature confirm permission error; key chip, set target counts per slice, decide before collecting. Right, DIFFICULTY, how hard each case is: Easy guards against regressions; Medium shows if it's improving; Hard the frontier; Safety must refuse or escalate; key chip, keep a visible mix, too easy saturates too hard hides progress. Footer: raw production clusters in easy plus main-task, design the rest in deliberately.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-coverage-difficulty-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/llm-evaluation-honestly/02-eval-dataset/coverage_difficulty.png" class="img-fluid figure-img" alt="Two cards. Left, COVERAGE, what situations the set must contain: Main tasks, book reschedule cancel; Common ambiguity, which Alex vague time forwarded thread; High-risk failures, wrong calendar premature confirm permission error; key chip, set target counts per slice, decide before collecting. Right, DIFFICULTY, how hard each case is: Easy guards against regressions; Medium shows if it's improving; Hard the frontier; Safety must refuse or escalate; key chip, keep a visible mix, too easy saturates too hard hides progress. Footer: raw production clusters in easy plus main-task, design the rest in deliberately.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-coverage-difficulty-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Design the dataset on two axes. Coverage is what situations the set must contain: main tasks (book, reschedule, cancel), common ambiguity (which Alex, vague time, forwarded thread), and high-risk failures (wrong calendar, premature confirm, permission error); set target counts per slice and decide before collecting. Difficulty is how hard each case is: easy guards against regressions, medium shows whether it is improving, hard is the frontier, safety must refuse or escalate; keep a visible mix. Raw production clusters in the easy, main-task corner, so the rest has to be designed in deliberately.
</figcaption>
</figure>
</div>
<p>A raw production sample usually overrepresents easy traffic. For a scheduling assistant, that means lots of simple booking requests and too few high-risk cases: ambiguous attendees, wrong-calendar writes, permission errors, forwarded email threads, time-zone mistakes.</p>
<p>Design the dataset intentionally:</p>
<ul>
<li><strong>Main tasks:</strong> booking, rescheduling, cancellation.</li>
<li><strong>Common ambiguity:</strong> unclear attendee, unclear event, vague time phrase, forwarded email thread.</li>
<li><strong>High-risk failures:</strong> wrong calendar, premature confirmation, missing conflict check, permission error.</li>
</ul>
<p>Set target counts for the slices that matter most (30 ambiguous-attendee cases, 25 wrong-calendar cases). The numbers are illustrative; the point is deciding the targets before collecting.</p>
<p>For rare but important slices, do a cheap pre-filtering pass before full labeling. If wrong-calendar writes or permission errors are rare in raw traffic, find the candidate traces first, then label those carefully. In a clinical-note project I led, sections like allergies and labs appeared sparsely across clinician-patient transcripts; a quick yes/no pass identified which transcripts contained them.</p>
<p>Balance difficulty too:</p>
<ul>
<li><strong>Easy cases:</strong> the system should already pass these. They protect against regressions.</li>
<li><strong>Medium cases:</strong> the system is mixed. These show whether the product is improving.</li>
<li><strong>Hard cases:</strong> the system often fails. These show the frontier.</li>
<li><strong>Safety cases:</strong> the system must refuse, escalate, or avoid forbidden behavior.</li>
</ul>
<p>A dataset that is too easy saturates, and one that is too hard fails to show progress, so aim for a visible mix that tells the team what actually improved.</p>
<p><a href="../../../../posts/series/llm-evaluation-honestly/05-scores-are-samples/index.html">Eval scores are samples, not truth</a> returns to sample size and uncertainty. If a slice matters, include it deliberately and track it separately.</p>
</section>
<section id="when-you-dont-have-production-data" class="level2">
<h2 class="anchored" data-anchor-id="when-you-dont-have-production-data">When you don’t have production data</h2>
<p>Before launch, the dataset has to come from somewhere other than production. Generate the inputs and let the real system produce the responses, then label the resulting traces like any other trace.</p>
<p>Two sources work well in combination.</p>
<p><strong>Persona-based generation.</strong> Define three to five user personas that match the product (for the scheduling assistant: a time-zone-confused product manager, an executive with overlapping calendars, an IC handling forwarded meeting threads). For each persona, generate eight to twelve plausible queries.</p>
<p><strong>Expert elicitation.</strong> Sit with someone who has done this product’s work before: a customer success lead, a power user from a prior product, a domain expert. Ask: <em>“What is the worst question someone could ask this?”</em> and <em>“Where do you expect this to fail?”</em> The answers encode failure intuition the team doesn’t yet have from data, including the high-risk cases the coverage section calls out.</p>
</section>
<section id="the-dataset-is-the-products-memory" class="level2">
<h2 class="anchored" data-anchor-id="the-dataset-is-the-products-memory">The dataset is the product’s memory</h2>
<p>An eval dataset is the product’s memory of what has gone wrong, what the team decided “good” means, and what must not break again.</p>
<p>That memory has to keep growing. The first version will be incomplete; failure modes will get sharper as production reveals new ones; the adjudicator will tighten the examples annotation guide as the team’s standard becomes clearer. The danger is pretending the dataset doesn’t need to evolve.</p>


</section>

 ]]></description>
  <category>LLM evaluation, honestly</category>
  <guid>https://yeesengchan.com/posts/series/llm-evaluation-honestly/02-eval-dataset/</guid>
  <pubDate>Tue, 19 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Stop vibe-checking your agent</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/llm-evaluation-honestly/01-stop-vibe-checking/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>LLM evaluation, honestly</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-stop-vibe-checking/">Stop vibe-checking your agent</a></li>
<li><a href="../02-eval-dataset/">Read traces before you write the labeling guide</a></li>
<li><a href="../03-judge-vs-code/">Don’t ask an LLM judge what code can check</a></li>
<li><a href="../04-judge-is-classifier/">Your LLM judge is a classifier</a></li>
<li><a href="../05-scores-are-samples/">Eval scores are samples, not truth</a></li>
<li><a href="../06-rag-evaluation/">Your RAG score hides the diagnosis</a></li>
<li><a href="../07-eval-drift/">Your eval system also drifts</a></li>
</ol>
</div>
<p>Some teams evaluate by feel: read a few runs after a prompt change, form an impression, ship. It works until something changes: a prompt update seems better but nobody can prove it, or a refactor might break something nobody can name.</p>
<p>The first useful eval system replaces feel with a loop: <a href="../../../../posts/series/the-agent-harness/05-traces/index.html">traces</a>, labels, failure modes, checks and judges, regression cases, and a regular review that feeds new failures back in.</p>
<p>This series is about evaluating LLM agents: turning the observable behavior the <a href="../../../../posts/series/the-agent-harness/index.html">harness</a> captures into evidence.</p>
<p>The running example: a customer support intake agent that classifies issues, asks clarifying questions, captures relevant facts, and produces a handoff record for a human. No refunds, no side-effecting tools.</p>
<section id="what-eval-is-for" class="level2">
<h2 class="anchored" data-anchor-id="what-eval-is-for">What eval is for</h2>
<p>A score is useful only if it changes what the team does next. Eval supports different decisions at different stages: during development, whether a new prompt is better; before merge, whether old failures came back; in production, whether new failure patterns are emerging.</p>
<p>The operational test for any metric: <em>When this number moves, what decision changes?</em> If the answer is merge, hold, investigate, roll back, or add a regression case, the metric is useful; if the answer is “nothing,” it does not belong on the dashboard.</p>
</section>
<section id="the-minimum-viable-eval-loop" class="level2">
<h2 class="anchored" data-anchor-id="the-minimum-viable-eval-loop">The minimum viable eval loop</h2>
<p>A useful eval system starts with traces and ends with decisions. The first version needs six pieces: traces, labels, failure modes, checks and judges, regression cases, and a regular review.</p>
<div id="fig-eval-loop" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A closed six-stage cycle. Stage 1 Traces, more than the final answer. Stage 2 Labels, pass or fail plus a short critique. Stage 3 Failure modes, patterns across many labels. Stage 4 Checks and judges, code checks plus LLM judges. Stage 5 Regression cases, real failures frozen as tests. Stage 6 Review and decide, merge, hold, or roll back. An arrow flows clockwise through all six, and a highlighted return edge from Review and decide back to Traces is labelled new production failures re-enter, closing the loop. Caption: start small, use real failures, make every new failure feed back into the loop.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-eval-loop-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/llm-evaluation-honestly/01-stop-vibe-checking/eval_mvp_loop.png" class="img-fluid figure-img" alt="A closed six-stage cycle. Stage 1 Traces, more than the final answer. Stage 2 Labels, pass or fail plus a short critique. Stage 3 Failure modes, patterns across many labels. Stage 4 Checks and judges, code checks plus LLM judges. Stage 5 Regression cases, real failures frozen as tests. Stage 6 Review and decide, merge, hold, or roll back. An arrow flows clockwise through all six, and a highlighted return edge from Review and decide back to Traces is labelled new production failures re-enter, closing the loop. Caption: start small, use real failures, make every new failure feed back into the loop.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-eval-loop-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: The minimum viable eval loop. Traces become labels, labels reveal failure modes, failure modes become checks and judges, checks and judges produce regression cases, and a regular review turns recent failures into decisions. The loop only works if it closes: new production failures re-enter as traces and regression cases rather than disappearing into Slack.
</figcaption>
</figure>
</div>
<section id="traces" class="level3">
<h3 class="anchored" data-anchor-id="traces">1. Traces</h3>
<p>A trace records one agent run: state, retrieved context, actions, tool calls, verification, and the final state it saved. It shows how the agent got there, not just where it ended up.</p>
<p>An agent’s final message often hides the real failure. The intake agent might say: <em>“Thanks, I captured your request and will route it to our support team for review.”</em> The trace may show that it classified the issue as “billing” instead of “refund,” failed to copy the order ID into the handoff, and marked the user’s frustration as “low” despite the message <em>“I’ve asked about this three times already.”</em></p>
<p>Reading only the final answer misses the first bad move. A malformed handoff at turn five may trace back to a misclassification at turn three.</p>
<div id="fig-first-bad-move" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Two panels. Top, what a final-answer check sees: the agent message, quote, Thanks, I captured your request and will route it to the support team, unquote, with a green PASS badge. Bottom, what the trace shows: four turn cards. Turns 1 to 2, issue captured, ok. Turn 3, classified billing, marked wrong with should be refund, tagged FIRST BAD MOVE. Turn 4, frustration set to low, marked wrong, user said asked three times. Turn 5, handoff written, marked wrong, order ID dropped. An arrow runs from Turn 5 back to Turn 3 labelled traces back to. Caption: eval that reads only the final answer misses the first bad move.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-first-bad-move-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/llm-evaluation-honestly/01-stop-vibe-checking/trace_first_bad_move.png" class="img-fluid figure-img" alt="Two panels. Top, what a final-answer check sees: the agent message, quote, Thanks, I captured your request and will route it to the support team, unquote, with a green PASS badge. Bottom, what the trace shows: four turn cards. Turns 1 to 2, issue captured, ok. Turn 3, classified billing, marked wrong with should be refund, tagged FIRST BAD MOVE. Turn 4, frustration set to low, marked wrong, user said asked three times. Turn 5, handoff written, marked wrong, order ID dropped. An arrow runs from Turn 5 back to Turn 3 labelled traces back to. Caption: eval that reads only the final answer misses the first bad move.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-first-bad-move-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: The final answer hides the first bad move. A final-answer check sees only the agent’s closing message, which reads fine and would pass. The trace shows the run was already broken: at turn 3 the issue was misclassified as billing instead of refund (the first bad move), which propagated to a mismarked frustration level at turn 4 and a handoff with the order ID dropped at turn 5. The turn-5 failure traces back to the turn-3 misclassification.
</figcaption>
</figure>
</div>
</section>
<section id="labels" class="level3">
<h3 class="anchored" data-anchor-id="labels">2. Labels</h3>
<p>A label is a human judgment attached to a trace: pass or fail, plus a short critique. For example: <em>“Failed: agent routed the case as billing instead of refund at turn three; downstream handoff omitted refund-request status.”</em> That critique tells the team what failed, where, and what to watch for; a score of 3 out of 5 does not.</p>
</section>
<section id="failure-modes" class="level3">
<h3 class="anchored" data-anchor-id="failure-modes">3. Failure modes</h3>
<p>After reading thirty or forty labeled traces, <strong>failure modes</strong> emerge: refund language the agent should not have used, redundant clarifying questions, dropped order IDs, missing handoff fields. Without them, every problem collapses into “the prompt is bad.”</p>
<p><a href="../../../../posts/series/llm-evaluation-honestly/02-eval-dataset/index.html">A later article</a> goes deeper on building this dataset: which traces to sample, how to label them, and how failure modes emerge from reading real runs.</p>
</section>
<section id="checks-and-judges" class="level3">
<h3 class="anchored" data-anchor-id="checks-and-judges">4. Checks and judges</h3>
<p>Each important failure mode should become either a check or a judge.</p>
<ul>
<li><strong>A check</strong> is a function that examines a trace and returns pass or fail without an LLM call. Use checks for structural failures: refund language appears, a required field is missing, the handoff schema is invalid, or the order ID was dropped.</li>
<li><strong>A judge</strong> is a separate LLM that examines a trace and returns a verdict on something a function cannot reliably decide. Use judges for semantic failures: the clarifying question was redundant, the tone was wrong, or the agent answered the wrong question.</li>
</ul>
<div id="fig-check-or-judge" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Two cards. Left, CHECK: a function, no LLM call; use for structural failures; examples refund language present, required field missing, order ID dropped; cheap and deterministic, run it on every change. Right, JUDGE: a separate LLM call; use for semantic failures; examples redundant clarifying question, wrong tone, answered the wrong question; needs calibration against human labels. Footer rule: structural goes to a check, semantic goes to a judge; if code can decide it, don't ask an LLM.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-check-or-judge-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/llm-evaluation-honestly/01-stop-vibe-checking/check_or_judge.png" class="img-fluid figure-img" alt="Two cards. Left, CHECK: a function, no LLM call; use for structural failures; examples refund language present, required field missing, order ID dropped; cheap and deterministic, run it on every change. Right, JUDGE: a separate LLM call; use for semantic failures; examples redundant clarifying question, wrong tone, answered the wrong question; needs calibration against human labels. Footer rule: structural goes to a check, semantic goes to a judge; if code can decide it, don't ask an LLM.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-check-or-judge-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;3: Check or judge? A check is a deterministic function with no LLM call, used for structural failures (refund language present, required field missing, order ID dropped); it is cheap and should run on every change. A judge is a separate LLM call, used for semantic failures (redundant clarifying question, wrong tone, answered the wrong question); it needs calibration against human labels before it can be trusted.
</figcaption>
</figure>
</div>
<p>For example, “the agent must never promise a refund” should be a code check:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb1-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> evaluate_no_refund_promise(trace) <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span> Score:</span>
<span id="cb1-2">    response <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> trace.outputs.get(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"agent_response"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">""</span>)</span>
<span id="cb1-3">    matches <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> find_matches(response, REFUND_PROMISE_PATTERNS)</span>
<span id="cb1-4"></span>
<span id="cb1-5">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> matches:</span>
<span id="cb1-6">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> Score(</span>
<span id="cb1-7">            passed<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">False</span>,</span>
<span id="cb1-8">            reason<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">f"Refund-promise language: </span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span>matches<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span><span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span>,</span>
<span id="cb1-9">        )</span>
<span id="cb1-10"></span>
<span id="cb1-11">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> Score(passed<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">True</span>, reason<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"No refund-promise language"</span>)</span></code></pre></div></div>
<p>Checks are cheap and deterministic. Run them on every important change.</p>
<p>Judges need <strong>calibration</strong>: run the judge on traces humans have already labeled, then look at where it agrees, where it misses failures humans caught, and where it flags cases humans considered acceptable. Calibration tells you whether the judge is reliable enough to support the decision you want it to support.</p>
<p><a href="../../../../posts/series/llm-evaluation-honestly/03-judge-vs-code/index.html">Don’t ask an LLM judge what code can check</a> focuses on when a failure should become a code check versus an LLM judge. <a href="../../../../posts/series/llm-evaluation-honestly/04-judge-is-classifier/index.html">Your LLM judge is a classifier</a> goes deeper on judge calibration.</p>
</section>
<section id="regression-cases" class="level3">
<h3 class="anchored" data-anchor-id="regression-cases">5. Regression cases</h3>
<p>A regression case is a real failure saved so future versions have to pass it.</p>
<p>If the intake agent once promised a refund, dropped an order ID, or asked for information the user already provided, that trace becomes a fixed case in the regression suite. Ten or fifteen real failures are enough to make the system harder to break in the same way twice.</p>
<p>Run the regression suite before important merges. If the new version fails cases the old version passed, the team should understand why before shipping.</p>
<p><a href="../../../../posts/series/llm-evaluation-honestly/05-scores-are-samples/index.html">Eval scores are samples, not truth</a> covers what changes once raw pass rates are no longer enough and you need to think about uncertainty, sample size, and meaningful differences between versions.</p>
</section>
<section id="review-and-decisions" class="level3">
<h3 class="anchored" data-anchor-id="review-and-decisions">6. Review and decisions</h3>
<p>The loop only matters if it changes what the team does. A failed check blocks a merge, a spike in redundant-question failures triggers investigation, a new production failure becomes a regression case, a judge disagreement becomes a calibration example.</p>
<p>Pull a small sample of recent failures regularly, read them end-to-end, and ask whether they reveal a pattern the current dataset misses. New failures feed back into the eval loop instead of disappearing into Slack.</p>
<p><a href="../../../../posts/series/llm-evaluation-honestly/07-eval-drift/index.html">Your eval system also drifts</a> returns to this production loop: how new failures from real usage become new eval cases, alerts, and hardening work.</p>
</section>
<section id="a-concrete-first-version" class="level3">
<h3 class="anchored" data-anchor-id="a-concrete-first-version">A concrete first version</h3>
<p>The first version for the intake agent might look like this:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 50%">
<col style="width: 50%">
</colgroup>
<thead>
<tr class="header">
<th>Piece</th>
<th>First version</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Traces</td>
<td>~50 intake traces, synthetic if pre-launch or sampled from production once traffic exists</td>
</tr>
<tr class="even">
<td>Labels</td>
<td>Pass/fail plus short critiques explaining the first bad move</td>
</tr>
<tr class="odd">
<td>Failure modes</td>
<td>Recurring patterns of mistakes (e.g.&nbsp;“promises refunds”, “drops order IDs”)</td>
</tr>
<tr class="even">
<td>Checks and judges</td>
<td>Code checks for the structural ones; one calibrated judge for redundant clarifying questions</td>
</tr>
<tr class="odd">
<td>Regression cases</td>
<td>10–15 real historical failures fixed as test cases</td>
</tr>
<tr class="even">
<td>Review</td>
<td>Regular review of recent failures and judge disagreements</td>
</tr>
</tbody>
</table>
<p>The numbers are illustrative; the shape is what matters: start small, use real failures, make every new failure feed back into the loop.</p>
</section>
</section>
<section id="the-smallest-useful-promise" class="level2">
<h2 class="anchored" data-anchor-id="the-smallest-useful-promise">The smallest useful promise</h2>
<p>Having a minimal eval loop in place is enough to change how the team builds. “Does v2 feel better?” becomes “Which failure modes improved, which regressed, and do we trust the measurement?” Eval stops being a side research exercise and becomes part of the engineering loop.</p>
<p>The harness made the agent’s behavior observable, the eval system turns that behavior into evidence, and the hardening loop turns evidence into a system harder to break.</p>
<p>The <a href="../../../../posts/series/llm-evaluation-honestly/02-eval-dataset/index.html">next article</a> goes one layer deeper: the eval dataset.</p>


</section>

 ]]></description>
  <category>LLM evaluation, honestly</category>
  <guid>https://yeesengchan.com/posts/series/llm-evaluation-honestly/01-stop-vibe-checking/</guid>
  <pubDate>Fri, 15 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Traces are how agents get better</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/the-agent-harness/05-traces/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>The agent harness</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-demos-break/">Why AI agent demos break in production</a></li>
<li><a href="../02-harness-is-the-product/">The harness is the product</a></li>
<li><a href="../03-state-not-transcript/">State, not transcript, is agent memory</a></li>
<li><a href="../04-prompts-gate/">Prompts guide. Gates enforce</a></li>
<li><a href="../05-traces/">Traces are how agents get better</a></li>
</ol>
</div>
<p>A user asked the docs Q&amp;A agent: “What’s the data retention policy for trial accounts?”</p>
<p>The agent answered: “Trial accounts: data is retained for 30 days after closure, after which it is deleted.” The answer was confident, polished, and wrong. The corpus says trial-account data is retained for 90 days. Thirty days is the standard-account window.</p>
<p>The team finds out a day later, when the user files a correction. The logs show this:</p>
<pre class="text"><code>2026-05-08 14:23:18  run_551  query received
2026-05-08 14:23:19  run_551  tool_call: retrieval, status: success
2026-05-08 14:23:21  run_551  response generated
2026-05-08 14:23:21  run_551  run completed</code></pre>
<p>That log records events. It does not show which passages were retrieved, what the model saw, why it chose 30 days, which passage supported the claim, or which validation checks ran. The team can guess but cannot debug.</p>
<p>Logs record what happened; traces show why.</p>
<p>The <a href="../../../../posts/series/the-agent-harness/04-prompts-gate/index.html">previous article</a> covered gates: runtime checks that stop wrong actions before they happen. This article covers traces: structured records that make failures explainable after they happen.</p>
<section id="what-a-useful-trace-records" class="level2">
<h2 class="anchored" data-anchor-id="what-a-useful-trace-records">What a useful trace records</h2>
<p>A trace records one structured step for each meaningful action: a model decision, tool call, verification, phase transition, or gate firing. It should not store a wall of chat text or a dump of model reasoning.</p>
<p>Each step should record:</p>
<ul>
<li><strong>Versions:</strong> prompt, model, tool, index, schema, and validator versions.</li>
<li><strong>State before:</strong> what the system knew before the step.</li>
<li><strong>Context pack:</strong> what the model actually saw.</li>
<li><strong>Action chosen:</strong> what the system decided to do, with a short reason.</li>
<li><strong>Verification:</strong> what was checked after the action.</li>
<li><strong>State after:</strong> what changed.</li>
</ul>
<p>Here is one step from the failed retention run, captured at synthesis:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb2-1">trace_step <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb2-2">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"step_kind"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"synthesize"</span>,</span>
<span id="cb2-3">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"versions"</span>: {</span>
<span id="cb2-4">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"prompt"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"syn_v6"</span>,</span>
<span id="cb2-5">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"index"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"ri_v3"</span>,</span>
<span id="cb2-6">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"validator"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"val_v4"</span>,</span>
<span id="cb2-7">    },</span>
<span id="cb2-8"></span>
<span id="cb2-9">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"state_before"</span>: {</span>
<span id="cb2-10">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"phase"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"synthesize"</span>,</span>
<span id="cb2-11">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"query"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"What is the data retention policy for trial accounts?"</span>,</span>
<span id="cb2-12">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"retrieved_passages"</span>: [</span>
<span id="cb2-13">            {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_1102"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"trial_accounts"</span>},</span>
<span id="cb2-14">            {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0871"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"general"</span>},</span>
<span id="cb2-15">            {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0883"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"general"</span>},</span>
<span id="cb2-16">            {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0902"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"general"</span>},</span>
<span id="cb2-17">            {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0915"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"general"</span>},</span>
<span id="cb2-18">        ],</span>
<span id="cb2-19">    },</span>
<span id="cb2-20"></span>
<span id="cb2-21">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"context_pack"</span>: {</span>
<span id="cb2-22">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"retrieved_refs"</span>: [<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_1102"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0871"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0883"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0902"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0915"</span>],</span>
<span id="cb2-23">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"passage_metadata_included"</span>: <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">False</span>,</span>
<span id="cb2-24">    },</span>
<span id="cb2-25"></span>
<span id="cb2-26">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"action_chosen"</span>: {</span>
<span id="cb2-27">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"draft_grounded_answer"</span>,</span>
<span id="cb2-28">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"reason"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"5 passages retrieved; drafting answer from passage content"</span>,</span>
<span id="cb2-29">    },</span>
<span id="cb2-30"></span>
<span id="cb2-31">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"verification"</span>: {</span>
<span id="cb2-32">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"all_claims_cited"</span>: <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">True</span>,</span>
<span id="cb2-33">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category_alignment_check"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"not_run"</span>,</span>
<span id="cb2-34">    },</span>
<span id="cb2-35"></span>
<span id="cb2-36">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"state_after"</span>: {</span>
<span id="cb2-37">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"phase"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"validate"</span>,</span>
<span id="cb2-38">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"claims"</span>: [</span>
<span id="cb2-39">            {</span>
<span id="cb2-40">                <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"text"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Trial accounts: data retained 30 days after closure"</span>,</span>
<span id="cb2-41">                <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"evidence"</span>: [<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0871"</span>],</span>
<span id="cb2-42">            },</span>
<span id="cb2-43">        ],</span>
<span id="cb2-44">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"drafted_answer"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Trial accounts: data is retained for 30 days after closure..."</span>,</span>
<span id="cb2-45">    },</span>
<span id="cb2-46">}</span></code></pre></div></div>
<p>That is enough to debug the run. <code>general</code> account passages mentioning “30 days” entered the answer path, and no <code>category_alignment_check</code> caught the category mismatch.</p>
<section id="rationale-not-chain-of-thought" class="level3">
<h3 class="anchored" data-anchor-id="rationale-not-chain-of-thought">Rationale, not chain-of-thought</h3>
<p>Store decision rationale, not full chain-of-thought. The rationale should be a short operational reason, captured in <code>action_chosen.reason</code>.</p>
<p>Examples:</p>
<ul>
<li><strong>Clarification:</strong> target was ambiguous, so the agent asked a clarifying question.</li>
<li><strong>Synthesis:</strong> five passages were retrieved, so the agent drafted from passage content.</li>
</ul>
<p>The team needs the reason for the action, not the model’s internal rumination.</p>
</section>
</section>
<section id="walking-the-retention-policy-trace" class="level2">
<h2 class="anchored" data-anchor-id="walking-the-retention-policy-trace">Walking the retention-policy trace</h2>
<p>The user asked about trial-account retention. The agent answered 30 days. The corpus says 90 days. The team opens <code>run_551</code>.</p>
<p>Step 1, <code>receive_query</code>, captured the user’s message and wrote it into state.</p>
<p>Step 2, <code>retrieve</code>, pulled five passages:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb3-1">retrieved_passages <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> [</span>
<span id="cb3-2">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_1102"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"trial_accounts"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"score"</span>: <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.93</span>,</span>
<span id="cb3-3">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"snippet"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Trial accounts: data retained 90 days after trial expiry."</span>},</span>
<span id="cb3-4">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0871"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"general"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"score"</span>: <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.91</span>,</span>
<span id="cb3-5">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"snippet"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Standard accounts: data retained 30 days after closure..."</span>},</span>
<span id="cb3-6">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0883"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"general"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"score"</span>: <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.89</span>,</span>
<span id="cb3-7">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"snippet"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Default retention windows for closed accounts are 30 days..."</span>},</span>
<span id="cb3-8">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0902"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"general"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"score"</span>: <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.87</span>,</span>
<span id="cb3-9">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"snippet"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"After account closure, customer data is purged within 30 days..."</span>},</span>
<span id="cb3-10">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"psg_0915"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"category"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"general"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"score"</span>: <span class="fl" style="color: #AD0000;
background-color: null;
font-style: inherit;">0.85</span>,</span>
<span id="cb3-11">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"snippet"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Standard data retention: 30 days post-closure..."</span>},</span>
<span id="cb3-12">]</span></code></pre></div></div>
<p>Retrieval found the right passage and ranked it first.</p>
<p>Step 3, <code>synthesize</code>, shows the first bad move. Synthesis received passage text without category tags. It saw four general passages saying 30 days and one trial-account passage saying 90 days. Without category metadata, the model had no signal that the 90-day passage was the relevant one.</p>
<p>Step 4, <code>ground</code>, mapped the drafted claim to <code>psg_0871</code>. Grounding succeeded narrowly because the claim had a citation. The missing check was category alignment. The trace shows <code>category_alignment_check: not_run</code>.</p>
<p>The failure is now specific: retrieval found the answer, context assembly dropped the metadata, synthesis chose the wrong passage, and validation missed the category mismatch.</p>
<p>Reading only the output, a team might edit the synthesis prompt; reading the trace, it fixes the right layer: include category metadata in the context pack, weight category-specific passages for category-specific queries, and validate category alignment before returning the answer.</p>
</section>
<section id="from-first-bad-move-to-durable-fix" class="level2">
<h2 class="anchored" data-anchor-id="from-first-bad-move-to-durable-fix">From first bad move to durable fix</h2>
<p>A failure should leave behind a trace, a fix, and a regression case.</p>
<ul>
<li><strong>First bad move:</strong> locate where the run first went wrong. The trace should show whether the failure began in retrieval, context assembly, action choice, tool arguments, state updates, validation, or workflow phase.</li>
<li><strong>Durable fix:</strong> change the harness layer that failed. For the retention issue, that means category-aware retrieval, category metadata in the context pack, and category-alignment validation.</li>
<li><strong>Regression case:</strong> encode the failure shape so it cannot return silently. A “retention policy for [category]” query should retrieve the category-specific passage at rank 1, and the validator should reject answers citing the wrong category.</li>
</ul>
<div id="fig-hardening-loop" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A closed feedback loop with four stages. A failure produces a trace; the trace is read to identify the first bad move; the team ships a durable fix to the harness plus a regression test case; the fix and case feed back into the system, where they prevent that failure on future runs. An annotation marks the harness as the part that changes each cycle, so the improvements accumulate over time.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-hardening-loop-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/the-agent-harness/05-traces/hardening_loop.png" class="img-fluid figure-img" alt="A closed feedback loop with four stages. A failure produces a trace; the trace is read to identify the first bad move; the team ships a durable fix to the harness plus a regression test case; the fix and case feed back into the system, where they prevent that failure on future runs. An annotation marks the harness as the part that changes each cycle, so the improvements accumulate over time.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-hardening-loop-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: The hardening loop. A failure produces a trace; the trace identifies the first bad move; the team ships a durable fix and a regression case. The cycle feeds back into the system. The harness is what changes, and what changes is what compounds.
</figcaption>
</figure>
</div>
<p>Running this loop on every incident is reliability engineering; stopping at “patched the prompt, moved on” only manages symptoms. A system improves when the team stops wasting failure.</p>
</section>
<section id="minimum-viable-trace" class="level2">
<h2 class="anchored" data-anchor-id="minimum-viable-trace">Minimum viable trace</h2>
<p>A v1 agent does not need a perfect observability platform. It needs a minimum viable trace.</p>
<p>For every meaningful step, record:</p>
<ul>
<li><strong>Identity:</strong> <code>run_id</code>, <code>step_id</code>, and step kind.</li>
<li><strong>Versions:</strong> prompt, model, tool, index, validator, and schema versions.</li>
<li><strong>State:</strong> <code>state_before</code> and <code>state_after</code>.</li>
<li><strong>Context:</strong> what entered the model context.</li>
<li><strong>Action:</strong> <code>action_chosen</code> and a short reason.</li>
<li><strong>Tool data:</strong> tool inputs and outputs, when a tool runs.</li>
<li><strong>Verification:</strong> checks that ran and their results.</li>
<li><strong>Metrics:</strong> latency, cost, token counts, and other operational fields.</li>
</ul>
<p>Different agents need different trace fields. For instance, these fields are useful for the <strong>Docs Q&amp;A agent:</strong></p>
<pre class="text"><code>retrieval query, passage IDs, claims, claim-to-evidence mapping, validator result</code></pre>
</section>
<section id="closing-the-loop" class="level2">
<h2 class="anchored" data-anchor-id="closing-the-loop">Closing the loop</h2>
<p>Five articles in this series, one argument:</p>
<ul>
<li><a href="../../../../posts/series/the-agent-harness/01-demos-break/index.html">Why AI agent demos break in production</a>: recurring production failures live in the system around the model.</li>
<li><a href="../../../../posts/series/the-agent-harness/02-harness-is-the-product/index.html">The harness is the product</a>: an agent is the model plus the harness around it.</li>
<li><a href="../../../../posts/series/the-agent-harness/03-state-not-transcript/index.html">State, not transcript, is agent memory</a>: state is the memory layer the runtime can read and update.</li>
<li><a href="../../../../posts/series/the-agent-harness/04-prompts-gate/index.html">Prompts guide. Gates enforce</a>: gates turn state into runtime enforcement.</li>
<li>Traces are how agents get better: traces turn failures into durable fixes.</li>
</ul>
<p>The harness has four jobs:</p>
<ul>
<li><strong>Shape the input:</strong> decide what the model sees.</li>
<li><strong>Bound the action:</strong> decide what the system allows.</li>
<li><strong>Verify the outcome:</strong> check what happened.</li>
<li><strong>Preserve the lesson:</strong> keep enough evidence to improve the system.</li>
</ul>
<p>State tells the system what it believes. Gates decide what it may do. Tools let it act. Verification checks the result. Traces preserve the path.</p>


</section>

 ]]></description>
  <category>The agent harness</category>
  <guid>https://yeesengchan.com/posts/series/the-agent-harness/05-traces/</guid>
  <pubDate>Sun, 10 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Prompts guide. Gates enforce</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/the-agent-harness/04-prompts-gate/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>The agent harness</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-demos-break/">Why AI agent demos break in production</a></li>
<li><a href="../02-harness-is-the-product/">The harness is the product</a></li>
<li><a href="../03-state-not-transcript/">State, not transcript, is agent memory</a></li>
<li><a href="../04-prompts-gate/">Prompts guide. Gates enforce</a></li>
<li><a href="../05-traces/">Traces are how agents get better</a></li>
</ol>
</div>
<p>A prompt can guide behavior but cannot enforce it.</p>
<p>The scheduling assistant’s prompt says, “Always confirm the target meeting before making any changes.” A user asks it to move “my Tuesday review with Priya” to Thursday afternoon. The model calls <code>reschedule_event(description="Tuesday review with Priya", new_time="Thursday afternoon")</code>; the API matches a different recurring meeting with Priya; the agent replies, “I’ve moved it, and I’ll keep an eye on it going forward.”</p>
<p>The failure was not the wording of the prompt; the runtime allowed an unsafe action. A gate should have blocked <code>reschedule_event</code> until the target event was uniquely confirmed, non-recurring, and represented by a specific event ID.</p>
<p>Prompts shape behavior; gates enforce it.</p>
<p>The <a href="../../../../posts/series/the-agent-harness/03-state-not-transcript/index.html">previous article</a> established that state holds the system’s beliefs. This article is about the runtime checks that decide whether the system can act on those beliefs.</p>
<section id="why-prompt-only-control-breaks" class="level2">
<h2 class="anchored" data-anchor-id="why-prompt-only-control-breaks">Why prompt-only control breaks</h2>
<p>Prompt-only control works best for soft behavior: tone, formatting, response length, and style. It is weak for the following.</p>
<ul>
<li><strong>Fuzzy boundaries:</strong> “Do not commit to remediation timelines” sounds clear, but real language is messy.</li>
<li><strong>Runtime facts:</strong> a prompt cannot verify that an event ID is unique, that a user has permission, that a write already happened after a timeout, or that every answer claim is supported by evidence.</li>
<li><strong>Competing rules:</strong> production prompts accumulate rules for tone, safety, tools, escalation, formatting, and edge cases. Rules important enough to block behavior should not live only in text.</li>
</ul>
<p>The system asked the model to enforce constraints the runtime should own.</p>
</section>
<section id="the-model-proposes.-the-harness-checks." class="level2">
<h2 class="anchored" data-anchor-id="the-model-proposes.-the-harness-checks.">The model proposes. The harness checks.</h2>
<p>A gate checks a proposed action or output at runtime. It can allow the proposal, reject it, or route it to a safer path. Two kinds of gates cover most production needs.</p>
<section id="hard-gates-check-crisp-facts" class="level3">
<h3 class="anchored" data-anchor-id="hard-gates-check-crisp-facts">Hard gates: check crisp facts</h3>
<p>Hard gates check conditions that are computable from state. They do not need model judgment.</p>
<ul>
<li>Is this tool allowed in the current workflow phase?</li>
<li>Is the target uniquely identified and confirmed?</li>
<li>Has approval been granted, and has it expired?</li>
<li>Is the user authorized, and is the write budget still available?</li>
</ul>
<p>If a check fails, the action is rejected before it reaches the tool.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb1-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> gate_reschedule(state, action):</span>
<span id="cb1-2">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> state[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"phase"</span>] <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">!=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"execute"</span>:</span>
<span id="cb1-3">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> Reject(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"write_not_allowed_in_phase"</span>)</span>
<span id="cb1-4">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> state[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"proposed_change"</span>] <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">is</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">None</span>:</span>
<span id="cb1-5">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> Reject(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"no_proposed_change_materialized"</span>)</span>
<span id="cb1-6">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> state[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"candidates"</span>][<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>][<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"match_confidence"</span>] <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">!=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confirmed"</span>:</span>
<span id="cb1-7">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> Reject(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"target_not_confirmed"</span>)</span>
<span id="cb1-8">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> state[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"candidates"</span>][<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>][<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"is_recurring"</span>]:</span>
<span id="cb1-9">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> Reject(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"recurring_series_requires_human_only"</span>)</span>
<span id="cb1-10">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> action.get(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"idempotency_key"</span>) <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">is</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">None</span>:</span>
<span id="cb1-11">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> Reject(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"missing_idempotency_key"</span>)</span>
<span id="cb1-12">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> Allow()</span></code></pre></div></div>
<p>Each line is a mechanical check. If a condition is crisp enough to enforce with code, enforce it with code.</p>
</section>
<section id="semantic-gates-check-meaning" class="level3">
<h3 class="anchored" data-anchor-id="semantic-gates-check-meaning">Semantic gates: check meaning</h3>
<p>Semantic gates check meaning rather than schema or permissions. They answer questions like:</p>
<ul>
<li>Does this answer overstate the evidence?</li>
<li>Does this message imply an unauthorized commitment?</li>
<li>Does this response give advice outside the agent’s role?</li>
</ul>
<p>These checks usually require model judgment. They are slower and more expensive than hard gates, so use them when the risk is semantic and code cannot capture it.</p>
<p>Use hard gates for crisp conditions and semantic gates for judgment calls.</p>
</section>
</section>
<section id="the-tool-executes" class="level2">
<h2 class="anchored" data-anchor-id="the-tool-executes">The tool executes</h2>
<p>Gates check proposals before they become actions. Tool contracts narrow what the model can safely propose in the first place.</p>
<section id="tools-are-contracts-not-functions" class="level3">
<h3 class="anchored" data-anchor-id="tools-are-contracts-not-functions">Tools are contracts, not functions</h3>
<p>In a notebook, a tool can be a function with a docstring. In production, a tool is a contract between the model, the runtime, and the outside world. It should define required inputs, allowed use, side effects, retry behavior, and verification.</p>
<p>A weak tool:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb2-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> update_calendar(field: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span>, value: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span>) <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span> <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">dict</span>:</span>
<span id="cb2-2">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"""Update a field on a calendar event."""</span></span></code></pre></div></div>
<p>A stronger tool:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb3-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> reschedule_event(</span>
<span id="cb3-2">    event_id: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span>,          <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># confirmed unique target only</span></span>
<span id="cb3-3">    new_start_iso: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span>,</span>
<span id="cb3-4">    new_end_iso: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span>,</span>
<span id="cb3-5">    idempotency_key: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span>,</span>
<span id="cb3-6">) <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span> RescheduleResult:</span>
<span id="cb3-7">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"""</span></span>
<span id="cb3-8"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">    Reschedule one confirmed, non-recurring event.</span></span>
<span id="cb3-9"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">    Requires a specific event_id, not a free-text description.</span></span>
<span id="cb3-10"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">    """</span></span></code></pre></div></div>
<p>The second tool removes unsafe paths. It requires a specific <code>event_id</code>, which forces the workflow to identify the target before the call. It requires an idempotency key. It does not expose a broad <code>field</code> parameter that could update anything.</p>
<p>Tool contracts should also distinguish reads from writes. Reads can usually be retried after a timeout; writes need more care. A write should carry an idempotency key, and an ambiguous timeout should route to verification before retry.</p>
<p>Broad tools push hidden responsibility onto the model; narrow tools move more of it into the harness.</p>
</section>
</section>
<section id="the-harness-verifies-and-routes" class="level2">
<h2 class="anchored" data-anchor-id="the-harness-verifies-and-routes">The harness verifies and routes</h2>
<p>Gates and tools do not finish the loop. The system also needs controlled fallbacks and safe human approval.</p>
<section id="gates-need-safe-fallbacks" class="level3">
<h3 class="anchored" data-anchor-id="gates-need-safe-fallbacks">Gates need safe fallbacks</h3>
<p>A blocked action should route to a safe next step.</p>
<p>If the scheduling target is ambiguous, route to a clarifying question. If the corpus does not support an answer, route to an honest “I don’t have enough evidence” response. If the intake user asks for a credit, route the request into the human handoff.</p>
<p>A gate should return a reason and a next action. A blocked action should become a controlled detour, not a dead end.</p>
</section>
<section id="approval-packets-approve-the-exact-action" class="level3">
<h3 class="anchored" data-anchor-id="approval-packets-approve-the-exact-action">Approval packets: approve the exact action</h3>
<p>Some actions need human judgment: rescheduling a meeting with eight attendees, sending a customer-facing summary on an enterprise account, or canceling an event from a recurring series.</p>
<p>The weak pattern asks the human for approval, then asks the model to perform the action. That gives the model a second chance to drift. The human approves one description, and the model may execute a slightly different action.</p>
<p>The safer pattern uses an approval packet. The packet is a fully materialized action object that the human reviews. After approval, the runtime executes that exact object: same tool name, same arguments, same idempotency key.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb4-1">packet <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb4-2">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"status"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"pending"</span>,</span>
<span id="cb4-3">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"expires_at"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"2026-05-09T17:32:18Z"</span>,</span>
<span id="cb4-4">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"tool"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"reschedule_event"</span>,</span>
<span id="cb4-5">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"args"</span>: {</span>
<span id="cb4-6">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"event_id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"evt_8819"</span>,</span>
<span id="cb4-7">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"new_start"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"2026-05-13T10:00:00-04:00"</span>,</span>
<span id="cb4-8">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"idempotency_key"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"run_204:evt_8819:reschedule"</span>,</span>
<span id="cb4-9">    },</span>
<span id="cb4-10">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"human_summary"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Move Tuesday 2pm with Priya to Wednesday 10am"</span>,</span>
<span id="cb4-11">}</span></code></pre></div></div>
<p>Before execution, the runtime checks that the approval has not expired and that the relevant state has not changed. If the target event, user request, or proposed action changed, the packet is stale and should not execute.</p>
<p>Humans should approve the exact action rather than a summary of it.</p>
</section>
</section>
<section id="gate-where-failure-matters" class="level2">
<h2 class="anchored" data-anchor-id="gate-where-failure-matters">Gate where failure matters</h2>
<p>Gate the actions where failure matters most, not every action.</p>
<p>Heavy gates belong around actions that change the outside world, affect another person, are hard to undo, depend on weak evidence, or may require human escalation. Keep the path light for harmless clarifying questions, read-only lookups, and low-stakes summaries.</p>
<p>The goal is appropriate gating, not maximum gating. A gate that fires on every action becomes a gate the team learns to ignore.</p>
</section>
<section id="the-shape-worth-keeping" class="level2">
<h2 class="anchored" data-anchor-id="the-shape-worth-keeping">The shape worth keeping</h2>
<p>The runtime loop is simple:</p>
<pre class="text"><code>The model proposes.
The harness checks.
The tool executes.
The harness verifies.</code></pre>
<p>Prompts inform the proposal. Gates check it. Tool contracts narrow what can be proposed. Safe fallbacks turn blocked actions into controlled detours. Approval packets keep humans and runtime aligned on the exact action.</p>
<p>A prompt alone cannot do those jobs. That is what gates are for.</p>
<p>The next article <a href="../../../../posts/series/the-agent-harness/05-traces/index.html">Traces are how agents get better</a> shows what useful traces contain, how they reveal the first bad move in a failing run, and how one bad run becomes a permanent improvement to the harness.</p>


</section>

 ]]></description>
  <category>The agent harness</category>
  <guid>https://yeesengchan.com/posts/series/the-agent-harness/04-prompts-gate/</guid>
  <pubDate>Fri, 01 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>State, not transcript, is agent memory</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/the-agent-harness/03-state-not-transcript/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>The agent harness</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-demos-break/">Why AI agent demos break in production</a></li>
<li><a href="../02-harness-is-the-product/">The harness is the product</a></li>
<li><a href="../03-state-not-transcript/">State, not transcript, is agent memory</a></li>
<li><a href="../04-prompts-gate/">Prompts guide. Gates enforce</a></li>
<li><a href="../05-traces/">Traces are how agents get better</a></li>
</ol>
</div>
<p>Conversation history looks like memory, so many agents use it that way. Each turn gets appended to the transcript, the transcript gets passed back to the model, and the model is expected to remember what matters. This works in short demos because the conversation is small and the stakes are low. It breaks when the agent has to make reliable decisions across many turns.</p>
<p>The intake agent makes the failure concrete. On turn 2, the user said, “We’re on the enterprise plan, and the dashboard is the only feature we use.” On turn 9, the agent asked, “Just to confirm, are you on the standard or enterprise plan?” On turn 12, the handoff record went out with <code>plan_tier: unknown</code>.</p>
<p>The system failed because it never stored the plan tier in state. The answer stayed buried in the transcript instead of becoming <code>plan_tier = enterprise</code>. Any fact that affects future behavior should become a field that later steps can read.</p>
<p><a href="../../../../posts/series/the-agent-harness/02-harness-is-the-product/index.html">The previous article</a> argued that the harness is the product. State is the first concrete part of that harness: the facts, uncertainties, workflow status, and pending actions that the runtime and model read before the next step.</p>
<section id="raw-history-is-a-record-not-a-decision-layer" class="level2">
<h2 class="anchored" data-anchor-id="raw-history-is-a-record-not-a-decision-layer">Raw history is a record, not a decision layer</h2>
<p>Raw history preserves the original material, including user messages and tool outputs. The transcript may contain the answer, but later steps need explicit fields to read.</p>
<p>For the intake agent, the handoff writer needed this field:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb1-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"plan_tier"</span>: {</span>
<span id="cb1-2">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"value"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"enterprise"</span>,</span>
<span id="cb1-3">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confidence"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confirmed"</span>,</span>
<span id="cb1-4">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"source"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"user_turn_2"</span>,</span>
<span id="cb1-5">}</span></code></pre></div></div>
<p>That field lets the agent skip a redundant plan-tier question, lets the handoff writer emit <code>plan_tier: enterprise</code>, and lets a required-field check decide whether the handoff is ready.</p>
<p>State also records reliability. If a user first says, “I think it might be the migration,” and later says, “Support confirmed it was the migration,” state should mark one claim as uncertain and the other as confirmed. The transcript preserves both sentences; state tells the system how to use them.</p>
<p>The same issue appears in handoff readiness. If the handoff requires <code>plan_tier</code>, <code>affected_feature</code>, and <code>confirmed_root_cause</code>, state should show which fields are filled, which are uncertain, and which still need follow-up. The agent can then read state instead of reconstructing the situation from the transcript every turn.</p>
</section>
<section id="state-turns-remembered-facts-into-usable-facts" class="level2">
<h2 class="anchored" data-anchor-id="state-turns-remembered-facts-into-usable-facts">State turns remembered facts into usable facts</h2>
<p>After turn 2, the intake agent should have written this state:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb2-1">state <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb2-2">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"facts"</span>: {</span>
<span id="cb2-3">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"plan_tier"</span>: {</span>
<span id="cb2-4">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"value"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"enterprise"</span>,</span>
<span id="cb2-5">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confidence"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confirmed"</span>,</span>
<span id="cb2-6">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"source"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"user_turn_2"</span>,</span>
<span id="cb2-7">        },</span>
<span id="cb2-8">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"affected_feature"</span>: {</span>
<span id="cb2-9">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"value"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"dashboard"</span>,</span>
<span id="cb2-10">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confidence"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confirmed"</span>,</span>
<span id="cb2-11">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"source"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"user_turn_2"</span>,</span>
<span id="cb2-12">        },</span>
<span id="cb2-13">    },</span>
<span id="cb2-14">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"open_questions"</span>: [],</span>
<span id="cb2-15">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"workflow_phase"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"discovery"</span>,</span>
<span id="cb2-16">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"ready_for_handoff"</span>: <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">False</span>,</span>
<span id="cb2-17">}</span></code></pre></div></div>
<p>On turn 9, the agent checks <code>state["facts"]["plan_tier"]</code> before asking another plan-tier question. The field already says <code>enterprise</code>, with <code>confidence = confirmed</code>, so the agent moves on. On turn 12, the handoff writer reads the same field and emits <code>plan_tier: enterprise</code> instead of <code>plan_tier: unknown</code>.</p>
<p>Store information the system needs later in named fields, and update those fields when new evidence arrives. The schema depends on the agent, but the rule stays the same: operational facts should not live only inside raw text.</p>
<p>State should stay focused. It does not need every utterance, retrieved passage, or intermediate model output. Those belong in raw history or trace. State should contain the information that changes future behavior: filled values, confidence, unresolved questions, workflow phase, proposed actions, and verification status.</p>
</section>
<section id="state-needs-belief-status" class="level2">
<h2 class="anchored" data-anchor-id="state-needs-belief-status">State needs belief status</h2>
<p>A weak state object stores only values:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb3-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"root_cause"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"migration"</span></span></code></pre></div></div>
<p>That field alone does not tell the system how safely it can rely on the value. A better state object stores belief status alongside the value:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb4-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"root_cause"</span>: {</span>
<span id="cb4-2">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"value"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"migration"</span>,</span>
<span id="cb4-3">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confidence"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"uncertain"</span>,</span>
<span id="cb4-4">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"source"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"user_turn_5"</span>,</span>
<span id="cb4-5">}</span></code></pre></div></div>
<p>The confidence label tells the harness whether to use, verify, qualify, or block the field. A guessed root cause and a confirmed root cause should not drive the same behavior.</p>
<p>Five labels cover many production cases:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 33%">
<col style="width: 33%">
<col style="width: 33%">
</colgroup>
<thead>
<tr class="header">
<th>Label</th>
<th>Meaning</th>
<th>Harness behavior</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>confirmed</code></td>
<td>Safe to rely on</td>
<td>Use it, summarize it, or act on it if other gates pass</td>
</tr>
<tr class="even">
<td><code>uncertain</code></td>
<td>Plausible, but not safe yet</td>
<td>Ask, verify, or avoid treating it as fact</td>
</tr>
<tr class="odd">
<td><code>needs_verification</code></td>
<td>Requires a specific check</td>
<td>Run a lookup, validator, or read-after-write step</td>
</tr>
<tr class="even">
<td><code>stale</code></td>
<td>Was once true but may no longer be true</td>
<td>Refresh before relying on it</td>
</tr>
<tr class="odd">
<td><code>contradicted</code></td>
<td>Conflicting evidence exists</td>
<td>Preserve both sides and resolve before acting</td>
</tr>
</tbody>
</table>
<p>Contradictions need to remain visible. If the user first says, “We do not have internal logs for this system,” and later says, “The application logs show the migration completed successfully,” the updater should preserve both statements, mark the relevant field as <code>contradicted</code> or <code>needs_verification</code>, and leave the next step with a clear ambiguity to resolve.</p>
<p>State should preserve ambiguity in a form the next decision can see.</p>
</section>
<section id="raw-history-state-and-trace-serve-different-jobs" class="level2">
<h2 class="anchored" data-anchor-id="raw-history-state-and-trace-serve-different-jobs">Raw history, state, and trace serve different jobs</h2>
<p>Raw history, state, and trace overlap, but each one has a different job.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 33%">
<col style="width: 33%">
<col style="width: 33%">
</colgroup>
<thead>
<tr class="header">
<th>Artifact</th>
<th>Job</th>
<th>Intake example</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Raw history</td>
<td>Preserves the original material</td>
<td>The user said, “We’re on the enterprise plan…”</td>
</tr>
<tr class="even">
<td>State</td>
<td>Stores the current working memory</td>
<td><code>plan_tier = enterprise</code>, <code>confidence = confirmed</code></td>
</tr>
<tr class="odd">
<td>Trace</td>
<td>Records what happened during the run</td>
<td>Turn 2 updated <code>plan_tier</code>; turn 9 skipped a redundant question; turn 12 produced the handoff</td>
</tr>
</tbody>
</table>
<p>Raw history preserves nuance, tone, and provenance. Trace explains how the system behaved. State guides the next decision. If a fact affects what the system asks, writes, summarizes, verifies, or hands off, it belongs in state.</p>
</section>
<section id="different-agents-need-different-state-schemas" class="level2">
<h2 class="anchored" data-anchor-id="different-agents-need-different-state-schemas">Different agents need different state schemas</h2>
<p>State should match the decisions the agent has to make.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 50%">
<col style="width: 50%">
</colgroup>
<thead>
<tr class="header">
<th>Agent type</th>
<th>State needs to record</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Intake agent</td>
<td>Confirmed facts, uncertain facts, open questions, handoff readiness</td>
</tr>
<tr class="even">
<td>Scheduling assistant</td>
<td>Candidate events, selected target, proposed change, approval, verification status</td>
</tr>
<tr class="odd">
<td>Docs Q&amp;A agent</td>
<td>Retrieved refs, grounded claims, evidence mapping, validation status</td>
</tr>
</tbody>
</table>
<p>A scheduling assistant must know whether it has selected the right calendar event before it can reschedule anything. A docs Q&amp;A agent must know whether each claim is supported by retrieved evidence before it can answer. An intake agent must know whether it has enough confirmed information to produce a useful handoff.</p>
<p>State holds the information the next decision needs.</p>
</section>
<section id="state-should-answer-four-questions" class="level2">
<h2 class="anchored" data-anchor-id="state-should-answer-four-questions">State should answer four questions</h2>
<p>A useful state object answers four questions at each step:</p>
<ol type="1">
<li>What do we currently believe?</li>
<li>How sure are we?</li>
<li>What remains unclear?</li>
<li>What stage of the workflow are we in?</li>
</ol>
<p>These questions expose whether state is usable. Without those fields, the model has to reconstruct the situation from raw history. It may treat guesses as facts, skip required questions, or advance the workflow too early.</p>
<p>State gives the model and runtime stable fields to read. The model uses state to decide what to say next; the runtime uses state to decide what is allowed next.</p>
</section>
<section id="state-changes-as-the-workflow-runs" class="level2">
<h2 class="anchored" data-anchor-id="state-changes-as-the-workflow-runs">State changes as the workflow runs</h2>
<p>State is maintained throughout the workflow. Each meaningful step reads from it, updates it, and leaves the system in a clearer position than before.</p>
<p>A typical loop looks like this:</p>
<ol type="1">
<li><strong>Understand.</strong> Read the current state and the new input. Update facts when the input is clear. Mark uncertainty when it is not.</li>
<li><strong>Decide.</strong> Choose the next action: ask a clarifying question, call a tool, draft a summary, produce a handoff, or refuse.</li>
<li><strong>Execute.</strong> Take the action. If a tool is called, capture its inputs and outputs. If a write happens, record the attempt.</li>
<li><strong>Verify.</strong> Check whether the action did what it was supposed to do. Update state with what is now known.</li>
</ol>
<div id="fig-state-loop" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A four-step loop drawn around a central state store. The steps run in sequence, with the Understand step shown reading from the central state and the Verify step shown writing back to it, so state persists across iterations. A label notes that the steps may be conversation turns, workflow phases, or pipeline stages, while state remains the through-line connecting them.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-state-loop-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/the-agent-harness/03-state-not-transcript/state_loop.png" class="img-fluid figure-img" alt="A four-step loop drawn around a central state store. The steps run in sequence, with the Understand step shown reading from the central state and the Verify step shown writing back to it, so state persists across iterations. A label notes that the steps may be conversation turns, workflow phases, or pipeline stages, while state remains the through-line connecting them.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-state-loop-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: The four-step loop operates on state. Every Understand step reads from state; every Verify step writes back to it. State is the system’s through-line across steps, whether the steps are conversation turns, workflow phases, or pipeline stages.
</figcaption>
</figure>
</div>
<p>The loop appears in different forms across agents. A scheduling assistant may run it across target selection, approval, execution, and verification. The names change, but the pattern stays the same: read state, choose the next move, execute it, and update state based on what happened.</p>
<p>Verification keeps state honest. After a write, the system should check the external source of truth before treating state as updated. For example, after rescheduling a meeting, it should confirm that the calendar shows the new time.</p>
</section>
<section id="gates-read-state" class="level2">
<h2 class="anchored" data-anchor-id="gates-read-state">Gates read state</h2>
<p>Gates enforce policies by inspecting state. A handoff gate can block missing fields, uncertain facts, or unresolved questions only if state records them explicitly.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb5-1"><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> action <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">==</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"produce_handoff"</span>:</span>
<span id="cb5-2">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">assert</span> state[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"facts"</span>][<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"plan_tier"</span>][<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confidence"</span>] <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">==</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confirmed"</span></span>
<span id="cb5-3">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">assert</span> state[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"facts"</span>][<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"affected_feature"</span>][<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confidence"</span>] <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">==</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confirmed"</span></span>
<span id="cb5-4">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">assert</span> state[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"ready_for_handoff"</span>] <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">is</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">True</span></span></code></pre></div></div>
<p>Prompts suggest behavior; runtime checks enforce it.</p>
</section>
<section id="common-mistakes" class="level2">
<h2 class="anchored" data-anchor-id="common-mistakes">Common mistakes</h2>
<p>State usually fails in predictable ways:</p>
<ul>
<li><strong>No operational state:</strong> raw history becomes state, and the model has to reconstruct the situation every step.</li>
<li><strong>Too much state:</strong> every utterance, passage, draft, and tool output gets persisted, making state as noisy as the transcript.</li>
<li><strong>No confidence labels:</strong> <code>"root_cause": "migration"</code> looks settled even if the user only guessed it.</li>
<li><strong>Silent contradiction handling:</strong> conflicting evidence gets overwritten instead of staying visible until the system resolves it.</li>
<li><strong>State drift:</strong> failed writes, stale retrieved passages, and user corrections do not update the stored belief.</li>
</ul>
<p>Use a simple test when deciding what belongs in state: will the system behave worse later if this information only lives in raw history or trace? Put it in state only when the answer is yes.</p>
</section>
<section id="state-is-the-systems-memory" class="level2">
<h2 class="anchored" data-anchor-id="state-is-the-systems-memory">State is the system’s memory</h2>
<p>The transcript records what was said; state records what the system can rely on.</p>
<p>The intake agent needed to store two confirmed facts: the user was on the enterprise plan, and the dashboard was the affected feature. Once those facts became state, later steps could use them. The agent could avoid a redundant question, produce a better handoff, and expose unresolved fields before claiming the workflow was ready.</p>
<p>State gives gates, tools, verification steps, and traces concrete fields to read and update. Runtime control reads state before acting. Gates and tool contracts decide what the system is allowed to do with that memory.</p>


</section>

 ]]></description>
  <category>The agent harness</category>
  <guid>https://yeesengchan.com/posts/series/the-agent-harness/03-state-not-transcript/</guid>
  <pubDate>Tue, 28 Apr 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>The harness is the product</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/the-agent-harness/02-harness-is-the-product/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>The agent harness</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-demos-break/">Why AI agent demos break in production</a></li>
<li><a href="../02-harness-is-the-product/">The harness is the product</a></li>
<li><a href="../03-state-not-transcript/">State, not transcript, is agent memory</a></li>
<li><a href="../04-prompts-gate/">Prompts guide. Gates enforce</a></li>
<li><a href="../05-traces/">Traces are how agents get better</a></li>
</ol>
</div>
<p>A stronger model does not automatically give you a reliable agent.</p>
<p>The <a href="../../../../posts/series/the-agent-harness/01-demos-break/index.html">previous article</a> catalogued six ways agent demos break in production. The intake agent forgot a fact the user gave it on turn 2. The scheduling assistant double-booked a meeting after an ambiguous timeout. The docs Q&amp;A agent gave a confident, well-formed, wrong answer.</p>
<p>The instinct after each failure is to reach for a stronger model. Sometimes that helps a little. More often, the failure returns in a slightly different shape because the system around the model has not changed. The reframe:</p>
<p><strong>Agent = model + harness</strong></p>
<p>The model is the reasoning engine; the harness is the system the model lives inside. Models get swapped, cheaper, and faster. The harness carries the team’s accumulated understanding of how to make the agent reliable. For production agents, the harness is the product.</p>
<section id="prompts-guide.-harnesses-constrain." class="level2">
<h2 class="anchored" data-anchor-id="prompts-guide.-harnesses-constrain.">Prompts guide. Harnesses constrain.</h2>
<p>A prompt tells the model what behavior you want; a harness decides what behavior the system permits.</p>
<p>The difference is concrete:</p>
<ul>
<li><strong>Refunds:</strong> a prompt can say, “Do not issue refunds.” A harness can make the refund tool unavailable.</li>
<li><strong>Scheduling:</strong> a prompt can say, “Ask for clarification if the target meeting is ambiguous.” A harness can reject <code>reschedule_event</code> unless state contains one confirmed event ID.</li>
<li><strong>Docs Q&amp;A:</strong> a prompt can say, “Use only the retrieved documents.” A harness can require every final claim to map to a cited passage.</li>
<li><strong>Retries:</strong> a prompt can say, “Be careful with retries.” A harness can enforce one write attempt, require an idempotency key, and route ambiguous timeouts to verification.</li>
</ul>
<p>The prompt shapes one model call; the harness controls the workflow around that call. It makes some failures impossible to commit and makes the rest easier to detect.</p>
<p>That is the shift from prompt engineering to harness engineering.</p>
</section>
<section id="scope-is-the-first-harness-decision" class="level2">
<h2 class="anchored" data-anchor-id="scope-is-the-first-harness-decision">Scope is the first harness decision</h2>
<p>Before designing the harness, define the agent’s job. Scope answers three questions:</p>
<ol type="1">
<li>What exact job does this agent do?</li>
<li>What artifact or outcome should exist when it succeeds?</li>
<li>What is it explicitly not allowed to do?</li>
</ol>
<p>For the intake agent, a scoped version reads:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode markdown code-with-copy"><code class="sourceCode markdown"><span id="cb1-1">A discovery-stage intake agent that conducts an initial conversation with a user reporting a support issue and produces a structured handoff for a human support agent. It may ask clarifying questions, retrieve approved account information, and summarize confirmed facts and open questions. It may not commit to remediation, issue credits or refunds, change account state, or attempt to resolve the issue itself.</span></code></pre></div></div>
</section>
<section id="the-harness-has-four-jobs" class="level2">
<h2 class="anchored" data-anchor-id="the-harness-has-four-jobs">The harness has four jobs</h2>
<div id="fig-harness-jobs" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A two-by-two grid of four numbered cards describing the harness's four jobs. Card 1, Decide what the model sees: build the right context, not a pile of everything. Mechanisms: state, context assembly. Card 2, Limit what the model can do: approve actions before they run. Mechanisms: workflow phases, tool contracts, gates, approval packets, permissions. Card 3, Check what actually happened: verify the world, not just the tool's success. Mechanisms: state readback, grounding checks, safe fallbacks. Card 4, Keep a record: failures should leave evidence. Mechanisms: traces, versions, decisions, tool inputs and outputs, verification results, regression cases.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-harness-jobs-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/the-agent-harness/02-harness-is-the-product/harness_four_jobs.png" class="img-fluid figure-img" alt="A two-by-two grid of four numbered cards describing the harness's four jobs. Card 1, Decide what the model sees: build the right context, not a pile of everything. Mechanisms: state, context assembly. Card 2, Limit what the model can do: approve actions before they run. Mechanisms: workflow phases, tool contracts, gates, approval packets, permissions. Card 3, Check what actually happened: verify the world, not just the tool's success. Mechanisms: state readback, grounding checks, safe fallbacks. Card 4, Keep a record: failures should leave evidence. Mechanisms: traces, versions, decisions, tool inputs and outputs, verification results, regression cases.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-harness-jobs-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: The harness has four jobs: decide what the model sees, limit what the model can do, check what actually happened, and keep a record.
</figcaption>
</figure>
</div>
<p>Once scope is clear, the harness has four jobs:</p>
<ul>
<li><strong>Shape the input:</strong> decide what the model sees at each step. The model should receive the relevant state, retrieved evidence, available tools, and local instruction, not a pile of every transcript turn and document.</li>
<li><strong>Bound the action:</strong> decide what the model is allowed to do. The model can propose an action, but workflow phases, tool contracts, gates, approval packets, and permissions decide whether it runs.</li>
<li><strong>Verify the outcome:</strong> check what actually happened. A tool returning <code>success</code> does not prove the world is correct. A rescheduled meeting needs calendar readback. A grounded answer needs claim-to-evidence checks.</li>
<li><strong>Preserve the evidence:</strong> record enough information to debug and improve the system. Useful traces include input, state, action, tool inputs and outputs, verification results, state after, and version information.</li>
</ul>
<p>A reliable agent takes bounded actions, fails safely when it cannot proceed, leaves enough evidence to debug the run, and becomes harder to break after each incident.</p>
</section>
<section id="prompt-context-and-harness-engineering" class="level2">
<h2 class="anchored" data-anchor-id="prompt-context-and-harness-engineering">Prompt, context, and harness engineering</h2>
<p>Prompt, context, and harness engineering solve different problems.</p>
<div id="fig-nested-levels" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Three concentric nested boxes or rings showing levels of engineering. The innermost level is prompt engineering, enclosed by a larger level for context engineering, enclosed by the outermost level for the harness. The nesting shows each level containing the one inside it: a good harness contains good context engineering, which contains good prompts.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-nested-levels-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/the-agent-harness/02-harness-is-the-product/nested.png" class="img-fluid figure-img" alt="Three concentric nested boxes or rings showing levels of engineering. The innermost level is prompt engineering, enclosed by a larger level for context engineering, enclosed by the outermost level for the harness. The nesting shows each level containing the one inside it: a good harness contains good context engineering, which contains good prompts.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-nested-levels-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: Three nested levels of engineering. Each level encompasses the previous. A good harness contains good context engineering, which contains good prompts.
</figcaption>
</figure>
</div>
<ul>
<li><strong>Prompt engineering:</strong> writes the instructions for one model call: wording, structure, examples, and output format.</li>
<li><strong>Context engineering:</strong> decides what enters the model context at each step: system instructions, relevant state, retrieved passages, memory, available tools, and local task framing.</li>
<li><strong>Harness engineering:</strong> controls the application around the model: when context is assembled, which tools are available, which actions are allowed, how writes are verified, how state persists, how failures recover, how traces are written, and how regression cases catch old failures.</li>
</ul>
<p>Putting the failure at the right level saves wasted iteration. A bad instruction may need a prompt change. Missing evidence may need context changes. Unsafe actions, duplicate writes, missing state, weak verification, and thin traces need harness changes.</p>
</section>
<section id="this-is-not-a-call-for-overengineering" class="level2">
<h2 class="anchored" data-anchor-id="this-is-not-a-call-for-overengineering">This is not a call for overengineering</h2>
<p>A harness matters, but teams can build too much harness too early. The minimum useful harness depends on the agent’s scope.</p>
<ul>
<li><strong>Intake agent:</strong> clear scope, structured state with confidence labels, a few workflow phases, no write tools, a basic handoff artifact, step traces, and a small regression set.</li>
<li><strong>Scheduling assistant:</strong> target identification, narrow write tools with idempotency, one-attempt write policy, read-after-write verification, and traces of tool inputs and outputs.</li>
<li><strong>Docs Q&amp;A agent:</strong> retrieval provenance, claim-to-evidence mapping, a “no supported answer” behavior for thin evidence, auth-aware retrieval, and traceable citations.</li>
</ul>
<p>Build the harness around the risks created by the job. An intake agent without write authority does not need the same side-effect controls as a scheduling assistant. A docs Q&amp;A agent does need evidence mapping because its main risk is confident unsupported synthesis.</p>
</section>
<section id="which-parts-of-the-harness-age" class="level2">
<h2 class="anchored" data-anchor-id="which-parts-of-the-harness-age">Which parts of the harness age?</h2>
<p>Some harness pieces get lighter as models improve. Tool-selection scaffolding matters less when models choose tools reliably. Format validators matter less when structured output becomes dependable. Long prompt chains matter less when models handle more reasoning in one call. These pieces compensate for model limitations, so stronger models reduce their importance.</p>
<p>Other harness pieces remain necessary because they control product risk. A smarter model still cannot unsend a calendar invite, undo a payment, or reconstruct last month’s behavior without traces. It still needs runtime boundaries around tools, writes, permissions, and user-visible actions.</p>
<p>The pieces that age are scaffolding around weak model behavior, while the pieces that stay are tied to reliability: state, tool contracts, gates, verification, recovery, traces, and tests.</p>


</section>

 ]]></description>
  <category>The agent harness</category>
  <guid>https://yeesengchan.com/posts/series/the-agent-harness/02-harness-is-the-product/</guid>
  <pubDate>Thu, 23 Apr 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Why AI agent demos break in production</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/the-agent-harness/01-demos-break/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>The agent harness</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-demos-break/">Why AI agent demos break in production</a></li>
<li><a href="../02-harness-is-the-product/">The harness is the product</a></li>
<li><a href="../03-state-not-transcript/">State, not transcript, is agent memory</a></li>
<li><a href="../04-prompts-gate/">Prompts guide. Gates enforce</a></li>
<li><a href="../05-traces/">Traces are how agents get better</a></li>
</ol>
</div>
<p>Most <a href="../../../../posts/series/what-an-agent-actually-is/index.html">AI agent</a> demos fail in boring ways.</p>
<p>The agent forgets something the user said five turns ago. It treats a guess as a fact. It calls the wrong tool because the tool name sounded close enough. It returns a polished answer only loosely connected to the evidence. It retries a calendar write after a timeout and books the same meeting twice. Then, when someone asks what happened, the logs say:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb1-1">tool_call: retrieval, status: success</span></code></pre></div></div>
<p>Reliability becomes concrete after something breaks. The team needs to know why the agent believed a claim, why it called a tool, why it skipped a clarifying question, why it retried a write, and why the run cannot be reconstructed.</p>
<p>The usual instinct is to blame the model. A stronger model may help, but many agent failures come from the system around the model: state, workflow, tool contracts, gates, retry policy, retrieval design, traces, and evaluation.</p>
<p>The diagnostic question is simple: what failed, and where should control have lived?</p>
<p>That question starts harness engineering. This series is about building agent systems that survive real users, real tools, real ambiguity, and real debugging.</p>
<section id="three-small-agents" class="level2">
<h2 class="anchored" data-anchor-id="three-small-agents">Three small agents</h2>
<p>This series uses three running examples.</p>
<ul>
<li><strong>Customer support intake agent:</strong> holds a conversation with a user, gathers the issue, identifies what is known and uncertain, and produces a structured handoff for a human. It does not solve the issue or write to external systems.</li>
<li><strong>Scheduling assistant:</strong> books, reschedules, and cancels meetings. It uses tools that change the world.</li>
<li><strong>Internal docs Q&amp;A agent:</strong> answers questions over company documentation. It retrieves passages and synthesizes a short response.</li>
</ul>
<p>Six failure shapes show up across these agents.</p>
</section>
<section id="failure-1-the-agent-bluffs-when-it-should-pause" class="level2">
<h2 class="anchored" data-anchor-id="failure-1-the-agent-bluffs-when-it-should-pause">Failure 1: The agent bluffs when it should pause</h2>
<p>A user opens the intake conversation: “We’re having issues with the new dashboard, and I think it’s related to the migration we did last month, but I’m not totally sure.”</p>
<p>A weak agent starts triaging the migration. It asks about migration steps, surfaces likely root causes, and drafts a handoff that pins the issue to the migration.</p>
<p>The user never confirmed the migration as the cause. The agent treated a hedge as a fact.</p>
<p>A reliable agent should record the uncertainty and ask a clarifying question before building on the claim: “Has the migration been confirmed as the cause, or is that still a guess?”</p>
<p>The system needs a field for uncertainty:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb2-1">state[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"root_cause"</span>] <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb2-2">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"value"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"migration"</span>,</span>
<span id="cb2-3">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confidence"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"uncertain"</span>,</span>
<span id="cb2-4">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"source"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"user_hedged_statement"</span>,</span>
<span id="cb2-5">}</span></code></pre></div></div>
<p>It also needs behavior tied to that label. If the next step depends on a fact labeled <code>uncertain</code>, the workflow should route to clarification instead of action.</p>
<ul>
<li><strong>Visible failure:</strong> the agent jumped to conclusions.</li>
<li><strong>Deeper failure:</strong> the system had no mechanism for treating uncertainty differently from certainty.</li>
</ul>
</section>
<section id="failure-2-the-agent-forgets-what-already-happened" class="level2">
<h2 class="anchored" data-anchor-id="failure-2-the-agent-forgets-what-already-happened">Failure 2: The agent forgets what already happened</h2>
<p>In the same intake conversation, the user says on turn 2: “We’re on the enterprise plan, and the dashboard is the only feature we use.” On turn 9, the agent asks: “Just to confirm, are you on the standard or enterprise plan?” On turn 12, the handoff record lists the plan as <code>unknown</code>.</p>
<p>The system failed because it never stored the plan tier in state. It stuffed the transcript back into context every turn and relied on the model to keep the right facts active.</p>
<p>The fix is to store confirmed facts once:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb3-1">state[<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"customer"</span>] <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb3-2">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"plan_tier"</span>: {</span>
<span id="cb3-3">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"value"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"enterprise"</span>,</span>
<span id="cb3-4">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confidence"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confirmed"</span>,</span>
<span id="cb3-5">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"source_turn"</span>: <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>,</span>
<span id="cb3-6">    },</span>
<span id="cb3-7">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"primary_feature"</span>: {</span>
<span id="cb3-8">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"value"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"dashboard"</span>,</span>
<span id="cb3-9">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confidence"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confirmed"</span>,</span>
<span id="cb3-10">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"source_turn"</span>: <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>,</span>
<span id="cb3-11">    },</span>
<span id="cb3-12">}</span></code></pre></div></div>
<p>Now the agent reads <code>plan_tier = enterprise</code> instead of rediscovering it from the transcript. The transcript records what the user said; state records what the system can rely on. <a href="../../../../posts/series/the-agent-harness/03-state-not-transcript/index.html">State, not transcript, is agent memory</a> goes deeper into that distinction.</p>
<ul>
<li><strong>Visible failure:</strong> the model forgot.</li>
<li><strong>Deeper failure:</strong> the system treated the conversation transcript as memory.</li>
</ul>
</section>
<section id="failure-3-the-agent-takes-the-wrong-action" class="level2">
<h2 class="anchored" data-anchor-id="failure-3-the-agent-takes-the-wrong-action">Failure 3: The agent takes the wrong action</h2>
<p>The scheduling assistant has these tools:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb4-1">list_calendar_events()</span>
<span id="cb4-2">find_event_by_description()</span>
<span id="cb4-3">reschedule_event()</span>
<span id="cb4-4">cancel_event()</span></code></pre></div></div>
<p>The user says: “Move my Tuesday review with Priya to Thursday.”</p>
<p>A weak agent calls <code>cancel_event</code> and creates a fresh booking. Or it calls <code>reschedule_event</code> with a fuzzy description, and the API matches the wrong event.</p>
<p>The safe sequence is explicit: find the event, disambiguate if multiple events match, then reschedule a specific event ID. The model should not invent that sequence from scratch every time.</p>
<p>The harness should make the wrong action hard to take. It can require an identified event ID before <code>reschedule_event</code>, require user confirmation when multiple events match, and block destructive actions unless the workflow phase allows them.</p>
<ul>
<li><strong>Visible failure:</strong> the agent picked the wrong tool.</li>
<li><strong>Deeper failure:</strong> the system relied on the model to invent a safe tool sequence instead of encoding the sequence in the harness.</li>
</ul>
</section>
<section id="failure-4-the-agent-sounds-grounded-but-it-is-not" class="level2">
<h2 class="anchored" data-anchor-id="failure-4-the-agent-sounds-grounded-but-it-is-not">Failure 4: The agent sounds grounded, but it is not</h2>
<p>The docs Q&amp;A agent receives this question: “What is our policy on customer data retention for trial accounts?”</p>
<p>It retrieves five passages and returns a confident answer saying 30 days. The corpus says 90 days. The right passage was in the retrieved set, but synthesis leaned on the wrong evidence.</p>
<p>A reliable retrieval agent should track supported claims explicitly. The system needs to know which claim came from which passage, and whether that passage addresses the question being asked.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb5-1">supported_claims <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> [</span>
<span id="cb5-2">    {</span>
<span id="cb5-3">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"claim"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Trial account data is retained for 90 days after trial expiry."</span>,</span>
<span id="cb5-4">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"evidence_refs"</span>: [<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"policy_42:trial_accounts"</span>],</span>
<span id="cb5-5">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confidence"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"confirmed"</span>,</span>
<span id="cb5-6">    }</span>
<span id="cb5-7">]</span></code></pre></div></div>
<p>Synthesis should write from supported claims, not from a raw pile of passages.</p>
<ul>
<li><strong>Visible failure:</strong> the agent gave a wrong answer.</li>
<li><strong>Deeper failure:</strong> the system never tracked which claims were supported by which evidence.</li>
</ul>
<p>Groundedness is a system property; polished prose does not make an answer grounded.</p>
</section>
<section id="failure-5-the-agent-repeats-a-side-effect" class="level2">
<h2 class="anchored" data-anchor-id="failure-5-the-agent-repeats-a-side-effect">Failure 5: The agent repeats a side effect</h2>
<p>The scheduling assistant calls:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb6-1">reschedule_event(</span>
<span id="cb6-2">    event_id<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"evt_482"</span>,</span>
<span id="cb6-3">    new_time<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"2026-05-12T14:00"</span>,</span>
<span id="cb6-4">)</span></code></pre></div></div>
<p>The API takes thirty seconds and returns a timeout. The agent cannot tell whether the write succeeded, so it retries. Now Priya receives two confusing calendar updates.</p>
<p>Writes need different retry semantics from reads. Every write should carry an idempotency key:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb7-1">payload <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb7-2">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"event_id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"evt_482"</span>,</span>
<span id="cb7-3">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"new_time"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"2026-05-12T14:00"</span>,</span>
<span id="cb7-4">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"idempotency_key"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"run_847_step_3"</span>,</span>
<span id="cb7-5">}</span></code></pre></div></div>
<p>After an ambiguous timeout, the agent should check the calendar before retrying.</p>
<ul>
<li><strong>Visible failure:</strong> the meeting update happened twice.</li>
<li><strong>Deeper failure:</strong> the system treated reads and writes as equivalent tool calls.</li>
</ul>
</section>
<section id="failure-6-nobody-can-debug-it" class="level2">
<h2 class="anchored" data-anchor-id="failure-6-nobody-can-debug-it">Failure 6: Nobody can debug it</h2>
<p>A user reports that the docs Q&amp;A agent gave a wrong answer about retention policy yesterday. The logs contain the user’s question, the agent’s response, and this line:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb8-1">tool_call: retrieval, status: success</span></code></pre></div></div>
<p>That log cannot localize the failure. It does not show which passages were retrieved, which were ranked highest, what prompt version was deployed, what state the system had, or which answer claims came from which passage.</p>
<p>A useful trace records enough to debug the run: input, relevant state, action chosen, tool inputs and outputs, verification result, state after, prompt version, tool version, and model version. Without that trace, the team cannot tell whether the failure came from retrieval, synthesis, prompting, model behavior, or a recent deployment.</p>
<ul>
<li><strong>Visible failure:</strong> the team cannot debug the answer.</li>
<li><strong>Deeper failure:</strong> the system logged steps instead of recording decisions.</li>
</ul>
<p><a href="../../../../posts/series/the-agent-harness/05-traces/index.html">Traces are how agents get better</a> goes deeper into useful traces.</p>
</section>
<section id="the-pattern-behind-the-failures" class="level2">
<h2 class="anchored" data-anchor-id="the-pattern-behind-the-failures">The pattern behind the failures</h2>
<p>Each failure asks the model to carry control that should live in the harness.</p>
<p>The harness decides what the model sees, what actions are allowed, what gets verified, and what is preserved for debugging and improvement. The model still matters, but reliability depends on the control surfaces around it.</p>
<div id="fig-failure-shapes" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A mapping diagram with six agent failure shapes listed on one side and the control surfaces in the surrounding system that should have caught them on the other, with lines connecting each failure to its responsible surface. Every line lands on a system-level control rather than on the model itself, visually making the point that these failures are fixed in the harness around the model, not in the model.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-failure-shapes-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/the-agent-harness/01-demos-break/failure_shapes.png" class="img-fluid figure-img" alt="A mapping diagram with six agent failure shapes listed on one side and the control surfaces in the surrounding system that should have caught them on the other, with lines connecting each failure to its responsible surface. Every line lands on a system-level control rather than on the model itself, visually making the point that these failures are fixed in the harness around the model, not in the model.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-failure-shapes-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Six failure shapes mapped to the control surfaces that should have caught them. The model is not the right place to fix any of these. The system around the model is.
</figcaption>
</figure>
</div>
<p>The control surfaces are concrete:</p>
<ul>
<li><strong>State:</strong> stores facts, uncertainty, workflow phase, and pending actions.</li>
<li><strong>Workflow logic:</strong> determines which step can happen next.</li>
<li><strong>Tool contracts:</strong> define safe inputs, outputs, side effects, and retry behavior.</li>
<li><strong>Gates and validators:</strong> block unsafe or unsupported actions.</li>
<li><strong>Retrieval and evidence mapping:</strong> connect answers to supporting sources.</li>
<li><strong>Traces:</strong> preserve enough information to debug and harden the system.</li>
<li><strong>Regression tests:</strong> keep known failures from returning.</li>
</ul>
<p>The next four articles go deeper on the most important parts:</p>
<ul>
<li><a href="../../../../posts/series/the-agent-harness/02-harness-is-the-product/index.html">The harness is the product</a>: why an agent is the model plus the harness around it.</li>
<li><a href="../../../../posts/series/the-agent-harness/03-state-not-transcript/index.html">State, not transcript, is agent memory</a>: how state gives the system memory it can read and update.</li>
<li><a href="../../../../posts/series/the-agent-harness/04-prompts-gate/index.html">Prompts guide. Gates enforce</a>: how gates and tools limit actions, execute them safely, and verify outcomes.</li>
<li><a href="../../../../posts/series/the-agent-harness/05-traces/index.html">Traces are how agents get better</a>: how traces support debugging, regression tests, and the hardening loop.</li>
</ul>
</section>
<section id="common-mistakes" class="level2">
<h2 class="anchored" data-anchor-id="common-mistakes">Common mistakes</h2>
<p>Teams usually get agent reliability wrong in predictable ways:</p>
<ul>
<li><strong>Model-only diagnosis:</strong> when the agent fails, the team swaps in a stronger model instead of fixing the missing control surface.</li>
<li><strong>Demo-as-evidence thinking:</strong> a demo shows that a path can work, not that the system survives messy users, ambiguous inputs, tool errors, or repeated runs.</li>
<li><strong>Prompt-only boundaries:</strong> a system prompt that says “do not do XYZ” is guidance, not enforcement. Real boundaries live in gates, validators, permissions, and tool contracts.</li>
<li><strong>Step logging:</strong> logs that say what happened do not explain why it happened. A useful trace records state, decisions, tool inputs, tool outputs, verification results, and version information.</li>
<li><strong>Random-failure framing:</strong> most failures are recurring shapes. Once the category is visible, the team can engineer against it.</li>
</ul>
<p>Reliable agents come from moving the right responsibilities into the harness rather than from prompts alone: state, workflow, gates, tools, verification, traces, and tests.</p>


</section>

 ]]></description>
  <category>The agent harness</category>
  <guid>https://yeesengchan.com/posts/series/the-agent-harness/01-demos-break/</guid>
  <pubDate>Sat, 18 Apr 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Behind the scenes of AI agent frameworks</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/what-an-agent-actually-is/02-agent-frameworks/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>What an agent actually is</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-what-is-an-agent/">What the heck is an AI agent?</a></li>
<li><a href="../02-agent-frameworks/">Behind the scenes of AI agent frameworks</a></li>
<li><a href="../03-what-mcp-removes/">What MCP Actually Removes From a Host</a></li>
</ol>
</div>
<p><a href="../../../../posts/series/what-an-agent-actually-is/01-what-is-an-agent/index.html">The first article</a> treated “agent” as a region on a spectrum. This one goes inside one specific point on that spectrum, the loop-with-tools agent, and shows what’s actually happening when you call <code>Runner.run(agent, message)</code> or its equivalent.</p>
<p>Frameworks like the OpenAI Agents SDK, LangChain, and LangGraph hide a lot of mechanics behind clean abstractions. That’s mostly a good thing. But when an agent misbehaves, calls the wrong tool, loops forever, returns garbage, the abstractions stop helping and the underlying mechanics start mattering. Debugging at that point requires distinguishing what the model does from what the API does from what your code does.</p>
<section id="the-three-layers-model-api-your-code" class="level2">
<h2 class="anchored" data-anchor-id="the-three-layers-model-api-your-code">The three layers: model, API, your code</h2>
<p>Three layers do work in any agentic system:</p>
<ul>
<li>The <strong>model</strong> generates tokens. Given tool schemas in the prompt, those tokens include structured tool calls.</li>
<li>The LLM <strong>API</strong> parses what the model generated, exposes it as a typed response, and labels why generation finished.</li>
<li><strong>Your code</strong> (or the framework’s code on your behalf) maintains the conversation state, dispatches tool calls, and runs the loop.</li>
</ul>
<p>The rest of this article walks through one concrete loop end-to-end and names which layer does what at each step. Code is OpenAI-anchored; provider differences are discussed in a later section.</p>
</section>
<section id="what-a-framework-hides" class="level2">
<h2 class="anchored" data-anchor-id="what-a-framework-hides">What a framework hides</h2>
<p>An agent framework wraps four things for you:</p>
<ul>
<li><strong>Schema generation.</strong> Turns Python functions (type hints, Pydantic models, docstrings) into the JSON tool schemas the LLM API expects.</li>
<li><strong>Message management.</strong> Maintains the conversation history, appending the model’s response and tool results in the right shape.</li>
<li><strong>Tool dispatch.</strong> When the API returns tool calls, routes each call to the right Python function with the right arguments and captures the result.</li>
<li><strong>Loop control.</strong> Calls the API, inspects the finish reason, decides whether to continue.</li>
</ul>
<p>The framework doesn’t change what the model or API do. It’s plumbing that you’d otherwise write yourself.</p>
</section>
<section id="a-tiny-framework-example" class="level2">
<h2 class="anchored" data-anchor-id="a-tiny-framework-example">A tiny framework example</h2>
<p>Here’s the OpenAI Agents SDK version of a small task: an agent that generates a few sales emails in different styles, picks the best, and sends it. Modest, but it exercises every mechanism the rest of this article explains.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb1-1"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">from</span> agents <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> Agent, Runner, function_tool</span>
<span id="cb1-2"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">from</span> pydantic <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> BaseModel, Field</span>
<span id="cb1-3"></span>
<span id="cb1-4"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">class</span> GenerateEmailParams(BaseModel):</span>
<span id="cb1-5">    style: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> Field(description<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"'professional', 'engaging', or 'concise'"</span>)</span>
<span id="cb1-6">    include_data: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">bool</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> Field(default<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">False</span>, description<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Include statistics?"</span>)</span>
<span id="cb1-7">    target_length: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> Field(default<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"medium"</span>, description<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"'short', 'medium', or 'long'"</span>)</span>
<span id="cb1-8"></span>
<span id="cb1-9"><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">@function_tool</span></span>
<span id="cb1-10"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> generate_email(params: GenerateEmailParams) <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span> <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span>:</span>
<span id="cb1-11">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"""Generate a sales email in the specified style."""</span></span>
<span id="cb1-12">    ...</span>
<span id="cb1-13"></span>
<span id="cb1-14"><span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">@function_tool</span></span>
<span id="cb1-15"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">def</span> send_email(body: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span>) <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span> <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">dict</span>:</span>
<span id="cb1-16">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"""Send an email to prospects."""</span></span>
<span id="cb1-17">    ...</span>
<span id="cb1-18"></span>
<span id="cb1-19">sales_manager <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> Agent(</span>
<span id="cb1-20">    name<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Sales Manager"</span>,</span>
<span id="cb1-21">    instructions<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"""You are a sales manager. Your job:</span></span>
<span id="cb1-22"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">1. Generate three sales emails using generate_email with different styles.</span></span>
<span id="cb1-23"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">2. Evaluate which is best.</span></span>
<span id="cb1-24"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">3. If none are good enough, generate better versions.</span></span>
<span id="cb1-25"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">4. Once satisfied, send the best one with send_email."""</span>,</span>
<span id="cb1-26">    tools<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>[generate_email, send_email],</span>
<span id="cb1-27">)</span>
<span id="cb1-28"></span>
<span id="cb1-29">result <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">await</span> Runner.run(sales_manager, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Send a cold sales email"</span>)</span></code></pre></div></div>
<p>About fifteen lines. The next section shows what those fifteen lines hide.</p>
</section>
<section id="the-same-loop-without-the-framework" class="level2">
<h2 class="anchored" data-anchor-id="the-same-loop-without-the-framework">The same loop without the framework</h2>
<p>Drop the framework and write the same task against the OpenAI API directly. This is partly an exercise in seeing what gets hidden, partly preparation for the moments when you need to reach into that hidden code.</p>
<section id="send-tool-schemas" class="level3">
<h3 class="anchored" data-anchor-id="send-tool-schemas">1. Send tool schemas</h3>
<p>The model never sees your Python function. It sees a JSON schema in the <code>tools</code> parameter on the API request, with name, description, and parameters:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb2-1">generate_email_schema <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb2-2">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>,</span>
<span id="cb2-3">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>: {</span>
<span id="cb2-4">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"name"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"generate_email"</span>,</span>
<span id="cb2-5">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Generate a sales email in the specified style."</span>,</span>
<span id="cb2-6">        <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"parameters"</span>: {</span>
<span id="cb2-7">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"object"</span>,</span>
<span id="cb2-8">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"properties"</span>: {</span>
<span id="cb2-9">                <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"style"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"string"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"'professional', 'engaging', or 'concise'"</span>},</span>
<span id="cb2-10">                <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"include_data"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"boolean"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Include statistics?"</span>},</span>
<span id="cb2-11">                <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"target_length"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"string"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"'short', 'medium', or 'long'"</span>},</span>
<span id="cb2-12">            },</span>
<span id="cb2-13">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"required"</span>: [<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"style"</span>],</span>
<span id="cb2-14">        },</span>
<span id="cb2-15">    },</span>
<span id="cb2-16">}</span></code></pre></div></div>
<p>The framework’s <code>@function_tool</code> decorator generates this from the Pydantic model and the docstring at import time. Without the framework you write it by hand for every tool, and keep it in sync when the function signature changes.</p>
<p>One thing worth knowing now, because it determines how well the agent works: the schema is part of the prompt the model sees during generation. Vague parameter descriptions weaken the agent more than people expect. Treat schemas with the same care you’d treat a system prompt.</p>
<p>The schema can also constrain values, but only for constraints you actually put in. If <code>style</code> should only be one of three strings, use an enum:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb3-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Without enum: model can emit any string for "style"</span></span>
<span id="cb3-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"style"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"string"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"description"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"'professional', 'engaging', or 'concise'"</span>}</span>
<span id="cb3-3"></span>
<span id="cb3-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># With enum: the API constrains the model to one of three values</span></span>
<span id="cb3-5"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">"style"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"string"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"enum"</span>: [<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"professional"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"engaging"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"concise"</span>]}</span></code></pre></div></div>
<p>The schema enforces what you specify. Anything beyond that (business rules, length limits, format checks) your code still has to validate after the call.</p>
</section>
<section id="how-tool-calls-actually-come-back" class="level3">
<h3 class="anchored" data-anchor-id="how-tool-calls-actually-come-back">2. How tool calls actually come back</h3>
<p>A persistent confusion: people think the model emits raw <code>&lt;tool_call&gt;</code> JSON tags as plain text, and the API parses that text post-hoc. That’s not what happens.</p>
<p>When tool schemas are passed in the request, providers train the model to emit tool calls as structured output, not text the API has to fish through. The structure is surfaced in the response as a typed field (or list of typed blocks), separate from any text the model produced. An OpenAI response with a tool call looks like this:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode json code-with-copy"><code class="sourceCode json"><span id="cb4-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb4-2">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"role"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"assistant"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb4-3">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"content"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">null</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb4-4">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"tool_calls"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">[</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb4-5">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"id"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"call_001"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb4-6">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"type"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb4-7">    <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"function"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb4-8">      <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"name"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"generate_email"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb4-9">      <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"arguments"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"{</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">style</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">: </span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">professional</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">}"</span></span>
<span id="cb4-10">    <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">}</span></span>
<span id="cb4-11">  <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">}</span><span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">]</span></span>
<span id="cb4-12"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">}</span></span></code></pre></div></div>
<p>The API surfaces this output in a typed <code>tool_calls</code> field, with <code>content: null</code> because the model didn’t generate any text alongside.</p>
</section>
<section id="the-messages-array" class="level3">
<h3 class="anchored" data-anchor-id="the-messages-array">3. The messages array</h3>
<p>A conversation is a list of messages with roles. Four matter:</p>
<ul>
<li><code>system</code>: the instructions you set up the agent with.</li>
<li><code>user</code>: what the user typed.</li>
<li><code>assistant</code>: what the model generated. Either text content, structured tool calls, or both.</li>
<li><code>tool</code>: the result of executing a tool call, tagged with the originating <code>tool_call_id</code>.</li>
</ul>
<p>Starting state for our task:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb5-1">messages <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> [</span>
<span id="cb5-2">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"role"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"system"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"content"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"You are a sales manager. ..."</span>},</span>
<span id="cb5-3">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"role"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"user"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"content"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Send a cold sales email"</span>},</span>
<span id="cb5-4">]</span></code></pre></div></div>
<p>The framework constructs this from your <code>Agent(...)</code> and <code>Runner.run(...)</code> arguments. Without it, you build the array yourself and append to it on every iteration.</p>
<p>In a manual loop, history is just a list you keep appending to. Frameworks can store that list for you between calls, and OpenAI’s Responses API can offload it to the server entirely via <code>previous_response_id</code> or a <code>conversation</code> ID, so your client doesn’t physically resend prior messages. Where the storage lives changes the bill but not the principle: the model’s next step is always conditioned on accumulated history, and that history grows unless you trim, summarize, or externalize it. The rest of this article stays in the manual mode, where the array is unambiguously your code’s job.</p>
</section>
<section id="the-finish-reason" class="level3">
<h3 class="anchored" data-anchor-id="the-finish-reason">4. The finish reason</h3>
<p>When the API returns a response, it includes a <code>finish_reason</code> field telling you why generation stopped. The four values that matter:</p>
<ul>
<li><code>stop</code>: the model finished naturally. Use the response and exit the loop.</li>
<li><code>tool_calls</code>: the model emitted tool calls. Execute them, append results, continue.</li>
<li><code>length</code>: generation hit the token limit. Response is truncated.</li>
<li><code>content_filter</code>: the API’s safety filter intervened.</li>
</ul>
<p><code>finish_reason</code> is the API’s label for what just happened. The model emits tokens; the API examines what was emitted and applies the label. If the output ended with structured tool-call tokens, the API labels it <code>tool_calls</code>. If the budget was hit mid-generation, the API labels it <code>length</code>. Knowing which layer the signal comes from is the first move in debugging.</p>
</section>
<section id="the-loop-itself" class="level3">
<h3 class="anchored" data-anchor-id="the-loop-itself">5. The loop itself</h3>
<p>The loop is short:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb6-1"><span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">while</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">True</span>:</span>
<span id="cb6-2">    response <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> client.chat.completions.create(</span>
<span id="cb6-3">        model<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"gpt-5.4-mini"</span>,</span>
<span id="cb6-4">        messages<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>messages,</span>
<span id="cb6-5">        tools<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>[generate_email_schema, send_email_schema],</span>
<span id="cb6-6">    )</span>
<span id="cb6-7">    msg <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> response.choices[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>].message</span>
<span id="cb6-8">    messages.append(msg)</span>
<span id="cb6-9"></span>
<span id="cb6-10">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> response.choices[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>].finish_reason <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">==</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"stop"</span>:</span>
<span id="cb6-11">        <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> msg.content</span>
<span id="cb6-12"></span>
<span id="cb6-13">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">for</span> call <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">in</span> msg.tool_calls <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">or</span> []:</span>
<span id="cb6-14">        name <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> call.function.name</span>
<span id="cb6-15">        args <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> json.loads(call.function.arguments)</span>
<span id="cb6-16">        result <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> dispatch_tool(name, args)</span>
<span id="cb6-17">        messages.append({</span>
<span id="cb6-18">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"role"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"tool"</span>,</span>
<span id="cb6-19">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"tool_call_id"</span>: call.<span class="bu" style="color: null;
background-color: null;
font-style: inherit;">id</span>,</span>
<span id="cb6-20">            <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"content"</span>: result <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">if</span> <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">isinstance</span>(result, <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">str</span>) <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">else</span> json.dumps(result),</span>
<span id="cb6-21">        })</span></code></pre></div></div>
<p>That is the loop. Frameworks add tracing, retries, sessions, handoffs, max-turn limits, and nicer tool registration on top. The core mechanics are: call model, inspect tool calls, run tools, append results, repeat.</p>
<div id="fig-agentic-loop" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A diagram of one iteration of an agentic loop across three layers. The layers each perform part of the work, and a messages array is shown crossing the boundary between them on every iteration: passed in, added to, and passed back. An annotation notes that in a manual implementation the messages array lives in the application code and is re-sent in full on each loop rather than being held by the framework.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-agentic-loop-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/what-an-agent-actually-is/02-agent-frameworks/agent_loop.png" class="img-fluid figure-img" alt="A diagram of one iteration of an agentic loop across three layers. The layers each perform part of the work, and a messages array is shown crossing the boundary between them on every iteration: passed in, added to, and passed back. An annotation notes that in a manual implementation the messages array lives in the application code and is re-sent in full on each loop rather than being held by the framework.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-agentic-loop-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: One iteration of the agentic loop. Three layers do work; the messages array crosses the boundary between them on every iteration. In a manual implementation, the array lives in your code and re-sends in full each loop.
</figcaption>
</figure>
</div>
</section>
</section>
<section id="tracing-one-run" class="level2">
<h2 class="anchored" data-anchor-id="tracing-one-run">Tracing one run</h2>
<p>Here’s what actually happens when the agent runs the sales-email task.</p>
<p><strong>Loop 1.</strong> The messages array contains just the system instructions and the user query. The model reads its instructions, sees that it has <code>generate_email</code> and <code>send_email</code> available, and emits structured output describing three parallel tool calls, one per style. The API parses those tokens into a <code>tool_calls</code> field and sets <code>finish_reason="tool_calls"</code>. The assistant message looks like this:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb7-1">{</span>
<span id="cb7-2">  <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"role"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"assistant"</span>,</span>
<span id="cb7-3">  <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"content"</span>: null,</span>
<span id="cb7-4">  <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"tool_calls"</span>: [</span>
<span id="cb7-5">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"call_001"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>,</span>
<span id="cb7-6">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"name"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"generate_email"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"arguments"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"{</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">style</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">: </span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">professional</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">}"</span>}},</span>
<span id="cb7-7">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"call_002"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>,</span>
<span id="cb7-8">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"name"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"generate_email"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"arguments"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"{</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">style</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">: </span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">engaging</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">}"</span>}},</span>
<span id="cb7-9">    {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"id"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"call_003"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"type"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>,</span>
<span id="cb7-10">     <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"function"</span>: {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"name"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"generate_email"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"arguments"</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"{</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">style</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">: </span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">concise</span><span class="ch" style="color: #20794D;
background-color: null;
font-style: inherit;">\"</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">}"</span>}}</span>
<span id="cb7-11">  ]</span>
<span id="cb7-12">}</span></code></pre></div></div>
<p>Your code appends that to the messages array, dispatches each tool call to <code>generate_email</code>, captures the results, and appends a <code>tool</code> message for each, tagged with the originating <code>tool_call_id</code>:</p>
<pre><code>{"role": "tool", "tool_call_id": "call_001", "content": "Subject: Streamlining your Q4 operations\n\nDear..."}</code></pre>
<p>The messages array now has six entries: <code>system</code>, <code>user</code>, <code>assistant</code> (with three tool calls), <code>tool</code>, <code>tool</code>, <code>tool</code>.</p>
<p><strong>Loop 2.</strong> Second API call. The model is now seeing the system prompt, the user query, the three tool calls it made, and the three emails the tools produced. It evaluates them, the professional one is generic, the engaging one too casual, the concise one lacks credibility, and emits two new <code>generate_email</code> calls with <code>include_data=true</code> and adjusted lengths. The model didn’t “remember” anything between loops; models are stateless. The continuity is your code’s responsibility, sitting in the messages array that gets re-sent each iteration.</p>
<p><strong>Loop 3.</strong> Your code dispatches the two retries. By now the array contains ten entries and five emails worth of text. Every loop iteration re-sends the entire array, so by Loop 3 you’re paying input tokens on the system prompt, the user query, three full email texts from Loop 1, two more from Loop 2, and the assistant messages tying them together. Server-managed state via <code>previous_response_id</code> avoids the physical re-upload, but the conceptual cost is the same: the model attends to all accumulated history on every step, and the token bill reflects that. This is one of the main reasons production agents trim, summarize, or compress their context as it grows. The framework hides the loop, but the token cost stays yours.</p>
<p><strong>Loops 4-5.</strong> The pattern repeats. The model sees the five emails, picks the strongest, and emits a single <code>send_email</code> call. Your code dispatches it. The next API call has nothing left to do; the model generates a brief confirmation in plain text, no tool calls, and the API sets <code>finish_reason="stop"</code>. Your code returns the assistant message and exits.</p>
<p>Five loops, multiple API round trips, tool executions, one cold sales email sent. The framework’s <code>Runner.run()</code> collapses all of that into one line.</p>
</section>
<section id="provider-shapes-differ-but-the-loop-is-the-same" class="level2">
<h2 class="anchored" data-anchor-id="provider-shapes-differ-but-the-loop-is-the-same">Provider shapes differ, but the loop is the same</h2>
<p>The article so far has used OpenAI. Other providers expose tool calls in different response shapes, but the idea is the same: the model chooses a tool name and arguments, and the API returns that choice in structured fields rather than as ordinary prose.</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb9-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># OpenAI: tool_calls list on the message</span></span>
<span id="cb9-2">message.tool_calls[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>].function.name</span>
<span id="cb9-3">message.tool_calls[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>].function.arguments    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># JSON string</span></span>
<span id="cb9-4"></span>
<span id="cb9-5"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Anthropic: content blocks with type "tool_use"</span></span>
<span id="cb9-6">block <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> message.content[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>]</span>
<span id="cb9-7">block.<span class="bu" style="color: null;
background-color: null;
font-style: inherit;">type</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">==</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"tool_use"</span></span>
<span id="cb9-8">block.name</span>
<span id="cb9-9">block.<span class="bu" style="color: null;
background-color: null;
font-style: inherit;">input</span>                                  <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># dict</span></span>
<span id="cb9-10"></span>
<span id="cb9-11"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Gemini: parts inside the candidate's content</span></span>
<span id="cb9-12">part <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> response.candidates[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>].content.parts[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>]</span>
<span id="cb9-13">part.function_call.name</span>
<span id="cb9-14">part.function_call.args                      <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># dict</span></span></code></pre></div></div>
<p>The field names differ, but your job is the same: read the tool name and arguments, execute the corresponding function, append the result, and call the model again. The three-layer model (model emits, API parses, your code dispatches) holds across all three providers.</p>
</section>
<section id="debug-by-asking-which-layer-failed" class="level2">
<h2 class="anchored" data-anchor-id="debug-by-asking-which-layer-failed">Debug by asking which layer failed</h2>
<p>When something goes wrong, the symptom often surfaces in the framework layer (an exception, a malformed response, a stuck loop), but the cause may be in any of the three. The fastest path back to working is asking which layer did something other than what you expected.</p>
<ul>
<li><strong>Did the model choose the wrong tool or wrong arguments?</strong> That’s a model failure. The framework reports it but doesn’t cause it. Fix: improve the system prompt, sharpen the schema descriptions, add an enum where you assumed strings would be free-form.</li>
<li><strong>Did the API parse and return the tool call correctly?</strong> Almost always yes for tool-call shape, since structured output is constrained at the API boundary. But <code>finish_reason</code> values come from the API and tell you why generation stopped (<code>length</code>, <code>content_filter</code>, etc.), not what the model decided.</li>
<li><strong>Did your code dispatch the tool, append the result, and continue the loop correctly?</strong> This is where most bugs in a manual implementation live, and where <a href="../../../../posts/series/the-agent-harness/05-traces/index.html">most bugs in a framework implementation are hidden behind abstractions you can’t see</a>. Drop down to the API call directly when in doubt; the framework can be a fog over what the model is actually emitting.</li>
</ul>
<p>The model emits, the API parses, your code dispatches and loops. Frameworks are convenience layered over those three steps.</p>


</section>

 ]]></description>
  <category>What an agent actually is</category>
  <guid>https://yeesengchan.com/posts/series/what-an-agent-actually-is/02-agent-frameworks/</guid>
  <pubDate>Tue, 14 Apr 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>What the heck is an AI agent?</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/what-an-agent-actually-is/01-what-is-an-agent/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>What an agent actually is</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-what-is-an-agent/">What the heck is an AI agent?</a></li>
<li><a href="../02-agent-frameworks/">Behind the scenes of AI agent frameworks</a></li>
<li><a href="../03-what-mcp-removes/">What MCP Actually Removes From a Host</a></li>
</ol>
</div>
<p>“Agent” has become the most overloaded word in AI engineering. Vendors apply it to everything from a single tool-use LLM call to systems that run autonomously for days. The definitions in serious use don’t agree with each other, and the disagreement isn’t going to resolve.</p>
<p>Rather than pick a winner, stop asking whether something is an agent. Ask how much autonomy it has, and whether that’s the right amount for the task.</p>
<section id="stop-asking-for-one-definition" class="level2">
<h2 class="anchored" data-anchor-id="stop-asking-for-one-definition">Stop asking for one definition</h2>
<p>The definitions across the field vary, and the variation is real:</p>
<ul>
<li><p><strong>Anthropic</strong> <span class="citation" data-cites="anthropic_2024_agents">(Anthropic 2024)</span> draws a sharp architectural line: <em>workflows</em> are systems where LLMs and tools run through predefined code paths; <em>agents</em> are systems where the LLM dynamically directs its own process and tool use. Under that definition, many production “agents” are better described as workflows.</p></li>
<li><p><strong>Chip Huyen</strong> <span class="citation" data-cites="huyen_2025_agents">(Huyen 2025)</span> uses the classical AI definition: an agent is anything that perceives its environment and acts on it. By that standard, ChatGPT is an agent, and so is a RAG system: its retrievers and SQL executor are the tools through which it perceives and acts on its environment.</p></li>
<li><p><strong>Nathan Lambert</strong> <span class="citation" data-cites="lambert_2024">(Lambert 2024)</span> treats tool-use language models as the starting point of the agent spectrum, with complexity increasing from there.</p></li>
<li><p><strong>Cameron Wolfe</strong> <span class="citation" data-cites="wolfe_2025">(Wolfe 2025)</span> builds the idea from first principles: standard LLMs, tool usage, decomposing problems, reasoning models, and more autonomous systems. He frames agents as lying on a spectrum of capabilities rather than drawing one hard threshold for what counts as a “real” agent.</p></li>
<li><p><strong>McKinsey’s 2025 enterprise survey</strong> <span class="citation" data-cites="mckinsey_2025">(McKinsey &amp; Company 2025)</span> frames AI agents as systems based on foundation models that act in the real world and are capable of autonomously planning and executing multiple steps in a workflow. This is a more autonomy-heavy enterprise framing than Lambert’s tool-use baseline.</p></li>
</ul>
<p>These definitions do not fully agree.</p>
</section>
<section id="the-autonomy-spectrum" class="level2">
<h2 class="anchored" data-anchor-id="the-autonomy-spectrum">The autonomy spectrum</h2>
<p>The cleanest way to understand what an agent is is to start with what it isn’t, and add one capability at a time.</p>
<table class="caption-top table">
<colgroup>
<col style="width: 50%">
<col style="width: 50%">
</colgroup>
<thead>
<tr class="header">
<th>Stage</th>
<th>What it adds</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>Standard LLM</strong></td>
<td>Text in, text out. Access to the world is limited to training data and the prompt.</td>
</tr>
<tr class="even">
<td><strong>+ Tools</strong></td>
<td>The model emits structured calls routed to external systems: search, databases, code execution, APIs.</td>
</tr>
<tr class="odd">
<td><strong>+ Loop</strong></td>
<td>Tool output feeds back into context, and the model decides what to do next. Control flow stops being linear.</td>
</tr>
<tr class="even">
<td><strong>+ Reasoning</strong></td>
<td>The model plans before acting, evaluates partial results, and revises when something fails.</td>
</tr>
<tr class="odd">
<td><strong>+ Autonomy</strong></td>
<td>The model decides when to stop. Open-ended tasks, no predefined path, runs that may last minutes or hours.</td>
</tr>
</tbody>
</table>
<p>A few of these stages deserve more than a row.</p>
<p><strong>+ Tools</strong> is the first place the word “agent” gets applied, and where the definition fight starts. A model that emits one tool call and synthesizes the result is doing more than text generation, but calling it autonomous is a stretch. Wolfe calls this Level 1; Lambert calls it the entry point to the spectrum.</p>
<p><strong>+ Loop</strong> is where “agentic” becomes a defensible label. The model can chain operations: search, read a result, refine the query, search again, synthesize. ReAct <span class="citation" data-cites="yao_etal_2023">(Yao et al. 2023)</span> formalized this early (alternating reasoning and action steps until the model decides it’s done) and it’s still the canonical pattern. The loop is also where <a href="../../../../posts/series/the-agent-harness/01-demos-break/index.html">things fail in interesting ways</a>: the model can loop without converging, hallucinate tool calls, or decide the task is done when it isn’t.</p>
<p><strong>+ Autonomy</strong> is the regime Anthropic reserves the word “agent” for, and it’s where the failure modes get expensive.</p>
<p>Each step adds capability and removes a constraint. Each one is also a deliberate engineering choice. More autonomy is different, not better. A task that would be well served by a fixed workflow doesn’t improve when you add a planning loop, and a task that genuinely needs open-ended exploration isn’t solved by a single tool call.</p>
<p>To make this concrete, take a customer-support refund task. A standard LLM can’t handle it at all. With no access to the order record, the best it can do is draft a generic reply that pretends to. With tools, it can look up the order and draft a real one. With a loop, it can ask for missing information and retry. With reasoning, it can handle edge cases: orders past the policy window, partial refunds, conflicts with prior tickets. With autonomy, it decides whether to issue the refund or escalate.</p>
<div id="fig-agent-spectrum" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A horizontal spectrum diagram for the word agent. The left end is labeled with a single tool call, the simplest case. Moving right, the points increase in autonomy and scope through multi-step tool use and longer task loops. The right end is labeled with systems that run without human input for extended periods. The figure frames agent as this whole range rather than one fixed point on it.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-agent-spectrum-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/what-an-agent-actually-is/01-what-is-an-agent/agent_spectrum.png" class="img-fluid figure-img" alt="A horizontal spectrum diagram for the word agent. The left end is labeled with a single tool call, the simplest case. Moving right, the points increase in autonomy and scope through multi-step tool use and longer task loops. The right end is labeled with systems that run without human input for extended periods. The figure frames agent as this whole range rather than one fixed point on it.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-agent-spectrum-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: "Agent" does not pick out a single artifact; it picks out a range, running from a single tool call to systems that operate without human input for extended periods.
</figcaption>
</figure>
</div>
</section>
<section id="what-agents-are-built-from" class="level2">
<h2 class="anchored" data-anchor-id="what-agents-are-built-from">What agents are built from</h2>
<p>Strip away the marketing and the architecture is consistent across the spectrum. Every agent, from a single tool-use call to a long-running autonomous system, is built from the same primitives:</p>
<ul>
<li><strong>Model.</strong> Decides what to do next.</li>
<li><strong>Tools.</strong> Let the model act on the world.</li>
<li><strong>Loop.</strong> Runs the model repeatedly, feeding outputs back as context.</li>
<li><strong>Memory.</strong> What’s available across loop iterations and across sessions.</li>
<li><strong>Control flow.</strong> Decides which path through the system gets taken; via explicit code, or implicit in the model’s reasoning.</li>
</ul>
<p>The proportions and complexity vary, but the components stay the same.</p>
<p>This is also why agents aren’t a fundamentally new paradigm. A modern agent is a tool-use LLM in a loop with structured control flow, and each component has been around for years: Toolformer <span class="citation" data-cites="schick_etal_2023">(Schick et al. 2023)</span> for tool use, ReAct <span class="citation" data-cites="yao_etal_2023">(Yao et al. 2023)</span> for the reasoning-and-action loop, and the broader control-flow patterns Anthropic documented in late 2024. The novelty is in the models, not the architecture: the underlying language models are finally good enough at instruction-following, structured output, and reasoning that the architecture works in production.</p>
<p>Frameworks like LangChain, LangGraph, and the OpenAI Agents SDK abstract these primitives into higher-level constructs. That’s often useful, as they handle schema generation, message management, tool dispatch, retry logic. But the abstraction has a cost. Anthropic’s recommendation is worth taking seriously: start with direct API calls so you understand the mechanics, then reach for frameworks once the cost of not using one is clear. The <a href="../../../../posts/series/what-an-agent-actually-is/02-agent-frameworks/index.html">next article</a> goes deep and looks at what tool calls look like at the token level, how the loop is implemented, and what frameworks save you versus what they hide.</p>
</section>
<section id="more-autonomy-is-not-always-better" class="level2">
<h2 class="anchored" data-anchor-id="more-autonomy-is-not-always-better">More autonomy is not always better</h2>
<p>If you read AI marketing in 2026, the implicit definition of an agent is the right end of the spectrum: a system that operates without human input, makes decisions, takes actions, plans across long horizons. That’s the picture, but it’s a small slice of what’s actually deployed.</p>
<p>Anthropic’s engineering team has been direct about this. Their observation, after working with dozens of teams building production systems: the most successful implementations weren’t using complex frameworks or specialized libraries. They were building with simple, composable patterns. The systems delivering value in production are mostly workflows with LLM calls in them, not open-ended autonomous agents.</p>
<p>Two systems I’ve built sit at different points on the spectrum. The clinical-documentation pipeline is a workflow: code routes a series of LLM calls that <a href="../../../../posts/series/research-foundations-of-modern-llms/05-information-extraction/index.html">extract problems, labs, and physical-exam findings into note sections</a>. The LLM is the labor; the control flow is fixed, and that’s what makes the system shippable. The deep research agent I built sits further right. Query formulation, how deeply to read each source, and when to stop are all the model’s calls, with a controller-enforced budget on top so the agency stays bounded.</p>
<p>This isn’t a temporary state that resolves when the models get better. It reflects a permanent engineering reality: predictability is valuable. A workflow with fixed control flow is easier to debug, evaluate, and ship. An autonomous agent that decides its own path is harder on every axis. For most production tasks, the cost of that flexibility has to be balanced against its benefit. McKinsey’s 2025 survey found only about 10% of enterprises have agents running in production in even a single department, despite vendor messaging suggesting far wider adoption.</p>
<p>So insisting that only the autonomous end of the spectrum counts as an agent is a category error. It defines the word by the marketing extreme rather than by what the systems actually do. The question that informs design decisions isn’t <em>is this an agent?</em> It’s <em>how much autonomy does this have, and is that the right amount for the task?</em></p>



</section>

<div id="quarto-appendix" class="default"><section class="quarto-appendix-contents" id="quarto-bibliography"><h2 class="anchored quarto-appendix-heading">References</h2><div id="refs" class="references csl-bib-body hanging-indent">
<div id="ref-anthropic_2024_agents" class="csl-entry">
Anthropic. 2024. <em>Building Effective Agents</em>. <a href="https://www.anthropic.com/engineering/building-effective-agents" class="uri">Https://www.anthropic.com/engineering/building-effective-agents</a>.
</div>
<div id="ref-huyen_2025_agents" class="csl-entry">
Huyen, Chip. 2025. <em>Agents</em>. <a href="https://huyenchip.com/2025/01/07/agents.html" class="uri">Https://huyenchip.com/2025/01/07/agents.html</a>.
</div>
<div id="ref-lambert_2024" class="csl-entry">
Lambert, Nathan. 2024. <em>The AI Agent Spectrum</em>. <a href="https://www.interconnects.ai/p/the-ai-agent-spectrum" class="uri">Https://www.interconnects.ai/p/the-ai-agent-spectrum</a>.
</div>
<div id="ref-mckinsey_2025" class="csl-entry">
McKinsey &amp; Company. 2025. <em>The State of AI in 2025: Agents, Innovation, and Transformation</em>.
</div>
<div id="ref-schick_etal_2023" class="csl-entry">
Schick, Timo, Jane Dwivedi-Yu, Roberto Dessì, et al. 2023. <span>“Toolformer: Language Models Can Teach Themselves to Use Tools.”</span> <em>Advances in Neural Information Processing Systems (NeurIPS)</em>. <a href="https://arxiv.org/abs/2302.04761">https://arxiv.org/abs/2302.04761</a>.
</div>
<div id="ref-wolfe_2025" class="csl-entry">
Wolfe, Cameron R. 2025. <em>AI Agents from First Principles</em>. <a href="https://cameronrwolfe.substack.com/p/ai-agents" class="uri">Https://cameronrwolfe.substack.com/p/ai-agents</a>.
</div>
<div id="ref-yao_etal_2023" class="csl-entry">
Yao, Shunyu, Jeffrey Zhao, Dian Yu, et al. 2023. <span>“ReAct: Synergizing Reasoning and Acting in Language Models.”</span> <em>International Conference on Learning Representations (ICLR)</em>. <a href="https://arxiv.org/abs/2210.03629">https://arxiv.org/abs/2210.03629</a>.
</div>
</div></section></div> ]]></description>
  <category>What an agent actually is</category>
  <guid>https://yeesengchan.com/posts/series/what-an-agent-actually-is/01-what-is-an-agent/</guid>
  <pubDate>Sat, 11 Apr 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Information extraction didn’t disappear. It moved inside the workflow.</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/05-information-extraction/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>Research foundations of modern LLMs</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-pretraining-objectives/">Pretraining objectives: Why decoder-only won</a></li>
<li><a href="../02-encoder-embeddings/">The encoder didn’t die. It became the embedding model</a></li>
<li><a href="../03-retrieval/">Retrieval is older than RAG: From DPR to end-to-end</a></li>
<li><a href="../04-fine-tuning-stack/">The fine-tuning stack: One loss, different data</a></li>
<li><a href="../05-information-extraction/">Information extraction didn’t disappear. It moved inside the workflow</a></li>
</ol>
</div>
<p>For thirty years, information extraction was a real subfield of NLP. Named entity recognition, relation extraction, event extraction, coreference resolution, slot filling, knowledge-base population. Annotated corpora: ACE, OntoNotes, KBP, TAC. A shared-task culture at every major conference. An entire industry of vendors and government-funded R&amp;D organizations whose product was, essentially, structured records pulled out of unstructured text.</p>
<p>By 2024, the center of gravity had shifted.</p>
<p>The standard story is that LLMs replaced IE. What actually happened is that IE moved inside the workflow. Where the pre-LLM IE pipeline was the <em>product</em> (extract entities, link them, populate a knowledge base, ship the KB), modern IE is the <em>structured layer</em> between raw text and downstream generation or action.</p>
<p>The work still exists. What changed is the labor model. Pre-LLM IE required labeling thousands of examples per task, engineering features, training task-specific classifiers, and tuning a pipeline. LLM-era IE requires designing a JSON schema, writing a prompt, and evaluating the result. Both require real engineering of different kinds.</p>
<p>I worked across all three eras the article traces: pre-BERT IE during a postdoc, BERT-era production IE through the late 2010s, and LLM-era workflow IE now.</p>
<div id="fig-three-eras" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Three stacked panels for three eras of information extraction. The pre-BERT and BERT panels show the same left-to-right pipeline of identical boxes, with only the contents of each box changing between them, hand-built features in the first and learned BERT representations in the second. The LLM panel replaces the linear pipeline with a workflow: several extraction calls run in parallel and emit structured frames that flow into a downstream system. A side label contrasts the labor profile, shifting from data annotation and feature engineering in the earlier eras to schema design, prompt engineering, and evaluation in the LLM era.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-three-eras-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/05-information-extraction/three_eras.png" class="img-fluid figure-img" alt="Three stacked panels for three eras of information extraction. The pre-BERT and BERT panels show the same left-to-right pipeline of identical boxes, with only the contents of each box changing between them, hand-built features in the first and learned BERT representations in the second. The LLM panel replaces the linear pipeline with a workflow: several extraction calls run in parallel and emit structured frames that flow into a downstream system. A side label contrasts the labor profile, shifting from data annotation and feature engineering in the earlier eras to schema design, prompt engineering, and evaluation in the LLM era.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-three-eras-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: The three eras of information extraction. The pipeline shape is unchanged from pre-BERT to BERT era; only the components inside each box change. The LLM era replaces the linear pipeline with a workflow: parallel extraction calls feed structured frames into a downstream system, and the labor profile shifts from annotation and feature engineering to schema design, prompt engineering, and evaluation.
</figcaption>
</figure>
</div>
<section id="what-ie-used-to-look-like" class="level2">
<h2 class="anchored" data-anchor-id="what-ie-used-to-look-like">What IE used to look like</h2>
<p>The canonical IE pipeline went something like this:</p>
<ol type="1">
<li><strong>NER</strong>: tag spans of text with entity types (PER, ORG, GPE, LOC).</li>
<li><strong>Coreference</strong>: cluster mentions that refer to the same entity.</li>
<li><strong>Relation extraction</strong>: for each pair of entities, classify the relation between them, if any.</li>
<li><strong>Event extraction</strong>: identify event triggers, classify the event type, then identify the arguments playing each role.</li>
<li><strong>Knowledge-base population</strong>: combine the above into a structured record per entity, linked across documents.</li>
</ol>
<p>Each of these was its own subfield. Each had its own datasets: CoNLL-2003 <span class="citation" data-cites="sang_demeulder_2003">(Tjong Kim Sang and Meulder 2003)</span> for NER, ACE-2005 <span class="citation" data-cites="walker_etal_2006">(Walker et al. 2006)</span> for events and relations, OntoNotes <span class="citation" data-cites="hovy_etal_2006">(Hovy et al. 2006)</span> for coreference, the TAC KBP datasets <span class="citation" data-cites="ji_grishman_2011">(Ji and Grishman 2011)</span> for end-to-end knowledge-base construction. Each had its own modeling tradition: HMMs and CRFs for sequence labeling, structured-prediction for relation classification, ILP-based joint inference for combining extractions.</p>
<p>The labor was substantial and concentrated on the input side. To build an NER system for a new entity type, you collected text, annotated mentions (typically thousands per type), engineered features (word identity, POS, gazetteer match, dependency context), and trained a classifier. Relation extraction added another annotation pass per relation type. Event extraction added another for triggers and another for each argument role.</p>
<p>I worked in this regime during my postdoc. The published work from that period is on relation extraction <span class="citation" data-cites="chan_roth_2010 chan_roth_2011">(Chan and Roth 2010, 2011)</span> and minimally-supervised event causality <span class="citation" data-cites="do_etal_2011">(Do et al. 2011)</span>. The relation-extraction setup was typical for the era: a feature engineering pass over syntactic and semantic structures (constituency parses, dependency paths, semantic roles, Wikipedia categories as background knowledge), then a structured classifier.</p>
<p>The defining property of pre-BERT IE was the labor model, not any particular architecture. Every new task, every new domain, every new ontology required a fresh labeling effort, a fresh feature-engineering pass, and a fresh trained model. Performance was decent on benchmarks and worse in domain transfer. Adapting to a new domain typically meant starting over.</p>
</section>
<section id="bert-made-ie-better-not-cheaper" class="level2">
<h2 class="anchored" data-anchor-id="bert-made-ie-better-not-cheaper">BERT made IE better, not cheaper</h2>
<p>BERT <span class="citation" data-cites="devlin_etal_2019">(Devlin et al. 2019)</span> changed the modeling but not the labor. The pipeline shape stayed identical to the pre-BERT pipeline; the component models got better:</p>
<ul>
<li><strong>NER</strong> became encoder + token classifier with BIO tags (what Hugging Face exposes as <code>AutoModelForTokenClassification</code>).</li>
<li><strong>Relation extraction</strong> became encoder + span-pair classifier: encode the sentence, pull out the two entity span representations, pool them, run through an MLP head.</li>
<li><strong>Event extraction</strong> became encoder + token classifier for triggers, plus a span-pair-style classifier for arguments.</li>
<li><strong>Coreference</strong> became span scoring and clustering over contextualized representations.</li>
</ul>
<p>The improvements were real. The transition from feature engineering to learned representations was, in retrospect, the largest single quality jump IE had seen in a decade.</p>
<p>But the labor model was unchanged. You still needed labeled data per task. You still needed an annotation effort per new domain. The encoder did the feature engineering for you, but the rest of the work (schema design, annotation, evaluation) was the same as before.</p>
<p>I spent most of the BERT era on this kind of production IE: event extraction <span class="citation" data-cites="chan_etal_2019">(Chan et al. 2019)</span>, KBP-style end-to-end systems <span class="citation" data-cites="deyoung_etal_2017">(DeYoung et al. 2017)</span>, domain-specific machine reading and few-shot event mention retrieval <span class="citation" data-cites="min_etal_2019 min_etal_2020">(Min et al. 2019, 2020)</span>. I designed and built the deep-learning IE platform these systems ran on, layered on the Hugging Face ecosystem and used across government-funded and industry projects.</p>
<p>One <strong>multilingual event extraction</strong> project captures how sophisticated BERT-era IE had become. The training data was English, the inference target was Arabic, and the ontology covered roughly forty event types with typed argument roles. The stack used XLM-R <span class="citation" data-cites="conneau_etal_2020">(Conneau et al. 2020)</span>, multi-stage fine-tuning, token classification, continued MLM pretraining on mixed English-Arabic text, and automated checkpoint selection. This was not “just put a classifier on BERT.”</p>
<p>A second project, <strong>rapid customization of event extraction</strong> for new ontologies <span class="citation" data-cites="chan_etal_2019">(Chan et al. 2019)</span>, was already gesturing at the LLM-era interface. The setup: take a small set of event-type definitions and a handful of seed examples, produce a working extractor without a full annotation effort. We compressed the labeling step with bootstrapping; the modern version puts definitions and examples into a prompt instead. <em>What changed is that the definitions-plus-examples now condition a frontier LLM directly rather than seeding a bootstrapping loop.</em></p>
<p>The point of dwelling on this isn’t nostalgia. Too many current writeups treat the BERT era as a stepping stone: “people used to fine-tune classifiers, then LLMs came along.” That undersells the engineering. The systems were good. They were just expensive to build per task, which is exactly the thing the next era changed.</p>
</section>
<section id="llms-changed-the-labor-model" class="level2">
<h2 class="anchored" data-anchor-id="llms-changed-the-labor-model">LLMs changed the labor model</h2>
<p>IE was already possible before LLMs. What they changed was the cost structure.</p>
<p>A frontier LLM with structured-output support can solve most NER, RE, and slot-filling tasks zero-shot or few-shot from a prompt and a JSON schema. Quality is task-dependent:</p>
<ul>
<li>On standard benchmarks like CoNLL-2003, a well-prompted frontier LLM lands within a few F1 points of fine-tuned baselines.</li>
<li>On harder benchmarks like ACE 2005 event extraction, zero-shot LLMs still trail fine-tuned specialists by double-digit margins <span class="citation" data-cites="zhang_etal_2025">(Zhang et al. 2025)</span>.</li>
</ul>
<p>The gap closes with a handful of in-context examples or light fine-tuning on synthetic data. But for most use cases the marginal value of building a specialized BERT-era IE pipeline collapsed, because the engineering cost of getting from “no extractor” to “working extractor” dropped by an order of magnitude even when the LLM isn’t strictly best on F1.</p>
<p>The mechanics are by now well-known. The prompt asks for structured output. The schema is enforced by the provider’s structured-output mode (OpenAI’s <code>response_format</code>), or by Pydantic schemas as a type-checked output contract. For NER, the schema is a list of typed entities. For RE, a list of (head, relation, tail) triples. For event extraction, a list of frames with event type and roles. The LLM call replaces the entire encoder-plus-classifier-head stack.</p>
</section>
<section id="three-modern-ie-patterns" class="level2">
<h2 class="anchored" data-anchor-id="three-modern-ie-patterns">Three modern IE patterns</h2>
<div id="fig-ie-patterns" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Three panels. Direct extraction: Document goes to an LLM call producing JSON, then structured output; the LLM is in the serving path; simple and latency-tolerant. LLM-built specialist: a build-time-only LLM produces training data which trains a specialist that serves production; the LLM is offline, the small model serves, cheap, fast, and auditable at scale; example STIX relation extraction with GPT generating data for DeBERTa and T5. IE as workflow state: raw text goes through N parallel extraction calls to frames plus metadata that feed a downstream system; extraction is not the product, it feeds a downstream system; example transcript to frames to clinical note. Footer: what differs is where the LLM sits and whether extraction is the product or the scaffolding.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-ie-patterns-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/05-information-extraction/ie_three_patterns.png" class="img-fluid figure-img" alt="Three panels. Direct extraction: Document goes to an LLM call producing JSON, then structured output; the LLM is in the serving path; simple and latency-tolerant. LLM-built specialist: a build-time-only LLM produces training data which trains a specialist that serves production; the LLM is offline, the small model serves, cheap, fast, and auditable at scale; example STIX relation extraction with GPT generating data for DeBERTa and T5. IE as workflow state: raw text goes through N parallel extraction calls to frames plus metadata that feed a downstream system; extraction is not the product, it feeds a downstream system; example transcript to frames to clinical note. Footer: what differs is where the LLM sits and whether extraction is the product or the scaffolding.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-ie-patterns-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: Three modern IE patterns. They differ in where the LLM sits and what ships: Direct extraction calls the LLM in the serving path and uses its output as-is; the LLM-built specialist uses the LLM offline only to generate training data, then serves a small specialist; IE as workflow state runs parallel extraction calls whose metadata-rich frames feed a downstream product, so extraction is the structured layer rather than the deliverable.
</figcaption>
</figure>
</div>
<section id="direct-llm-extraction" class="level3">
<h3 class="anchored" data-anchor-id="direct-llm-extraction">Direct LLM extraction</h3>
<p>The baseline pattern: the production system makes an LLM call per document or chunk, parses the structured output, and uses it directly. It works for moderate-volume, latency-tolerant settings, and it’s the reference point the other two patterns are defined against.</p>
</section>
<section id="llm-for-data-specialist-for-serving" class="level3">
<h3 class="anchored" data-anchor-id="llm-for-data-specialist-for-serving">LLM for data, specialist for serving</h3>
<p>The hybrid pattern uses an <strong>LLM to generate training data</strong>, then trains a smaller specialist for production serving.</p>
<p>I ran into this in relation extraction over the MITRE STIX cyber security ontology (threat-actor, attack-pattern, malware, target, etc.). The available pre-LLM datasets were too sparse, incompletely annotated, or didn’t match STIX cleanly. The path forward: use GPT to extract candidate entities and relations from a few hundred documents. These LLM-generated annotations became training data for a DeBERTa-v3 <span class="citation" data-cites="he_etal_2021">(He et al. 2021)</span> span-pair classifier (relations) and a T5 <span class="citation" data-cites="raffel_etal_2020">(Raffel et al. 2020)</span> seq2seq tagger (NER, chosen because entity spans overlapped). The serving system never called GPT. It used the smaller specialists, trained on GPT-generated data.</p>
<p>The labor shift is the whole point. The classical version would have started with months of annotation. The LLM-era version started with prompt and schema design. The production model was still a classical encoder; the annotation cost collapsed.</p>
</section>
<section id="ie-as-workflow-state" class="level3">
<h3 class="anchored" data-anchor-id="ie-as-workflow-state">IE as workflow state</h3>
<p>The other interesting pattern is when extraction stops being the product. The product is a downstream system that uses <strong>extraction as its structured intermediate representation</strong>.</p>
<p>I’ve designed and led a clinical-documentation pipeline that takes a clinician-patient encounter transcript and produces a structured clinical note. The system uses multiple specialized LLM extraction calls: separate calls for problems and major note sections. Each extraction returns structured frames, with metadata attached so a downstream system can route facts into the right place:</p>
<ul>
<li><strong>Example clinical note sections covered.</strong> History of Present Illness (HPI), Assessment &amp; Plan (A&amp;P), Labs / Test Results, Physical Exam, Allergies, Past Medical History.</li>
<li><strong>Frame fields, for a clinical problem.</strong> Symptoms, severity, duration, status, associated findings, treatments, medications, plan items.</li>
<li><strong>Metadata on each extracted item.</strong> Temporal status (past / present / future), source and stance (patient-reported vs clinician-assessed), section affinity.</li>
<li><strong>Downstream routing.</strong> Present-tense problem details go to HPI; clinician assessment and treatment plans go to A&amp;P; past resolved history goes to medical history.</li>
</ul>
<p>This is the modern form of IE I see most often: a structured control layer that lets the rest of the workflow behave reliably. The product is the clinical note; the extractions are what the downstream system builds it from.</p>
<p>The pattern isn’t unique. SpecialtyScribe <span class="citation" data-cites="goyal_etal_2025">(Goyal et al. 2025)</span> and GENIE <span class="citation" data-cites="ying_etal_2025">(Ying et al. 2025)</span> are published instances of the same idea: IE as the structured intermediate layer of a workflow, with the extraction done by a frontier LLM, a fine-tuned smaller specialist, or a mix.</p>
<p>GraphRAG <span class="citation" data-cites="edge_etal_2024">(Edge et al. 2024)</span> is another version of the same move: use IE to turn documents into entities, relations, and summaries, then run retrieval and reasoning over that structure rather than over raw chunks. These are entity extraction, relation extraction, and light coreference, with the graph as the structured layer they feed.</p>
</section>
</section>
<section id="the-new-labor-schema-prompts-eval-orchestration" class="level2">
<h2 class="anchored" data-anchor-id="the-new-labor-schema-prompts-eval-orchestration">The new labor: schema, prompts, eval, orchestration</h2>
<p>The labor profile is different from BERT-era IE in specific ways. There is no large annotation effort for training. The center of the work is schema design, and the contrast is concrete. A weak schema for clinical problems is <code>{"problems": ["chest pain"]}</code>, a flat list of strings. A schema that lets the downstream system route, deduplicate, and surface evidence for clinician review looks more like:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode json code-with-copy"><code class="sourceCode json"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">{</span></span>
<span id="cb1-2">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"problem"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"chest pain"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb1-3">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"status"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"active | resolved | historical"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb1-4">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"temporality"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"past | present | future_planned"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb1-5">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"source"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"patient_reported | clinician_assessed"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb1-6">  <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">"evidence_span"</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">:</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"..."</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">,</span></span>
<span id="cb1-7"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">}</span></span></code></pre></div></div>
<p>The richer schema is what makes the workflow possible: every extracted fact carries the metadata the downstream system needs to act on it without re-extracting. Around that, the new labor is:</p>
<ul>
<li><strong>Schema design</strong>: the frames, fields, metadata, and uncertainty behavior. The architecture decision that determines what the downstream system can do.</li>
<li><strong>Prompt design</strong>: per-extraction prompts that reliably fill the schema, iterated against failure cases.</li>
<li><strong>Evaluation</strong>: still labeled data, but at much smaller scale, for measuring rather than training.</li>
<li><strong>Workflow orchestration</strong>: how extractions compose, how frames are routed, where metadata gates decisions.</li>
<li><strong>Cost control</strong>: which calls run on a frontier model, which on a smaller specialist, which can be batched or cached.</li>
</ul>
<p>None of this is less work than before. It moved from model training into workflow design.</p>
</section>
<section id="when-classical-ie-still-wins" class="level2">
<h2 class="anchored" data-anchor-id="when-classical-ie-still-wins">When classical IE still wins</h2>
<p>The “LLMs absorbed IE” framing oversells. There are concrete situations where classical IE wins.</p>
<p><strong>High-volume or latency-sensitive serving.</strong> A DeBERTa-v3-base NER system runs at thousands of tokens per second per GPU at near-zero marginal cost; a frontier LLM call costs cents per document at a fraction of that throughput. If you process millions of documents per day, or a pipeline step has to run in tens of milliseconds, the encoder wins on cost and latency by an order of magnitude. The hybrid pattern (LLM-generated data, classical model serves) is the standard answer here.</p>
<p><strong>Fine-grained span tasks under audit constraint.</strong> Medical coding, legal entity extraction, financial reporting: the span boundaries matter exactly, and LLM hallucination is unacceptable. A trained span-classifier with auditable failure modes is the safer choice. The LLM may generate the training data; the production extractor stays classical.</p>
<p><strong>Ontology-backed coding and normalization.</strong> ICD-10 coding is not just extraction. It requires mapping clinical evidence to a controlled code system, following rules around specificity, exclusions and so on. A standalone LLM should not be trusted to do this from parametric memory alone. Use retrieval or lookup against the official code set and deterministic validation.</p>
<p><strong>Stable schemas with mature labeled data.</strong> With a CoNLL-2003-scale labeled dataset and a stable schema, a fine-tuned encoder is hard to beat on the headline metric. The crossover varies by task and prompting strategy, but with thousands of labeled examples the encoder typically pulls ahead.</p>
</section>
<section id="code-companion-tnlp-ie-examples" class="level2">
<h2 class="anchored" data-anchor-id="code-companion-tnlp-ie-examples">Code companion: TNLP IE examples</h2>
<p>To make the transition concrete, my <a href="https://github.com/chanys/tnlp">TNLP repo</a> includes three working IE examples, written in late 2023 and early 2024, that map onto three stages of the shift.</p>
<ul>
<li><p><strong>Token classification / NER (BERT-era classical).</strong> <code>microsoft/deberta-v3-base</code> with <code>AutoModelForTokenClassification</code> on CoNLL-2003, predicting the four canonical entity types. The standard “encoder + BIO tagger” pattern that became the default after BERT. <a href="https://github.com/chanys/tnlp/blob/main/src/config/token_model/conll2003.train.yaml">Config</a>, <a href="https://github.com/chanys/tnlp/blob/main/src/scripts/run_model.py">run script</a>.</p></li>
<li><p><strong>Span-pair classification / relation extraction (BERT-era).</strong> A custom implementation, because span-pair classification has no clean off-the-shelf Hugging Face head when the code was written: encode the sentence with DeBERTa-v3, pool the two span representations, classify the relation. This is what production BERT-era RE actually looked like, a custom span aggregation on top of an encoder. <a href="https://github.com/chanys/tnlp/blob/main/src/model/spanpair_model/custom_spanpair_model.py">Model</a>, <a href="https://github.com/chanys/tnlp/blob/main/src/model/spanpair_model/custom_spanpair_classification.py">training</a>.</p></li>
<li><p><strong>Seq2seq NER with FLAN-T5 (the bridge to LLM-era).</strong> Instead of BIO tags, the model generates the sentence with bracket-tagged entity spans. <code>google/flan-t5-base</code> with LoRA <span class="citation" data-cites="hu_etal_2022">(Hu et al. 2022)</span> and 8-bit quantization. This handles overlapping and nested spans naturally (token classification can’t easily produce nested entities) and is structurally identical to how LLM-era extraction works: a generative model emits structured output. <a href="https://github.com/chanys/tnlp/blob/main/src/model/seq2seq_model/seq2seq_utils.py">Code</a>, <a href="https://github.com/chanys/tnlp/blob/main/src/config/seq2seq_model/ner.train.yaml">config</a>.</p></li>
</ul>
</section>
<section id="what-people-get-wrong" class="level2">
<h2 class="anchored" data-anchor-id="what-people-get-wrong">What people get wrong</h2>
<ol type="1">
<li><p><strong>Treating IE as deprecated.</strong> It isn’t. It’s everywhere: inside agents, RAG and knowledge graphs, structured data extraction, and so on. It’s just not explicitly labeled “IE”.</p></li>
<li><p><strong>Skipping schema design.</strong> The schema is the architecture of the extraction layer. The single highest-leverage improvement to most LLM-extraction setups is tightening it.</p></li>
<li><p><strong>Underweighting evaluation.</strong> Labeled data didn’t go away. It moved from training to eval. <a href="../../../../posts/series/llm-evaluation-honestly/01-stop-vibe-checking/index.html">You can’t ship an IE workflow you haven’t measured</a>, and measuring requires gold annotations. The eval set is the new annotation effort: smaller, but irreducible.</p></li>
<li><p><strong>Picking the wrong model for the operating constraints.</strong> Try the LLM first for new or ambiguous tasks. Train a specialist when cost, latency, scale, auditability, or span precision force it. Both reflexes (always train a specialist; always reach for the frontier LLM) are mistakes. The constraints decide, not the fashion.</p></li>
<li><p><strong>Treating extraction as the product.</strong> In modern systems extraction is rarely the product. It’s the structured intermediate layer between raw text and downstream generation, retrieval, or action.</p></li>
</ol>
</section>
<section id="closing" class="level2">
<h2 class="anchored" data-anchor-id="closing">Closing</h2>
<p>For decades, information extraction was a product: annotate, train, extract, ship the knowledge base. BERT made the models better but left the labor model intact; every new domain still cost an annotation effort. LLMs did not make IE possible; it already was. They made it low-startup-cost enough that it stopped being a product and became infrastructure: the structured layer between raw text and whatever the system does next.</p>
<p>The extractor now serves as workflow state rather than the final deliverable.</p>
<p>The papers on the hard research problems (coreference, cross-document linking, strict argument-role constraints) are still being written; they’re just no longer where most production IE work happens. The next time someone says their system “doesn’t do information extraction,” it’s worth asking what the JSON between their LLM calls is.</p>



</section>

<div id="quarto-appendix" class="default"><section class="quarto-appendix-contents" id="quarto-bibliography"><h2 class="anchored quarto-appendix-heading">References</h2><div id="refs" class="references csl-bib-body hanging-indent">
<div id="ref-chan_etal_2019" class="csl-entry">
Chan, Yee Seng, Joshua Fasching, Haoling Qiu, and Bonan Min. 2019. <span>“Rapid Customization for Event Extraction.”</span> <em>Proceedings of the 57th Annual Meeting of the Association for Computational Linguistics: System Demonstrations</em>.
</div>
<div id="ref-chan_roth_2010" class="csl-entry">
Chan, Yee Seng, and Dan Roth. 2010. <span>“Exploiting Background Knowledge for Relation Extraction.”</span> <em>Proceedings of the 23rd International Conference on Computational Linguistics (COLING)</em>.
</div>
<div id="ref-chan_roth_2011" class="csl-entry">
Chan, Yee Seng, and Dan Roth. 2011. <span>“Exploiting Syntactico-Semantic Structures for Relation Extraction.”</span> <em>Proceedings of the 49th Annual Meeting of the Association for Computational Linguistics: Human Language Technologies</em>.
</div>
<div id="ref-conneau_etal_2020" class="csl-entry">
Conneau, Alexis, Kartikay Khandelwal, Naman Goyal, et al. 2020. <span>“Unsupervised Cross-Lingual Representation Learning at Scale.”</span> <em>Proceedings of the 58th Annual Meeting of the Association for Computational Linguistics</em>. <a href="https://arxiv.org/abs/1911.02116">https://arxiv.org/abs/1911.02116</a>.
</div>
<div id="ref-devlin_etal_2019" class="csl-entry">
Devlin, Jacob, Ming-Wei Chang, Kenton Lee, and Kristina Toutanova. 2019. <span>“BERT: Pre-Training of Deep Bidirectional Transformers for Language Understanding.”</span> <em>Proceedings of the 2019 Conference of the North American Chapter of the Association for Computational Linguistics: Human Language Technologies (NAACL-HLT)</em>. <a href="https://arxiv.org/abs/1810.04805">https://arxiv.org/abs/1810.04805</a>.
</div>
<div id="ref-deyoung_etal_2017" class="csl-entry">
DeYoung, Jay, Yee Seng Chan, Chester Pittapally, Hannah Provenza, Ryan Gabbard, and Marjorie Freedman. 2017. <span>“BBN’s 2017 KBP EAL Submission.”</span> <em>Proceedings of the 2017 Text Analysis Conference (TAC)</em>.
</div>
<div id="ref-do_etal_2011" class="csl-entry">
Do, Quang, Yee Seng Chan, and Dan Roth. 2011. <span>“Minimally Supervised Event Causality Identification.”</span> <em>Proceedings of the 2011 Conference on Empirical Methods in Natural Language Processing</em>.
</div>
<div id="ref-edge_etal_2024" class="csl-entry">
Edge, Darren, Ha Trinh, Newman Cheng, et al. 2024. <span>“From Local to Global: A Graph RAG Approach to Query-Focused Summarization.”</span> <em>arXiv Preprint</em>. <a href="https://arxiv.org/abs/2404.16130">https://arxiv.org/abs/2404.16130</a>.
</div>
<div id="ref-goyal_etal_2025" class="csl-entry">
Goyal, Sagar, Eti Rastogi, Fen Zhao, Dong Yuan, and Andrew Beinstein. 2025. <span>“SpecialtyScribe: Enhancing SOAP Note Scribing for Medical Specialties Using LLMs.”</span> <em>Proceedings of the Second Workshop on Patient-Oriented Language Processing (CL4Health)</em>.
</div>
<div id="ref-he_etal_2021" class="csl-entry">
He, Pengcheng, Xiaodong Liu, Jianfeng Gao, and Weizhu Chen. 2021. <span>“DeBERTaV3: Improving DeBERTa Using ELECTRA-Style Pre-Training with Gradient-Disentangled Embedding Sharing.”</span> <em>arXiv Preprint</em>. <a href="https://arxiv.org/abs/2111.09543">https://arxiv.org/abs/2111.09543</a>.
</div>
<div id="ref-hovy_etal_2006" class="csl-entry">
Hovy, Eduard, Mitchell Marcus, Martha Palmer, Lance Ramshaw, and Ralph Weischedel. 2006. <span>“OntoNotes: The 90% Solution.”</span> <em>Proceedings of the Human Language Technology Conference of the NAACL, Companion Volume: Short Papers</em>.
</div>
<div id="ref-hu_etal_2022" class="csl-entry">
Hu, Edward J., Yelong Shen, Phillip Wallis, et al. 2022. <span>“LoRA: Low-Rank Adaptation of Large Language Models.”</span> <em>International Conference on Learning Representations</em>. <a href="https://arxiv.org/abs/2106.09685">https://arxiv.org/abs/2106.09685</a>.
</div>
<div id="ref-ji_grishman_2011" class="csl-entry">
Ji, Heng, and Ralph Grishman. 2011. <span>“Knowledge Base Population: Successful Approaches and Challenges.”</span> <em>Proceedings of the 49th Annual Meeting of the Association for Computational Linguistics: Human Language Technologies</em>.
</div>
<div id="ref-min_etal_2019" class="csl-entry">
Min, Bonan, Yee Seng Chan, Haoling Qiu, and Joshua Fasching. 2019. <span>“Towards Machine Reading for Interventions from Humanitarian-Assistance Program Literature.”</span> <em>Proceedings of the 2019 Conference on Empirical Methods in Natural Language Processing and the 9th International Joint Conference on Natural Language Processing (EMNLP-IJCNLP)</em>.
</div>
<div id="ref-min_etal_2020" class="csl-entry">
Min, Bonan, Yee Seng Chan, and Lingjun Zhao. 2020. <span>“Towards Few-Shot Event Mention Retrieval: An Evaluation Framework and a Siamese Network Approach.”</span> <em>Proceedings of the Twelfth Language Resources and Evaluation Conference (LREC)</em>.
</div>
<div id="ref-raffel_etal_2020" class="csl-entry">
Raffel, Colin, Noam Shazeer, Adam Roberts, et al. 2020. <span>“Exploring the Limits of Transfer Learning with a Unified Text-to-Text Transformer.”</span> <em>Journal of Machine Learning Research</em>. <a href="https://arxiv.org/abs/1910.10683">https://arxiv.org/abs/1910.10683</a>.
</div>
<div id="ref-sang_demeulder_2003" class="csl-entry">
Tjong Kim Sang, Erik F., and Fien De Meulder. 2003. <span>“Introduction to the CoNLL-2003 Shared Task: Language-Independent Named Entity Recognition.”</span> <em>Proceedings of the Seventh Conference on Natural Language Learning at HLT-NAACL 2003 (CoNLL)</em>.
</div>
<div id="ref-walker_etal_2006" class="csl-entry">
Walker, Christopher, Stephanie Strassel, Julie Medero, and Kazuaki Maeda. 2006. <em>ACE 2005 Multilingual Training Corpus</em>. Linguistic Data Consortium, LDC2006T06.
</div>
<div id="ref-ying_etal_2025" class="csl-entry">
Ying, Huaiyuan, Hongyi Yuan, Jinsen Lu, et al. 2025. <span>“GENIE: Generative Note Information Extraction Model for Structuring EHR Data.”</span> <em>arXiv Preprint</em>. <a href="https://arxiv.org/abs/2501.18435">https://arxiv.org/abs/2501.18435</a>.
</div>
<div id="ref-zhang_etal_2025" class="csl-entry">
Zhang, Zikun, Wei You, Tongtao Wu, Xiaolong Wang, Jianxin Li, and Min Zhang. 2025. <span>“A Survey of Generative Information Extraction.”</span> <em>Proceedings of the 31st International Conference on Computational Linguistics (COLING)</em>.
</div>
</div></section></div> ]]></description>
  <category>Research foundations of modern LLMs</category>
  <guid>https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/05-information-extraction/</guid>
  <pubDate>Sun, 05 Apr 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>The fine-tuning stack: one loss, different data</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/04-fine-tuning-stack/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>Research foundations of modern LLMs</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-pretraining-objectives/">Pretraining objectives: Why decoder-only won</a></li>
<li><a href="../02-encoder-embeddings/">The encoder didn’t die. It became the embedding model</a></li>
<li><a href="../03-retrieval/">Retrieval is older than RAG: From DPR to end-to-end</a></li>
<li><a href="../04-fine-tuning-stack/">The fine-tuning stack: One loss, different data</a></li>
<li><a href="../05-information-extraction/">Information extraction didn’t disappear. It moved inside the workflow</a></li>
</ol>
</div>
<p>The standard story about post-pretraining is: first supervised fine-tuning (often called instruction tuning), then alignment with RLHF or DPO. Two stages, presented as several distinct techniques because the SFT stage has gone by many names: supervised fine-tuning, instruction tuning, distilled SFT, chat tuning. Mechanically, they’re the same operation.</p>
<p>SFT shares its loss function with pretraining. They’re both next-token cross-entropy, differing only in what data goes in and which tokens contribute to the loss. Preference optimization is the only stage that introduces a genuinely new loss family.</p>
<section id="two-loss-families-across-three-stages" class="level2">
<h2 class="anchored" data-anchor-id="two-loss-families-across-three-stages">Two loss families across three stages</h2>
<div id="fig-three-jobs" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A diagram of three training stages grouped by loss family. Pretraining and supervised fine-tuning are bracketed together as one group because both use next-token cross-entropy; the diagram contrasts them only by their input data and by which tokens contribute to the loss. Preference optimization sits in a separate group, labeled as the only stage that introduces a different loss family.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-three-jobs-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/04-fine-tuning-stack/three_jobs.png" class="img-fluid figure-img" alt="A diagram of three training stages grouped by loss family. Pretraining and supervised fine-tuning are bracketed together as one group because both use next-token cross-entropy; the diagram contrasts them only by their input data and by which tokens contribute to the loss. Preference optimization sits in a separate group, labeled as the only stage that introduces a different loss family.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-three-jobs-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: The three stages, grouped by loss family. Pretraining and SFT share next-token cross-entropy: they differ only in what data goes in and which tokens contribute to the loss. Preference optimization is the only stage that introduces a new loss family.
</figcaption>
</figure>
</div>
<p>Pretraining and SFT use the same loss: next-token cross-entropy. They differ in:</p>
<ul>
<li><strong>What data the model sees.</strong> Raw text for pretraining; (instruction, response) pairs for SFT.</li>
<li><strong>Which tokens contribute to the loss.</strong> All tokens for pretraining; only response tokens for SFT (the prompt is masked out).</li>
</ul>
<p>That’s the whole mechanical difference. SFT is pretraining on more curated data with a loss mask on the prompt.</p>
<p>Preference optimization is the only stage that introduces a new loss family. The output is no longer compared to a target sequence; it’s compared to a <em>competing</em> sequence under a preference framework. RLHF wraps this in a reward model and PPO; <a href="https://chanys.github.io/dpo/">DPO</a> <span class="citation" data-cites="rafailov_etal_2023">(Rafailov et al. 2023)</span> collapses the same idea into a single supervised loss; GRPO replaces the value network with group statistics. Each has its own deep-dive in this site’s RL series: <a href="../../../../posts/series/how-llms-learn-to-reason/01-ppo/index.html">PPO is REINFORCE Plus Five Fixes</a>, <a href="../../../../posts/series/how-llms-learn-to-reason/03-dpo/index.html">DPO: RLHF Collapsed Into One Loss</a>, and <a href="../../../../posts/series/how-llms-learn-to-reason/02-grpo/index.html">GRPO: The Algorithm Behind Reasoning Models</a>.</p>
</section>
<section id="supervised-fine-tuning" class="level2">
<h2 class="anchored" data-anchor-id="supervised-fine-tuning">Supervised fine-tuning</h2>
<p>SFT’s loss is the same as pretraining’s. Everything interesting is in the data.</p>
<p>The mechanics, traced through one example. Take an instruction-response pair:</p>
<ul>
<li><strong>Instruction</strong>: “Classify the sentiment of this review and explain why: ‘The food was great but the service was terrible.’”</li>
<li><strong>Response</strong>: “Mixed sentiment. The reviewer praises the food but criticizes the service.”</li>
</ul>
<p>You concatenate the instruction and response into a single sequence and feed it through the model. The model computes next-token cross-entropy as it would in pretraining. The difference: a loss mask zeros out the contribution from instruction tokens. Only the response tokens carry gradient.</p>
<p>The wrong reading is that the model learns to generate instruction-and-response pairs. It doesn’t. The instruction tokens have no loss contribution, so the model gets no gradient signal saying “produce instructions like these.” What the model learns is to produce response tokens given the instruction tokens as context. At inference, you provide the instruction; the model continues with the response.</p>
<p>That’s it for the loss. Everything else is data engineering. SFT data comes in a few forms:</p>
<p><strong>Human-written demonstrations.</strong> The original <a href="https://chanys.github.io/chatgpt/">InstructGPT</a> <span class="citation" data-cites="ouyang_etal_2022">(Ouyang et al. 2022)</span> recipe: humans wrote responses to a curated set of instructions, and the model was fine-tuned on those pairs. High-quality, but expensive and slow.</p>
<p><strong>Multitask instruction data.</strong> The Sept 2021 <a href="https://chanys.github.io/flan/">FLAN</a> <span class="citation" data-cites="wei_etal_2022">(Wei et al. 2022)</span> paper showed that training on many NLP tasks formatted as instructions improves zero-shot performance on unseen tasks. <a href="https://chanys.github.io/t0/">T0</a> <span class="citation" data-cites="sanh_etal_2022">(Sanh et al. 2022)</span> showed the same on encoder-decoder models; <a href="https://chanys.github.io/tkinstruct/">Tk-INSTRUCT</a> <span class="citation" data-cites="wang_etal_2022_tkinstruct">(Wang et al. 2022)</span> and <a href="https://chanys.github.io/flan-palm/">FLAN-PaLM</a> scaled to 1,600 and 1,800 tasks. More tasks, more diverse templates, and larger base models all help. FLAN also found a size threshold: multitask SFT <em>hurt</em> held-out performance at 8B and below, helped substantially at 137B. T0 found the threshold lower (3B) for encoder-decoder models, the kind of small-scale inductive-bias advantage that scales away (see <a href="../../../../posts/series/research-foundations-of-modern-llms/01-pretraining-objectives/index.html">Pretraining objectives: why decoder-only won</a>).</p>
<p><strong>Synthetic teacher-generated data.</strong> By late 2022 the bottleneck was data. Self-Instruct <span class="citation" data-cites="wang_etal_2023_selfinstruct">(Wang et al. 2023)</span> showed an instruction-following LLM could generate its own training data: seed with a few human instructions, generate variations and responses, filter for quality. Alpaca and <a href="https://chanys.github.io/zephyr/">Zephyr</a> <span class="citation" data-cites="tunstall_etal_2023">(Tunstall et al. 2023)</span> operationalized this (Zephyr’s UltraChat: 1.47M GPT-3.5 dialogues filtered to 200K). This <em>distilled SFT</em> (dSFT) pattern is now standard; the scale is set by what teacher models can produce, not what humans can write. Two caveats: the student inherits the teacher’s limits, and the filter does real work. The empirical lesson of the past two years is that 10K well-chosen examples often beat 1M scraped ones <span class="citation" data-cites="zhou_etal_2023">(Zhou et al. 2023)</span>.</p>
<p><strong>Chat-format data.</strong> The same loss is applied across multi-turn conversations, with the loss mask zeroing out user turns and computing loss only on assistant turns. Mechanically identical to single-turn SFT; the data just has more turns.</p>
<p>A note on single-task vs multi-task SFT. If your application is one task with consistent phrasing, single-task SFT is fine. If your application is a chat assistant handling varied user queries, multi-task SFT is doing real work: the diverse training data is what gives the model robustness across phrasings. Almost everyone does multi-task SFT, because almost everyone is building something at least chat-shaped.</p>
</section>
<section id="lora-and-qlora-change-the-memory-story-not-the-objective" class="level2">
<h2 class="anchored" data-anchor-id="lora-and-qlora-change-the-memory-story-not-the-objective">LoRA and QLoRA change the memory story, not the objective</h2>
<p>LoRA and QLoRA don’t change what the model is being trained on or how the loss is computed. They change what parameters are trainable and what fits in memory. That distinction matters for the article’s thesis: most of the post-pretraining stack is the same algorithm applied to different data; parameter-efficient methods are a memory-engineering layer on top, not a new training paradigm.</p>
<div id="fig-lora" class="quarto-float quarto-figure quarto-figure-center anchored" alt="Two panels. Left: a frozen base weight matrix W-zero shown alongside a low-rank update formed by multiplying matrix B of size d by r with matrix A of size r by k, where the rank r is much smaller than d or k, so only A and B are trained. Right: a bar chart comparing GPU memory to fine-tune a 7B model under three regimes, roughly 84GB for full fine-tuning at FP32, about 14GB for LoRA on an FP16 base, and about 4GB for QLoRA on a 4-bit quantized base.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-lora-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/04-fine-tuning-stack/lora.png" class="img-fluid figure-img" alt="Two panels. Left: a frozen base weight matrix W-zero shown alongside a low-rank update formed by multiplying matrix B of size d by r with matrix A of size r by k, where the rank r is much smaller than d or k, so only A and B are trained. Right: a bar chart comparing GPU memory to fine-tune a 7B model under three regimes, roughly 84GB for full fine-tuning at FP32, about 14GB for LoRA on an FP16 base, and about 4GB for QLoRA on a 4-bit quantized base.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-lora-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: LoRA decomposes the fine-tuning update into two small matrices B (d×r) and A (r×k), with r much smaller than d or k. The base weight matrix W₀ stays frozen. Right: memory for fine-tuning a 7B model under three regimes; full fine-tuning at FP32 needs ~84GB, LoRA on FP16 base ~14GB, QLoRA on 4-bit base ~4GB.
</figcaption>
</figure>
</div>
<p><a href="https://chanys.github.io/lora/">LoRA</a> <span class="citation" data-cites="hu_etal_2022">(Hu et al. 2022)</span> trains a small low-rank adapter on top of a frozen base model. The premise: full fine-tuning is overkill for most tasks because fine-tuning updates have low intrinsic rank. The base already knows grammar, world facts, and reasoning patterns; fine-tuning is usually a <em>nudge</em>, not a rewrite. LoRA replaces the full update <img src="https://latex.codecogs.com/png.latex?%5CDelta%20W"> with the product of two much smaller matrices <img src="https://latex.codecogs.com/png.latex?BA">, where the rank <img src="https://latex.codecogs.com/png.latex?r"> is much smaller than the original matrix dimensions. The base <img src="https://latex.codecogs.com/png.latex?W_0"> stays frozen; only the small <img src="https://latex.codecogs.com/png.latex?B"> and <img src="https://latex.codecogs.com/png.latex?A"> are trained.</p>
<p><a href="https://chanys.github.io/qlora/">QLoRA</a> <span class="citation" data-cites="dettmers_etal_2023">(Dettmers et al. 2023)</span> keeps LoRA’s structure but quantizes the frozen base to 4 bits while leaving the adapter in 16-bit. The QLoRA mechanics (NF4 quantization, double quantization of scale constants, paged optimizers for memory spikes) are covered in the <a href="https://chanys.github.io/qlora/">QLoRA deep-dive post</a>; they’re what makes the memory math work but they’re orthogonal to the article’s thesis.</p>
<p>Almost all open-source fine-tuning in 2024-2026 uses LoRA or QLoRA. Full fine-tuning still happens for frontier-scale base-model training, but downstream specialization is overwhelmingly LoRA-based. The wave of fine-tuned open-source models from late 2023 onward is downstream of this.</p>
</section>
<section id="when-preference-optimization-actually-matters" class="level2">
<h2 class="anchored" data-anchor-id="when-preference-optimization-actually-matters">When preference optimization actually matters</h2>
<p>Once a model has been SFT’d, you can do another round of training that uses preference data: pairs of responses where one is judged better than the other. The goal is to push the model toward responses that match human (or proxy) preferences for properties like helpfulness, harmlessness, conciseness.</p>
<p>Three families dominate the post-2022 alignment landscape:</p>
<ul>
<li><strong>RLHF</strong> (<a href="https://chanys.github.io/chatgpt/">InstructGPT</a> <span class="citation" data-cites="ouyang_etal_2022">(Ouyang et al. 2022)</span> recipe): SFT, then train a reward model on preference pairs, then run PPO using the reward model as the reward signal. Four LLM-sized things in memory at training time.</li>
<li><strong>DPO</strong> (Direct Preference Optimization): skip the reward model and the RL machinery. A single supervised loss on preference pairs. Two models in memory.</li>
<li><strong>GRPO</strong> (Group Relative Policy Optimization): PPO with the value network removed. Memory-efficient, well-suited to verifiable-reward settings (math, code). The algorithm behind R1 and most open-source reasoning models.</li>
</ul>
<p>The mechanics (why DPO’s derivation works, what PPO is doing under the hood, why GRPO works for reasoning) are covered in detail in this site’s RL series: <a href="../../../../posts/series/how-llms-learn-to-reason/01-ppo/index.html">PPO is REINFORCE plus five fixes</a>, <a href="../../../../posts/series/how-llms-learn-to-reason/03-dpo/index.html">DPO: RLHF collapsed into one loss</a>, and <a href="../../../../posts/series/how-llms-learn-to-reason/02-grpo/index.html">GRPO: the algorithm behind reasoning models</a>. For this article the strategic question is simpler: when does preference optimization actually matter for your application?</p>
<p>For chat assistants serving general users, almost always. SFT alone produces a model that can follow instructions but doesn’t have stable behavioral preferences. It’ll be helpful one moment and verbose or evasive the next. Preference tuning is what shapes that into something consistent.</p>
<p>For applications where the desired behavior is itself contested (creative writing, advice-giving, judgments about taste), preference tuning is doing the bulk of the work. The model isn’t learning facts; it’s learning whose preferences to optimize for.</p>
</section>
<section id="tnlp-showpiece-the-fine-tuning-stack-in-code" class="level2">
<h2 class="anchored" data-anchor-id="tnlp-showpiece-the-fine-tuning-stack-in-code">TNLP showpiece: the fine-tuning stack in code</h2>
<p>The <a href="https://github.com/chanys/tnlp">TNLP repo</a> implements the three stages of post-pretraining on LLaMA-2-7B and Mistral-7B: instruction fine-tuning, chat fine-tuning, and DPO:</p>
<ul>
<li><a href="https://github.com/chanys/tnlp#instruction-fine-tuning"><strong>Instruction fine-tuning</strong></a> trains LLaMA-2-7B on the Alpaca dataset using <code>AutoModelForCausalLM</code> with LoRA and 8-bit loading.</li>
<li><a href="https://github.com/chanys/tnlp#chat-fine-tuning"><strong>Chat fine-tuning</strong></a> trains Mistral-7B on the multi-turn UltraChat dataset using TRL’s <code>SFTTrainer</code> with QLoRA (4-bit base, 16-bit adapters). Same next-token objective as instruction tuning; only the data format (turn-based user/assistant exchanges) differs.</li>
<li><a href="https://github.com/chanys/tnlp#direct-preference-optimization-dpo"><strong>DPO</strong></a> takes the chat-tuned model and runs preference optimization on UltraFeedback (binarized chosen/rejected pairs) via TRL’s <code>DPOTrainer</code>, again with QLoRA. This is where the objective itself changes: from next-token imitation to direct preference optimization against a frozen reference policy.</li>
</ul>
<p>The QLoRA setup runs on a single 24GB GPU.</p>
<p>Related writeups: <a href="https://chanys.github.io/flan-code/">LoRA fine-tuning of LLaMA-2-7B</a> and <a href="../../../../posts/series/how-llms-learn-to-reason/03-dpo/index.html">DPO: RLHF Collapsed Into One Loss</a>.</p>
</section>
<section id="closing" class="level2">
<h2 class="anchored" data-anchor-id="closing">Closing</h2>
<p>The post-pretraining stack looks complicated because every stage has its own name. Most of those names are about the data, not the algorithm. Pretraining and SFT share next-token cross-entropy; SFT just curates the data and masks the loss to response tokens. Preference optimization is the one place the loss family genuinely changes. LoRA and QLoRA don’t change the loss; they change what fits in memory.</p>
<p>That’s most of post-pretraining in a paragraph. The depth is in the data engineering (what instruction sources to use, how to filter synthetic data, how to balance task mixtures, when preference data is worth collecting) and in the algorithmic deep-dives for preference optimization.</p>



</section>

<div id="quarto-appendix" class="default"><section class="quarto-appendix-contents" id="quarto-bibliography"><h2 class="anchored quarto-appendix-heading">References</h2><div id="refs" class="references csl-bib-body hanging-indent">
<div id="ref-dettmers_etal_2023" class="csl-entry">
Dettmers, Tim, Artidoro Pagnoni, Ari Holtzman, and Luke Zettlemoyer. 2023. <span>“QLoRA: Efficient Finetuning of Quantized LLMs.”</span> <em>Advances in Neural Information Processing Systems (NeurIPS)</em>. <a href="https://arxiv.org/abs/2305.14314">https://arxiv.org/abs/2305.14314</a>.
</div>
<div id="ref-hu_etal_2022" class="csl-entry">
Hu, Edward J., Yelong Shen, Phillip Wallis, et al. 2022. <span>“LoRA: Low-Rank Adaptation of Large Language Models.”</span> <em>International Conference on Learning Representations</em>. <a href="https://arxiv.org/abs/2106.09685">https://arxiv.org/abs/2106.09685</a>.
</div>
<div id="ref-ouyang_etal_2022" class="csl-entry">
Ouyang, Long, Jeff Wu, Xu Jiang, et al. 2022. <span>“Training Language Models to Follow Instructions with Human Feedback.”</span> <em>Advances in Neural Information Processing Systems (NeurIPS)</em>. <a href="https://arxiv.org/abs/2203.02155">https://arxiv.org/abs/2203.02155</a>.
</div>
<div id="ref-rafailov_etal_2023" class="csl-entry">
Rafailov, Rafael, Archit Sharma, Eric Mitchell, Stefano Ermon, Christopher D. Manning, and Chelsea Finn. 2023. <span>“Direct Preference Optimization: Your Language Model Is Secretly a Reward Model.”</span> <em>Advances in Neural Information Processing Systems (NeurIPS)</em>. <a href="https://arxiv.org/abs/2305.18290">https://arxiv.org/abs/2305.18290</a>.
</div>
<div id="ref-sanh_etal_2022" class="csl-entry">
Sanh, Victor, Albert Webson, Colin Raffel, et al. 2022. <span>“Multitask Prompted Training Enables Zero-Shot Task Generalization.”</span> <em>International Conference on Learning Representations (ICLR)</em>. <a href="https://arxiv.org/abs/2110.08207">https://arxiv.org/abs/2110.08207</a>.
</div>
<div id="ref-tunstall_etal_2023" class="csl-entry">
Tunstall, Lewis, Edward Beeching, Nathan Lambert, et al. 2023. <span>“Zephyr: Direct Distillation of LM Alignment.”</span> <em>arXiv Preprint</em>. <a href="https://arxiv.org/abs/2310.16944">https://arxiv.org/abs/2310.16944</a>.
</div>
<div id="ref-wang_etal_2023_selfinstruct" class="csl-entry">
Wang, Yizhong, Yeganeh Kordi, Swaroop Mishra, et al. 2023. <span>“Self-Instruct: Aligning Language Models with Self-Generated Instructions.”</span> <em>Proceedings of the 61st Annual Meeting of the Association for Computational Linguistics (ACL)</em>. <a href="https://arxiv.org/abs/2212.10560">https://arxiv.org/abs/2212.10560</a>.
</div>
<div id="ref-wang_etal_2022_tkinstruct" class="csl-entry">
Wang, Yizhong, Swaroop Mishra, Pegah Alipoormolabashi, et al. 2022. <span>“Super-NaturalInstructions: Generalization via Declarative Instructions on 1600+ NLP Tasks.”</span> <em>Proceedings of the 2022 Conference on Empirical Methods in Natural Language Processing (EMNLP)</em>. <a href="https://arxiv.org/abs/2204.07705">https://arxiv.org/abs/2204.07705</a>.
</div>
<div id="ref-wei_etal_2022" class="csl-entry">
Wei, Jason, Maarten Bosma, Vincent Y. Zhao, et al. 2022. <span>“Finetuned Language Models Are Zero-Shot Learners.”</span> <em>International Conference on Learning Representations (ICLR)</em>. <a href="https://arxiv.org/abs/2109.01652">https://arxiv.org/abs/2109.01652</a>.
</div>
<div id="ref-zhou_etal_2023" class="csl-entry">
Zhou, Chunting, Pengfei Liu, Puxin Xu, et al. 2023. <span>“LIMA: Less Is More for Alignment.”</span> <em>Advances in Neural Information Processing Systems (NeurIPS)</em>. <a href="https://arxiv.org/abs/2305.11206">https://arxiv.org/abs/2305.11206</a>.
</div>
</div></section></div> ]]></description>
  <category>Research foundations of modern LLMs</category>
  <guid>https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/04-fine-tuning-stack/</guid>
  <pubDate>Tue, 31 Mar 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Retrieval is older than RAG: from DPR to end-to-end</title>
  <dc:creator>Yee Seng Chan</dc:creator>
  <link>https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/03-retrieval/</link>
  <description><![CDATA[ 





<!-- Shared series navigation. Each PART includes this file with a Quarto
     include shortcode pointing at ../_series.qmd (see any part's index.qmd
     for the exact syntax — do NOT repeat that shortcode here, it would
     recurse). Links are sibling-relative (../NN-slug/) so they resolve
     identically from any part. When you add a part, add one line here.
     Files starting with "_" are never rendered as their own page. -->
<div class="series-nav">
<div class="series-label">
<p>Part of a series</p>
</div>
<div class="series-name">
<p>Research foundations of modern LLMs</p>
</div>
<!-- Add parts as an ordered list below as you publish, e.g.
     1. [Part title](../01-slug/)
     2. [Part title](../02-slug/) -->
<ol type="1">
<li><a href="../01-pretraining-objectives/">Pretraining objectives: Why decoder-only won</a></li>
<li><a href="../02-encoder-embeddings/">The encoder didn’t die. It became the embedding model</a></li>
<li><a href="../03-retrieval/">Retrieval is older than RAG: From DPR to end-to-end</a></li>
<li><a href="../04-fine-tuning-stack/">The fine-tuning stack: One loss, different data</a></li>
<li><a href="../05-information-extraction/">Information extraction didn’t disappear. It moved inside the workflow</a></li>
</ol>
</div>
<p>Most production RAG systems today are simple pipelines: a frozen embedding model, a vector database, and a frozen LLM connected by prompt assembly. That is closer to <strong>Dense Passage Retrieval</strong> (<a href="https://chanys.github.io/dpr/">DPR</a>), the 2020 bi-encoder retrieval method from Karpukhin et al. <span class="citation" data-cites="karpukhin_etal_2020">(Karpukhin et al. 2020)</span> with a generator, than to the original <a href="https://chanys.github.io/rag/">RAG</a> paper <span class="citation" data-cites="lewis_etal_2020">(Lewis et al. 2020)</span>, which proposed joint training of the retriever and generator with marginalization over retrieved documents.</p>
<p>This article builds on paper-by-paper notes I wrote on <a href="https://chanys.github.io" class="uri">https://chanys.github.io</a> between 2022 and 2023. Those posts explain the individual papers. Here, I step back and connect them into a larger story: retrieval was already a mature technical lineage before “RAG” became the umbrella term. Later, I also point to my <a href="https://github.com/chanys/tnlp">TNLP</a> code that implements the more expensive end-to-end version.</p>
<section id="what-rag-meant-in-2020-vs-what-it-means-today" class="level2">
<h2 class="anchored" data-anchor-id="what-rag-meant-in-2020-vs-what-it-means-today">What RAG meant in 2020 vs what it means today</h2>
<p>The Lewis et al.&nbsp;(2020) paper proposed:</p>
<ol type="1">
<li>A DPR-style bi-encoder retriever</li>
<li>A BART seq2seq generator</li>
<li><strong>Joint training</strong> of query encoder and generator (document encoder frozen)</li>
</ol>
<p>What most production RAG deploys:</p>
<ol type="1">
<li>An off-the-shelf embedding model (BGE, E5, OpenAI text-embedding-3, Cohere), frozen</li>
<li>A vector database (Pinecone, Weaviate, Qdrant, Milvus, pgvector, Chroma)</li>
<li>An LLM API (OpenAI, Anthropic, Google), frozen</li>
<li><strong>No joint training</strong></li>
</ol>
<p>This isn’t a critique. The simpler pattern works well for many use cases, doesn’t require ML engineering depth, and decouples the retriever from the generator.</p>
</section>
<section id="a-short-history-of-dense-retrieval" class="level2">
<h2 class="anchored" data-anchor-id="a-short-history-of-dense-retrieval">A short history of dense retrieval</h2>
<p>The history below is the chronology that produced the drift. Each step changed what was possible, and most of the production world adopted only part of what each step demonstrated. The story is short because it happened fast: almost all the field-defining work landed between 2020 and 2023, on top of a lexical baseline (BM25) you still need.</p>
<div id="fig-retrieval-lineage" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A swim-lane timeline with four lanes from top to bottom: joint training, late interaction, bi-encoder dense, and lexical. The x-axis is years, with 2009 on the left, then an axis break, then 2020, 2021, 2022, 2023. BM25 sits alone in the lexical lane at 2009. In 2020 four papers appear across three lanes: REALM and RAG on the joint-training lane, ColBERT on the late-interaction lane, DPR on the bi-encoder-dense lane. ColBERTv2 follows in 2021 on the late-interaction lane. End-to-end RAG appears in 2022 and RA-DIT in 2023, both on the joint-training lane. DPR is highlighted with a thicker border and a light indigo fill, with the label production sits here directly below it. The bi-encoder-dense lane is empty to the right of DPR while the joint-training lane keeps going. Caption: production rode the bi-encoder and stopped at DPR; the joint-training lineage is what got skipped.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-retrieval-lineage-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/03-retrieval/retrieval_lineage.png" class="img-fluid figure-img" alt="A swim-lane timeline with four lanes from top to bottom: joint training, late interaction, bi-encoder dense, and lexical. The x-axis is years, with 2009 on the left, then an axis break, then 2020, 2021, 2022, 2023. BM25 sits alone in the lexical lane at 2009. In 2020 four papers appear across three lanes: REALM and RAG on the joint-training lane, ColBERT on the late-interaction lane, DPR on the bi-encoder-dense lane. ColBERTv2 follows in 2021 on the late-interaction lane. End-to-end RAG appears in 2022 and RA-DIT in 2023, both on the joint-training lane. DPR is highlighted with a thicker border and a light indigo fill, with the label production sits here directly below it. The bi-encoder-dense lane is empty to the right of DPR while the joint-training lane keeps going. Caption: production rode the bi-encoder and stopped at DPR; the joint-training lineage is what got skipped.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-retrieval-lineage-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: The dense-retrieval lineage on four paradigm lanes. BM25 has been the lexical baseline since 2009. Between 2020 and 2023 the dense-retrieval lineage splits across three other paths: bi-encoder dense (DPR), late interaction (ColBERT, ColBERTv2), and joint training (REALM, RAG, End-to-end RAG, RA-DIT). Production RAG today is essentially DPR plus a frozen generator: it took the bi-encoder lane and stopped there, while the joint-training lane kept going.
</figcaption>
</figure>
</div>
<section id="bm25-robertson_zaragoza_2009" class="level3">
<h3 class="anchored" data-anchor-id="bm25-robertson_zaragoza_2009">BM25 <span class="citation" data-cites="robertson_zaragoza_2009">(Robertson and Zaragoza 2009)</span></h3>
<p>The baseline that wouldn’t die. Lexical ranking over inverted indices, using TF-IDF with length normalization and term saturation. For roughly two decades, BM25 was the baseline neural methods couldn’t consistently beat. DPR’s primary contribution was beating it cleanly enough that the field finally moved on, but BM25 didn’t go away: hybrid setups (BM25 + dense, often with reciprocal rank fusion <span class="citation" data-cites="cormack_etal_2009">(Cormack et al. 2009)</span>) are common in production today, and pure-dense systems frequently lose to hybrid on tasks with rare-term queries.</p>
</section>
<section id="realm-guu_etal_2020" class="level3">
<h3 class="anchored" data-anchor-id="realm-guu_etal_2020"><a href="https://chanys.github.io/realm/">REALM</a> <span class="citation" data-cites="guu_etal_2020">(Guu et al. 2020)</span></h3>
<p>Two months before DPR, REALM proposed something more ambitious: retrieval-augmented language model pretraining, with the retriever trained jointly with the generator from MLM signal. It got far less production traction than DPR because joint pretraining was expensive, and DPR’s recipe was simpler and more transferable.</p>
</section>
<section id="dpr-karpukhin_etal_2020" class="level3">
<h3 class="anchored" data-anchor-id="dpr-karpukhin_etal_2020"><a href="https://chanys.github.io/dpr/">DPR</a> <span class="citation" data-cites="karpukhin_etal_2020">(Karpukhin et al. 2020)</span></h3>
<p>The paper that made bi-encoder dense retrieval practical. Two BERT encoders, <img src="https://latex.codecogs.com/png.latex?E_Q"> for queries and <img src="https://latex.codecogs.com/png.latex?E_P"> for passages. Score by inner product. Train with the negative log-likelihood of the positive passage against negatives (both explicit hard negatives, and in-batch negatives where the positives of other examples in the same batch serve as negatives):</p>
<p><img src="https://latex.codecogs.com/png.latex?%5Cmathcal%7BL%7D(q_i,%20p_i%5E+,%20p_%7Bi,1%7D%5E-,%20%5Cldots,%20p_%7Bi,n%7D%5E-)%20=%20-%5Clog%20%5Cfrac%7Be%5E%7B%5Cmathrm%7Bsim%7D(q_i,%20p_i%5E+)%7D%7D%7Be%5E%7B%5Cmathrm%7Bsim%7D(q_i,%20p_i%5E+)%7D%20+%20%5Csum_j%20e%5E%7B%5Cmathrm%7Bsim%7D(q_i,%20p_%7Bi,j%7D%5E-)%7D%7D"></p>
<p>The contributions were practical: in-batch negatives (free supervision), independent encoders for query and passage (asymmetric architecture for asymmetric inputs), and a clean training recipe. The result was the first dense retriever to consistently beat BM25 on open-domain QA.</p>
<p>DPR’s bi-encoder is the architecture that most RAG systems still use, with newer embedding models in place of BERT. The drift starts here: the production world adopted DPR’s architecture but not its training discipline.</p>
</section>
<section id="rag-lewis_etal_2020" class="level3">
<h3 class="anchored" data-anchor-id="rag-lewis_etal_2020"><a href="https://chanys.github.io/rag/">RAG</a> <span class="citation" data-cites="lewis_etal_2020">(Lewis et al. 2020)</span></h3>
<p>The paper RAG got its name from, and the one production drift moved furthest away from. DPR retriever plus BART seq2seq generator, jointly fine-tunable. The RAG-token model marginalizes over the top-k retrieved documents per output token:</p>
<p><img src="https://latex.codecogs.com/png.latex?p_%5Cmathrm%7BRAG%5Ctext%7B-%7DToken%7D(y%7Cx)%20%5Capprox%20%5Cprod_%7Bi=1%7D%5EN%20%5Csum_%7Bz%20%5Cin%20%5Cmathrm%7Btop%5Ctext%7B-%7D%7Dk(p(%5Ccdot%7Cx))%7D%20p_%5Cmathrm%7BDPR%7D(z%7Cx)%20%5Ccdot%20p_%5Ctheta(y_i%20%7C%20x,%20z,%20y_%7B1:i-1%7D)"></p>
<p>Two important details. First, only the <strong>query encoder</strong> is updated during training; the document encoder (and the FAISS index) stay frozen. Re-encoding the corpus during training is too expensive. Second, the marginalization is per-token: at every output token, the model sums the probability of that token across the retrieved documents weighted by retrieval probability. This is genuinely different from concatenating top-k documents and running a single forward pass, which is what most production RAG does.</p>
</section>
<section id="colbert-khattab_zaharia_2020" class="level3">
<h3 class="anchored" data-anchor-id="colbert-khattab_zaharia_2020"><a href="https://chanys.github.io/colbert/">ColBERT</a> <span class="citation" data-cites="khattab_zaharia_2020">(Khattab and Zaharia 2020)</span></h3>
<p>Late interaction was the path the field didn’t ultimately take, though it remains the most interesting compromise between bi-encoder speed and cross-encoder accuracy. Encode query and document independently with BERT, then compute similarity at the token level:</p>
<p><img src="https://latex.codecogs.com/png.latex?S_%7Bq,d%7D%20=%20%5Csum_%7Bi%20%5Cin%20E_q%7D%20%5Cmax_%7Bj%20%5Cin%20E_d%7D%20%5Cleft(%20E_%7Bq_i%7D%20%5Ccdot%20E_%7Bd_j%7D%5E%5Ctop%20%5Cright)"></p>
<p>For each query token, find the most similar document token, sum the maxima. Encoding stays independent (documents can be indexed offline), but the matching is fine-grained. The cost is storage: ColBERT stores one vector per token instead of one per document, roughly 100x for a 100-token document. That’s its main operational drawback.</p>
</section>
<section id="colbertv2-santhanam_etal_2021" class="level3">
<h3 class="anchored" data-anchor-id="colbertv2-santhanam_etal_2021"><a href="https://chanys.github.io/colbertv2/">ColBERTv2</a> <span class="citation" data-cites="santhanam_etal_2021">(Santhanam et al. 2022)</span></h3>
<p>Same architecture with two improvements: residual quantization (cluster the per-token embeddings, store each as <code>(centroid_id, residual)</code> with a 2-bit residual; storage drops 6-10x), and distillation from a cross-encoder. ColBERT and ColBERTv2 are the canonical late-interaction systems, and they work at scale. But modern strong bi-encoders have closed enough of the gap that the storage cost is hard to justify for most use cases.</p>
</section>
<section id="end-to-end-rag-siriwardhana_etal_2023" class="level3">
<h3 class="anchored" data-anchor-id="end-to-end-rag-siriwardhana_etal_2023"><a href="https://chanys.github.io/rag-domain-qa/">End-to-end RAG</a> <span class="citation" data-cites="siriwardhana_etal_2023">(Siriwardhana et al. 2023)</span></h3>
<p>If the original RAG paper described joint training with a frozen document encoder, this is the paper that made even the document encoder trainable. The engineering cost is what most teams won’t pay.</p>
<p>The original RAG paper kept the document encoder frozen during training because re-encoding the corpus was too expensive. End-to-end RAG removes that constraint with two asynchronous processes: one continuously re-encodes passages with the updated document encoder, and one rebuilds the index. Training proceeds in parallel; the index is updated periodically with the latest version of the encoder.</p>
<p>This makes joint training of all components feasible: query encoder, document encoder, and generator. The paper also adds an auxiliary loss: regenerate the input query from the retrieved passages. This forces the retriever to find passages that contain enough information to reconstruct the query, which is a useful signal when domain-specific labels are scarce.</p>
<p>This is the closest to a “real” end-to-end retrieval-augmented model. The async re-encoding pipeline is the cost most production teams won’t pay.</p>
</section>
<section id="ra-dit-lin_etal_2023" class="level3">
<h3 class="anchored" data-anchor-id="ra-dit-lin_etal_2023"><a href="https://chanys.github.io/radit/">RA-DIT</a> <span class="citation" data-cites="lin_etal_2023">(Lin et al. 2023)</span></h3>
<p>The practical answer to the engineering question end-to-end RAG raises: how do you get most of the benefit of joint training without the async re-indexing pipeline? Two separate fine-tunings, run in sequence. First, fine-tune the LLM to use retrieved chunks. Second, fine-tune the retriever using LM-supervised retrieval (LSR): score documents by how much they raise the LM’s probability of the correct output:</p>
<p><img src="https://latex.codecogs.com/png.latex?p_%5Cmathrm%7BLSR%7D(c%20%7C%20x,%20y)%20%5Cpropto%20%5Cfrac%7B%5Cexp%5Cleft(%20p_%5Cmathrm%7BLM%7D(y%20%7C%20c%20%5Ccirc%20x)%20/%20%5Ctau%20%5Cright)%7D%7B%5Csum_%7Bc'%20%5Cin%20C'%7D%20%5Cexp%5Cleft(%20p_%5Cmathrm%7BLM%7D(y%20%7C%20c'%20%5Ccirc%20x)%20/%20%5Ctau%20%5Cright)%7D"></p>
<p>Only the query encoder is updated; the document encoder stays frozen. The two stages can each be done with standard fine-tuning infrastructure. The paper reports SOTA results on MMLU, NQ, TriviaQA, and KILT subsets with a 65B-parameter LLM.</p>
</section>
</section>
<section id="the-retrieval-pattern-most-production-systems-actually-use" class="level2">
<h2 class="anchored" data-anchor-id="the-retrieval-pattern-most-production-systems-actually-use">The retrieval pattern most production systems actually use</h2>
<p>Setting aside the joint-training story, here is the pipeline most teams have built. The bi-encoder/cross-encoder split is the operational story; everything else is supporting infrastructure.</p>
<div id="fig-rag-pipeline" class="quarto-float quarto-figure quarto-figure-center anchored" alt="A two-row pipeline diagram. The top row is offline indexing: source documents are chunked, embedded, and written into a vector index, run once. The bottom row is the online query path: a user query is embedded, used to retrieve candidate chunks from the same vector index, passed through an optional reranker stage, and the top results plus the query are sent to the generator to produce the answer. An arrow shows the vector index built in the top row being read by every request in the bottom row.">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-rag-pipeline-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/03-retrieval/rag_pipeline.png" class="img-fluid figure-img" alt="A two-row pipeline diagram. The top row is offline indexing: source documents are chunked, embedded, and written into a vector index, run once. The bottom row is the online query path: a user query is embedded, used to retrieve candidate chunks from the same vector index, passed through an optional reranker stage, and the top results plus the query are sent to the generator to produce the answer. An arrow shows the vector index built in the top row being read by every request in the bottom row.">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-rag-pipeline-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: The production RAG pipeline. Top: offline indexing. Bottom: online query, with the reranker as an optional but usually worthwhile stage. The vector index built once at indexing time is queried at every request.
</figcaption>
</figure>
</div>
<p><strong>Chunking.</strong> Source documents are split into chunks. Fixed-token chunking (256-512 tokens with 10-20% overlap) is the default and works well enough for most prose. Recursive chunking respects document structure (Markdown headers, paragraph breaks). Semantic chunking (splitting on sentence-embedding distance) is more expensive and rarely justifies the cost on standard text. The ceiling on retrieval quality is often set here: chunks too small lose context, chunks too large produce diffuse embeddings that match weakly. The default chunker in the framework is rarely the right one for your data: PDFs with tables, code with function boundaries, and Markdown with structured sections each need different handling.</p>
<p><strong>Embedding.</strong> Each chunk is encoded once with a frozen embedding model.</p>
<p><strong>Vector database.</strong> The encoded chunks live in a vector index, e.g.&nbsp;HNSW or IVF-PQ under the hood. Pinecone, Weaviate, Qdrant, Milvus, pgvector, Chroma: pick on operational fit, not on retrieval quality. The choice of embedding model and chunking strategy usually dominates. pgvector is increasingly competitive when your data is already in Postgres and you don’t want a second system to operate. For the indexing intuition behind approximate nearest-neighbor search and product quantization, see my earlier <a href="https://chanys.github.io/knn/">KNN search note</a>.</p>
<p><strong>Top-k retrieval.</strong> Encode the query with the same embedding model used at indexing time, retrieve top-<img src="https://latex.codecogs.com/png.latex?k"> by cosine similarity (or dot product if vectors are normalized). Typical <img src="https://latex.codecogs.com/png.latex?k"> is 10-50. Larger <img src="https://latex.codecogs.com/png.latex?k"> is wasted unless you rerank.</p>
<p><strong>Reranking (optional but usually worth it).</strong> A cross-encoder reranker takes the query and each retrieved chunk together and produces a relevance score. The bi-encoder is fast but cannot model query-document interactions; the cross-encoder can. Bi-encoder retrieves 50, cross-encoder reranks to top 5-10. The off-the-shelf rerankers in 2024-2025 are strong enough that the “no reranker” failure mode is increasingly hard to justify: <a href="https://huggingface.co/BAAI/bge-reranker-v2-m3">BGE-reranker-v2-m3</a>, <a href="https://docs.cohere.com/docs/reranking-with-cohere">Cohere Rerank 3</a>, <a href="https://huggingface.co/mixedbread-ai/mxbai-rerank-large-v1">mxbai-rerank-large-v1</a>, and <a href="https://jina.ai/models/jina-reranker-v2-base-multilingual/">Jina Reranker v2</a> are all usable before you train your own reranker.</p>
<p>A note on the bi-encoder vs cross-encoder choice: cross-encoders score query-document pairs jointly, so they cannot be precomputed. Running one over millions of documents at query time is infeasible. They belong in reranking, after a bi-encoder narrows the candidates.</p>
<p><strong>Context assembly.</strong> A prompt template combines the system instruction, the retrieved-and-reranked chunks, and the query. Order matters: long-context models attend more to the start and end of the prompt than to the middle (the “lost in the middle” effect). Some systems include chunk metadata (source URL, section title) to help the LLM cite.</p>
<p><strong>LLM call.</strong> Frozen API or local model. The LLM generates the answer from the assembled prompt.</p>
<p>This pipeline is what most blog posts mean when they say RAG. It’s also what most teams should build first before they consider anything more complex.</p>
</section>
<section id="code-companion-tnlp-end-to-end-rag" class="level2">
<h2 class="anchored" data-anchor-id="code-companion-tnlp-end-to-end-rag">Code companion: TNLP end-to-end RAG</h2>
<p>Most production RAG systems stop at frozen retrieval plus prompt assembly. In <a href="https://github.com/chanys/tnlp">TNLP</a>, I implemented the more expensive pattern: end-to-end RAG with retriever-generator coupling and asynchronous index refresh.</p>
<p>The point of the exercise isn’t that everyone should joint-train. Most teams shouldn’t. The point is to make the distinction concrete: production RAG is usually a pipeline; end-to-end RAG is a trained retrieval-augmented model.</p>
</section>
<section id="rag-evaluation" class="level2">
<h2 class="anchored" data-anchor-id="rag-evaluation">RAG evaluation</h2>
<p>Three things tend to go wrong when teams evaluate this pipeline.</p>
<ol type="1">
<li><p><strong>Treating RAG as monolithic.</strong> RAG is a pipeline. Each stage (chunking, embedding, retrieval, reranking, prompt assembly, generation) has its own quality and latency tradeoffs. “Our RAG isn’t working” is rarely diagnosed by treating the system as a black box.</p></li>
<li><p><strong>Vibes-only evaluation.</strong> <a href="../../../../posts/series/llm-evaluation-honestly/01-stop-vibe-checking/index.html">“It seems to work” is not evaluation</a>. At minimum: a labeled set of (query, relevant_chunk) pairs, <a href="https://chanys.github.io/ir-metrics/">retrieval metrics</a> on a held-out set, and end-to-end answer correctness. Without these, you don’t know whether the embedding model, the reranker, or the LLM is failing.</p></li>
<li><p><strong>Not measuring retrieval recall before LLM accuracy.</strong> If the relevant chunks aren’t retrieved, no LLM can save you. Measure <a href="../../../../posts/series/llm-evaluation-honestly/06-rag-evaluation/index.html">retrieval recall@k</a> first, then end-to-end accuracy.</p></li>
</ol>
</section>
<section id="closing" class="level2">
<h2 class="anchored" data-anchor-id="closing">Closing</h2>
<p>The retrieval lineage from DPR through RA-DIT is a story of escalating sophistication: bi-encoder dense retrieval, then joint training with a frozen index, then joint training with an updating index, then a two-stage practical compromise. Production RAG, meanwhile, mostly stopped at step one. The simpler pattern works well for most use cases, and the engineering cost of going further is real.</p>
<p>But the gap is worth knowing. When the off-the-shelf pipeline isn’t working on your data, the next step isn’t to swap embedding models or try another vector database. It’s to figure out where in the pipeline the loss is happening, and whether the answer is a configuration change, a fine-tune, or the more ambitious territory the original RAG paper was actually about.</p>



</section>

<div id="quarto-appendix" class="default"><section class="quarto-appendix-contents" id="quarto-bibliography"><h2 class="anchored quarto-appendix-heading">References</h2><div id="refs" class="references csl-bib-body hanging-indent">
<div id="ref-cormack_etal_2009" class="csl-entry">
Cormack, Gordon V., Charles L. A. Clarke, and Stefan Buettcher. 2009. <span>“Reciprocal Rank Fusion Outperforms Condorcet and Individual Rank Learning Methods.”</span> <em>Proceedings of the 32nd International ACM SIGIR Conference on Research and Development in Information Retrieval</em>.
</div>
<div id="ref-guu_etal_2020" class="csl-entry">
Guu, Kelvin, Kenton Lee, Zora Tung, Panupong Pasupat, and Ming-Wei Chang. 2020. <span>“REALM: Retrieval-Augmented Language Model Pre-Training.”</span> <em>Proceedings of the 37th International Conference on Machine Learning (ICML)</em>. <a href="https://arxiv.org/abs/2002.08909">https://arxiv.org/abs/2002.08909</a>.
</div>
<div id="ref-karpukhin_etal_2020" class="csl-entry">
Karpukhin, Vladimir, Barlas Oğuz, Sewon Min, et al. 2020. <span>“Dense Passage Retrieval for Open-Domain Question Answering.”</span> <em>Proceedings of the 2020 Conference on Empirical Methods in Natural Language Processing (EMNLP)</em>. <a href="https://arxiv.org/abs/2004.04906">https://arxiv.org/abs/2004.04906</a>.
</div>
<div id="ref-khattab_zaharia_2020" class="csl-entry">
Khattab, Omar, and Matei Zaharia. 2020. <span>“ColBERT: Efficient and Effective Passage Search via Contextualized Late Interaction over BERT.”</span> <em>Proceedings of the 43rd International ACM SIGIR Conference on Research and Development in Information Retrieval</em>. <a href="https://arxiv.org/abs/2004.12832">https://arxiv.org/abs/2004.12832</a>.
</div>
<div id="ref-lewis_etal_2020" class="csl-entry">
Lewis, Patrick, Ethan Perez, Aleksandra Piktus, et al. 2020. <span>“Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks.”</span> <em>Advances in Neural Information Processing Systems (NeurIPS)</em>. <a href="https://arxiv.org/abs/2005.11401">https://arxiv.org/abs/2005.11401</a>.
</div>
<div id="ref-lin_etal_2023" class="csl-entry">
Lin, Xi Victoria, Xilun Chen, Mingda Chen, et al. 2023. <span>“RA-DIT: Retrieval-Augmented Dual Instruction Tuning.”</span> <em>arXiv Preprint</em>. <a href="https://arxiv.org/abs/2310.01352">https://arxiv.org/abs/2310.01352</a>.
</div>
<div id="ref-robertson_zaragoza_2009" class="csl-entry">
Robertson, Stephen, and Hugo Zaragoza. 2009. <span>“The Probabilistic Relevance Framework: BM25 and Beyond.”</span> <em>Foundations and Trends in Information Retrieval</em> 3 (4): 333–89.
</div>
<div id="ref-santhanam_etal_2021" class="csl-entry">
Santhanam, Keshav, Omar Khattab, Jon Saad-Falcon, Christopher Potts, and Matei Zaharia. 2022. <span>“ColBERTv2: Effective and Efficient Retrieval via Lightweight Late Interaction.”</span> <em>Proceedings of the 2022 Conference of the North American Chapter of the Association for Computational Linguistics: Human Language Technologies (NAACL-HLT)</em>. <a href="https://arxiv.org/abs/2112.01488">https://arxiv.org/abs/2112.01488</a>.
</div>
<div id="ref-siriwardhana_etal_2023" class="csl-entry">
Siriwardhana, Shamane, Rivindu Weerasekera, Elliott Wen, Tharindu Kaluarachchi, Rajib Rana, and Suranga Nanayakkara. 2023. <span>“Improving the Domain Adaptation of Retrieval Augmented Generation (RAG) Models for Open Domain Question Answering.”</span> <em>Transactions of the Association for Computational Linguistics (TACL)</em>. <a href="https://arxiv.org/abs/2210.02627">https://arxiv.org/abs/2210.02627</a>.
</div>
</div></section></div> ]]></description>
  <category>Research foundations of modern LLMs</category>
  <guid>https://yeesengchan.com/posts/series/research-foundations-of-modern-llms/03-retrieval/</guid>
  <pubDate>Fri, 27 Mar 2026 00:00:00 GMT</pubDate>
</item>
</channel>
</rss>
